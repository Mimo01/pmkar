---
phase: quick-260325-pp3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/features/tickets/CopyPreviewPage.tsx]
autonomous: true
requirements: [QUICK-pp3]
must_haves:
  truths:
    - "Copy preview page shows action buttons in only one location (header), not duplicated"
    - "Discard and Confirm Copy buttons remain fully functional"
  artifacts:
    - path: "src/features/tickets/CopyPreviewPage.tsx"
      provides: "Copy preview page with single set of action buttons"
  key_links:
    - from: "CopyPreviewPage header buttons"
      to: "handleDiscard, handleConfirm"
      via: "onClick handlers"
      pattern: "onClick=.handle(Discard|Confirm)"
---

<objective>
Remove the duplicated footer action buttons from the CopyPreviewPage, keeping only the header buttons.

Purpose: The copy preview page currently renders Discard and Confirm Copy buttons in both the header bar and a footer bar. This is redundant and clutters the UI.
Output: CopyPreviewPage.tsx with a single set of action buttons in the header.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/tickets/CopyPreviewPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicated footer buttons from CopyPreviewPage</name>
  <files>src/features/tickets/CopyPreviewPage.tsx</files>
  <action>
In CopyPreviewPage.tsx, remove:

1. The Separator import from `@/components/ui/separator` (line 6) — it is only used for the footer divider.
2. The thin separator before footer (lines 315-316): the `{!isLoading && <Separator ... />}` block.
3. The entire footer action row (lines 318-340): the `{!isLoading && <div className="flex items-center justify-between px-6 py-4 ...">` block containing the duplicate Discard and Confirm buttons.

Keep the header (lines 80-114) unchanged — it already contains the Discard (back arrow) button on the left and the Confirm Copy button on the right.

Verify that the Separator import is not used elsewhere in the file before removing it. If it is used elsewhere, keep the import.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
  </verify>
  <done>CopyPreviewPage renders action buttons only in the header bar. No footer action row exists. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- Open the copy preview page (click Copy on any ticket detail) and confirm buttons appear only at the top
- Both Discard and Confirm Copy buttons work as before
- No footer buttons or separator visible
</verification>

<success_criteria>
- Single set of Discard/Confirm buttons in the header only
- No duplicate footer buttons
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260325-pp3-on-the-issue-copy-page-the-buttons-are-b/260325-pp3-SUMMARY.md`
</output>
