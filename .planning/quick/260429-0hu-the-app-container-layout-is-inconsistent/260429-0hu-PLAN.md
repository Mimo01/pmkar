---
phase: quick-260429-0hu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/tickets/TicketDetailPage.tsx
  - src/features/connections/SettingsPage.tsx
autonomous: true
requirements: [LAYOUT-CONSISTENCY]
must_haves:
  truths:
    - "Ticket detail page content fills the full available width, same as ticket list views"
    - "Settings page content fills the full available width, same as ticket list views"
    - "Neither page feels crammed — existing px-8 gutter provides comfortable reading margin"
    - "Settings sidebar + content layout is unaffected (still side-by-side)"
  artifacts:
    - path: "src/features/tickets/TicketDetailPage.tsx"
      provides: "Full-width scrollable content area without max-w-3xl centering"
    - path: "src/features/connections/SettingsPage.tsx"
      provides: "Full-width content column without max-w-[760px] cap"
  key_links:
    - from: "TicketDetailPage"
      to: "AppShell main"
      via: "flex-1 overflow-y-auto fills shell height"
    - from: "SettingsPage content column"
      to: "flex-1 wrapper"
      via: "flex-1 already present on px-8 py-6 div, inner cap removed"
---

<objective>
Unify all page container layouts so every view uses full-width, edge-to-edge content rendering consistent with the ticket list, ignored tickets, and audit log pages.

Purpose: The app window is a fixed-size desktop app — wasted margins feel broken, not spacious. Full-width layout maximizes the available real estate for content without being crammed because the existing px-8 padding provides sufficient breathing room.

Output: Two files modified. No new files, no new dependencies.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/pmkar/.planning/STATE.md
@/Users/mimo/Documents/Projects/pmkar/.planning/ROADMAP.md

## Layout baseline (for reference — do not modify these)

TicketListPage: `flex-1 flex flex-col overflow-hidden` — no max-w, no mx-auto. Fullscreen.
AuditLogPage: `ScrollArea className="flex-1"` — no max-w. Fullscreen.
AppShell main: `<main className="flex-1 flex flex-col overflow-hidden">` — children fill it.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove centered narrow container from TicketDetailPage</name>
  <files>src/features/tickets/TicketDetailPage.tsx</files>
  <action>
In the loaded detail JSX return (around line 221), find the scrollable content wrapper:

```tsx
<div className="flex-1 overflow-y-auto">
  <div className="max-w-3xl mx-auto px-8 py-6">
```

Remove the inner div with `max-w-3xl mx-auto px-8 py-6` and move its padding onto the outer scroll div, making it:

```tsx
<div className="flex-1 overflow-y-auto px-8 py-6">
```

The children (h1, action buttons, tab bar, tab panels) are placed directly inside the scroll container now. No other structural changes — do not touch the header div, the loading states, or the error state (those already use `flex-1 flex flex-col` without max-w and are fine). Also apply the same px-8 py-6 change to the loading state's content area if it uses a separate inner wrapper (check lines 157–176 and 178–197 — they don't have a narrow container so leave them as-is).
  </action>
  <verify>
    <automated>grep -n "max-w-3xl\|mx-auto" /Users/mimo/Documents/Projects/pmkar/src/features/tickets/TicketDetailPage.tsx | grep -v "^.*#" || echo "CLEAN"</automated>
  </verify>
  <done>No max-w-3xl or mx-auto in TicketDetailPage. Content div is `flex-1 overflow-y-auto px-8 py-6` with children rendered directly inside.</done>
</task>

<task type="auto">
  <name>Task 2: Remove max-width cap from SettingsPage content column</name>
  <files>src/features/connections/SettingsPage.tsx</files>
  <action>
In SettingsPage's return JSX (around line 1223–1226), find the section content wrapper:

```tsx
{/* Section content — scrollable */}
<div className="flex-1 overflow-y-auto px-8 py-6">
  <div className="max-w-[760px]">{renderContent()}</div>
</div>
```

Remove the inner `<div className="max-w-[760px]">` wrapper entirely, rendering `{renderContent()}` directly inside the scroll div:

```tsx
{/* Section content — scrollable */}
<div className="flex-1 overflow-y-auto px-8 py-6">
  {renderContent()}
</div>
```

The sidebar nav (w-40) and this content column already sit in a `flex flex-1 overflow-hidden` parent — removing the cap makes the content column fill its available flex space, matching fullscreen views. The sidebar width is unaffected.
  </action>
  <verify>
    <automated>grep -n "max-w-\[760px\]\|max-w-\[" /Users/mimo/Documents/Projects/pmkar/src/features/connections/SettingsPage.tsx || echo "CLEAN"</automated>
  </verify>
  <done>No max-w-[760px] in SettingsPage. renderContent() rendered directly inside flex-1 overflow-y-auto px-8 py-6 container. Settings sidebar layout unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS layout | Pure visual change — no data flow, no trust boundary affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-0hu-01 | Tampering | Layout CSS | accept | Pure visual refactor; no authentication, data, or logic paths involved |
</threat_model>

<verification>
Run the dev server and open the app. Navigate to:
1. Ticket list (main view) — baseline, should be unchanged
2. Any ticket detail — content should now span full available width with comfortable px-8 gutter on each side
3. Settings — sidebar nav on left (w-40), content fills remaining width with px-8 gutter on each side

All three views should have visually consistent content width. No scrollbars broken, no content cut off.
</verification>

<success_criteria>
- Ticket detail page: content spans full width within px-8 gutters, matching ticket list visual weight
- Settings page: section content fills the panel right of the sidebar, no narrow centering
- No regressions: tab switching works, settings navigation works, action buttons render correctly
- No TypeScript or build errors: `npm run build` or `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/quick/260429-0hu-the-app-container-layout-is-inconsistent/260429-0hu-01-SUMMARY.md`
</output>
