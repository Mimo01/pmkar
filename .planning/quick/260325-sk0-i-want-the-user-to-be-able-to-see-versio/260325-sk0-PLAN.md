---
phase: quick
plan: 260325-sk0
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - package.json
  - src/features/update/VersionHistoryModal.tsx
  - src/features/update/AboutSection.tsx
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: true
requirements: [QUICK]
must_haves:
  truths:
    - "User can open a version history view from the About modal"
    - "User can see a list of versions with their changelogs grouped by category"
    - "Changelog is generated from git history using git-cliff"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Generated changelog from git history"
    - path: "src/features/update/VersionHistoryModal.tsx"
      provides: "Version history modal with parsed changelog display"
  key_links:
    - from: "src/features/update/AboutSection.tsx"
      to: "src/features/update/VersionHistoryModal.tsx"
      via: "View Version History button opens VersionHistoryModal"
---

<objective>
Add a version history and changelog viewer to the app. Users can see all past releases with categorized change lists, accessible from the About modal.

Purpose: Let users understand what changed across versions without leaving the app.
Output: CHANGELOG.md generated from git history, VersionHistoryModal component, updated About modal with "Version History" button.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/update/AboutModal.tsx
@src/features/update/AboutSection.tsx
@src/features/update/updateStore.ts
@src/i18n/locales/en.json
@src/i18n/locales/sk.json
@cliff.toml

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/features/update/AboutModal.tsx:
```typescript
interface AboutModalProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
// Renders <AboutSection /> inside a Dialog
```

From src/features/update/AboutSection.tsx:
```typescript
// Standalone component showing version, last checked, update status
// Uses useUpdateStore for status, getVersion() from @tauri-apps/api/app
```

From cliff.toml:
```toml
# git-cliff is already configured with conventional commit parsing
# Groups: Features, Bug Fixes, Refactoring, Performance, Testing, Documentation, Miscellaneous, CI/CD
# Tag pattern: v[0-9]*
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate CHANGELOG.md and add npm script</name>
  <files>CHANGELOG.md, package.json</files>
  <action>
    1. Run `npx git-cliff --config cliff.toml -o CHANGELOG.md` to generate the full changelog from git history using the existing cliff.toml config.

    2. Add a `"changelog"` script to package.json scripts: `"changelog": "git cliff --config cliff.toml -o CHANGELOG.md"` so it can be regenerated before releases.

    3. Verify CHANGELOG.md was generated with content (it should have at least the v0.1.0 tag's changes).
  </action>
  <verify>
    <automated>test -s CHANGELOG.md && grep -q "v1" CHANGELOG.md && echo "CHANGELOG exists with content"</automated>
  </verify>
  <done>CHANGELOG.md exists with version history content, package.json has changelog script</done>
</task>

<task type="auto">
  <name>Task 2: Create VersionHistoryModal and wire into AboutSection</name>
  <files>src/features/update/VersionHistoryModal.tsx, src/features/update/AboutSection.tsx, src/i18n/locales/en.json, src/i18n/locales/sk.json</files>
  <action>
    1. Create `src/features/update/VersionHistoryModal.tsx`:
       - Import CHANGELOG.md as raw text using Vite's `?raw` suffix: `import changelogRaw from '../../../CHANGELOG.md?raw'`
       - Parse the markdown into version sections by splitting on `## [vX.Y.Z]` or `## [X.Y.Z]` heading patterns (each heading = a version entry)
       - For each version section, extract the version number and the body content
       - Render in a Dialog (same pattern as AboutModal — use shadcn Dialog, DialogContent, DialogHeader, DialogTitle, ScrollArea)
       - Max width ~520px, ScrollArea max-h-[60vh] for the changelog content
       - Each version gets a heading with the version number as a Badge (bg-brand), and the body rendered as grouped changelog items
       - Parse the markdown body simply: `### Category` headings become bold category labels, `- item` lines become list items underneath
       - Use existing app design tokens (text-brand-text-secondary, text-brand-muted, border-brand-border, etc.)
       - Props: `{ open: boolean; onOpenChange: (open: boolean) => void }`

    2. Update `src/features/update/AboutSection.tsx`:
       - Add state: `const [showHistory, setShowHistory] = useState(false)`
       - Add a "Version History" button below the version display row — use a text-style button (variant="link" or a simple `button` with `text-brand hover:underline text-[13px]` classes), with a `History` lucide icon (w-3.5 h-3.5)
       - Render `<VersionHistoryModal open={showHistory} onOpenChange={setShowHistory} />` at the bottom of the component
       - Import useState from react (already imported)

    3. Add i18n keys to en.json:
       - `"about.versionHistory": "Version History"`
       - `"about.versionHistory.title": "Version History"`
       - `"about.versionHistory.noEntries": "No version history available"`

    4. Add i18n keys to sk.json:
       - `"about.versionHistory": "Historia verzii"`
       - `"about.versionHistory.title": "Historia verzii"`
       - `"about.versionHistory.noEntries": "Historia verzii nie je dostupna"`
       - Use proper Slovak diacritics: "Historia verzii" -> "Historia verzii", "nie je dostupna" -> "nie je dostupna" (verify diacritics match existing sk.json style with full diacritics like "Skontrolovať", "aktualizácie")
       - Correct Slovak: `"about.versionHistory": "História verzií"`, `"about.versionHistory.title": "História verzií"`, `"about.versionHistory.noEntries": "História verzií nie je dostupná"`

    5. For the Vite raw import to work with TypeScript, ensure there is a type declaration. Check if `src/vite-env.d.ts` already has `/// <reference types="vite/client" />` (it should from Phase 10 decisions). If the `*.md?raw` import causes a TS error, add a module declaration: `declare module '*.md?raw' { const content: string; export default content; }` to vite-env.d.ts.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -q "versionHistory" src/i18n/locales/en.json && echo "i18n keys present"</automated>
  </verify>
  <done>VersionHistoryModal renders changelog content parsed from CHANGELOG.md. AboutSection has a "Version History" button that opens the modal. Both en.json and sk.json have the new translation keys with proper diacritics.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors (confirms Vite raw import works)
- About modal shows "Version History" button
- Clicking "Version History" opens modal with changelog entries grouped by version
- Slovak translations use proper diacritics
</verification>

<success_criteria>
User can open the About modal, click "Version History", and see a scrollable list of all versions with their categorized changes. The changelog is generated from actual git history using git-cliff.
</success_criteria>

<output>
After completion, create `.planning/quick/260325-sk0-i-want-the-user-to-be-able-to-see-versio/260325-sk0-SUMMARY.md`
</output>
