---
phase: quick
plan: 260325-k6l
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/tickets/TicketFilterBar.tsx
  - src/features/tickets/TicketListPage.tsx
  - src/features/tickets/IgnoredTicketsPage.tsx
  - src/features/tickets/LinkedTicketsPage.tsx
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Filter bar looks sleek and polished with compact, modern styling"
    - "Assignee filter is an autocomplete dropdown pulling real users from Jira API"
    - "User can type in assignee field and see matching users from Jira"
    - "Selecting a user from dropdown filters tickets by that assignee"
    - "Clearing assignee filter shows all tickets again"
    - "Filter bar works identically on all three pages (TicketList, Ignored, Linked)"
  artifacts:
    - path: "src/features/tickets/TicketFilterBar.tsx"
      provides: "Sleek filter bar with assignee autocomplete dropdown"
  key_links:
    - from: "src/features/tickets/TicketFilterBar.tsx"
      to: "search_jira_users Tauri command"
      via: "invoke with debounced query"
      pattern: "invoke.*search_jira_users"
    - from: "TicketListPage/IgnoredTicketsPage/LinkedTicketsPage"
      to: "TicketFilterBar"
      via: "assigneeFilter prop for filtering tickets"
      pattern: "assigneeFilter"
---

<objective>
Make the ticket filter bar more sleek and polished. Replace the plain text search with a split design: a key search input and a separate assignee autocomplete dropdown that queries real Jira users via the existing `search_jira_users` Tauri command.

Purpose: Improve filter UX from a single text field to purpose-built controls — key search stays as text input, assignee gets a proper autocomplete with real user data from Jira.
Output: Updated TicketFilterBar with assignee autocomplete, updated consumer pages.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/tickets/TicketFilterBar.tsx
@src/features/tickets/TicketListPage.tsx
@src/features/tickets/IgnoredTicketsPage.tsx
@src/features/tickets/LinkedTicketsPage.tsx
@src/features/tickets/types.ts
@src/features/connections/SettingsPage.tsx (lines 140-180 — reference for search_jira_users invoke pattern)
@src/i18n/locales/en.json
@src/i18n/locales/sk.json

<interfaces>
<!-- Existing Tauri command for user search -->
search_jira_users(base_url: String, query: String) -> Vec<serde_json::Value>
// Returns array of objects with: { name, displayName, emailAddress? }

<!-- JiraUser type from types.ts -->
export interface JiraUser {
  name?: string;       // Server v2
  accountId?: string;  // Cloud v3
  displayName: string;
}

<!-- Current TicketFilterBar props -->
interface TicketFilterBarProps {
  searchText: string;
  onSearchChange: (v: string) => void;
  sortDirection: 'asc' | 'desc';
  onToggleSort: () => void;
  resultCount: number;
}

<!-- connectionStore provides serverConnection.baseUrl for API calls -->
useConnectionStore((s) => s.serverConnection)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign TicketFilterBar with assignee autocomplete</name>
  <files>src/features/tickets/TicketFilterBar.tsx, src/i18n/locales/en.json, src/i18n/locales/sk.json</files>
  <action>
Redesign TicketFilterBar.tsx with these changes:

**New props interface:**
```typescript
interface TicketFilterBarProps {
  searchText: string;
  onSearchChange: (v: string) => void;
  assigneeFilter: string | null;        // displayName of selected user, or null
  onAssigneeChange: (v: string | null) => void;
  sortDirection: 'asc' | 'desc';
  onToggleSort: () => void;
  resultCount: number;
}
```

**Layout:** Single row, compact height. From left to right:
1. Key search input (Search icon, same as current but narrower max-w-[200px])
2. Assignee autocomplete dropdown (User icon from lucide, max-w-[220px])
3. Sort toggle button (same as current)
4. Result count (same as current, pushed to right with ml-auto)

**Assignee autocomplete implementation:**
- Use a custom dropdown (div-based, not native select) — position absolute below input
- On typing, debounce 250ms, then invoke `search_jira_users` with `baseUrl` from `useConnectionStore` and the query text
- Show dropdown with matching users (displayName + emailAddress if available)
- On selecting a user, call `onAssigneeChange(user.displayName)` and close dropdown
- Show selected user as a chip/tag with X button to clear
- When cleared, call `onAssigneeChange(null)`
- Keyboard support: arrow up/down to navigate, Enter to select, Escape to close
- Close dropdown on click outside (useEffect with document click listener)
- Follow the exact same pattern from SettingsPage.tsx lines 140-180 for the debounced invoke

**Styling — sleek and polished:**
- Reduce vertical padding: py-1.5 instead of py-2 on container
- All inputs: h-8 (32px) for compact feel
- Subtle bg-brand-surface/50 on inputs instead of full bg-brand-surface
- Border radius rounded-md on everything
- Dropdown: bg-brand-surface border border-brand-border rounded-md shadow-lg mt-1, max-h-48 overflow-y-auto
- Dropdown items: px-3 py-1.5 text-sm hover:bg-brand/10 cursor-pointer
- Selected item highlight with bg-brand/10
- Assignee chip when selected: inline-flex items-center gap-1 bg-brand/10 text-brand text-xs rounded-full px-2.5 py-0.5
- Smooth transitions on all interactive elements

**i18n keys to add (both en.json and sk.json):**
- `tickets.filter.assigneePlaceholder`: "Filter by assignee..." / "Filtrovať podľa priradenia..."
- `tickets.filter.assigneeLabel`: "Filter by assignee" / "Filtrovať podľa priradenia"
- `tickets.filter.noUsersFound`: "No users found" / "Žiadni používatelia nenájdení"
- `tickets.filter.clearAssignee`: "Clear assignee filter" / "Vymazať filter priradenia"

Update the existing `tickets.filter.placeholder` to "Filter by key..." / "Filtrovať podľa kľúča..." (since assignee now has its own field).
Update `tickets.filter.searchLabel` to "Filter tickets by key" / "Filtrovať tikety podľa kľúča".
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>TicketFilterBar has sleek compact design with key search input and assignee autocomplete dropdown that queries Jira API. All i18n keys added in both languages.</done>
</task>

<task type="auto">
  <name>Task 2: Update consumer pages to use assignee filter</name>
  <files>src/features/tickets/TicketListPage.tsx, src/features/tickets/IgnoredTicketsPage.tsx, src/features/tickets/LinkedTicketsPage.tsx</files>
  <action>
Update all three pages that use TicketFilterBar to support the new assigneeFilter prop.

**In each page (TicketListPage, IgnoredTicketsPage, LinkedTicketsPage):**

1. Add state: `const [assigneeFilter, setAssigneeFilter] = useState<string | null>(null);`

2. Update the filtering logic in the `useMemo` that produces sorted/filtered tickets. Currently the search filters by key OR assignee displayName. Change to:
   - `searchText` filters ONLY by ticket key (ticket.key.toLowerCase().includes(lower))
   - `assigneeFilter` filters by exact assignee displayName match (case-insensitive): `(ticket.fields.assignee?.displayName ?? '').toLowerCase() === assigneeFilter.toLowerCase()`
   - Both filters are AND-combined (both must match if both are set)

3. Pass new props to TicketFilterBar:
   ```tsx
   <TicketFilterBar
     searchText={searchText}
     onSearchChange={setSearchText}
     assigneeFilter={assigneeFilter}
     onAssigneeChange={setAssigneeFilter}
     sortDirection={sortDirection}
     onToggleSort={() => setSortDirection((d) => (d === 'desc' ? 'asc' : 'desc'))}
     resultCount={sorted.length}   // or sortedCandidates.length for TicketListPage
   />
   ```

4. Add `assigneeFilter` to the useMemo dependency arrays.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three ticket list pages pass assigneeFilter to TicketFilterBar and use separate key/assignee filtering logic. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. App loads and filter bar appears on all three tabs
3. Key search filters by ticket key only
4. Typing in assignee field shows dropdown with real Jira users (from mock server)
5. Selecting a user filters tickets to that assignee
6. Clearing assignee shows all tickets again
7. Sort toggle still works
8. Result count updates correctly with combined filters
</verification>

<success_criteria>
- Filter bar is visually sleeker: compact height, clean layout, polished styling
- Assignee autocomplete queries real Jira users via search_jira_users command
- Key search and assignee filter work independently and combine with AND logic
- Keyboard navigation works in assignee dropdown (arrows, enter, escape)
- All three pages (TicketList, Ignored, Linked) have the updated filter bar
- Both EN and SK translations are complete
</success_criteria>

<output>
After completion, create `.planning/quick/260325-k6l-make-ticket-filter-bar-sleek-with-assign/260325-k6l-SUMMARY.md`
</output>
