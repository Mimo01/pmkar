---
phase: quick
plan: 260323-w2c
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/ui/AppShell.tsx
  - src/features/tickets/TicketListPage.tsx
  - src/features/tickets/LinkedTicketsPage.tsx
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: false
requirements: []

must_haves:
  truths:
    - "New tab shows only new+seen tickets (not ignored, not copied)"
    - "Not Mine tab shows ignored tickets with restore action"
    - "Already Linked tab shows copied tickets with copiedKey link"
    - "Copying a ticket moves it from New to Already Linked tab"
    - "Ignoring a ticket moves it from New to Not Mine tab"
  artifacts:
    - path: "src/components/ui/AppShell.tsx"
      provides: "3-tab navigation with i18n labels"
      contains: "'new' | 'not-mine' | 'linked'"
    - path: "src/features/tickets/LinkedTicketsPage.tsx"
      provides: "Already Linked tab page component"
      contains: "state === 'copied'"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/ui/AppShell.tsx"
      via: "tab type union and activeTab prop"
      pattern: "'new' \\| 'not-mine' \\| 'linked'"
    - from: "src/App.tsx"
      to: "src/features/tickets/LinkedTicketsPage.tsx"
      via: "currentTab === 'linked' conditional render"
      pattern: "currentTab === 'linked'"
---

<objective>
Redesign homepage tab system from 2 tabs (Tickets/Ignored) to 3 tabs (New/Not Mine/Already Linked) with proper ticket filtering by triage state.

Purpose: Users need clear separation between new tickets to triage, tickets they've dismissed, and tickets already copied to company Jira.
Output: Updated tab navigation, new LinkedTicketsPage component, corrected ticket filtering.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.tsx
@src/components/ui/AppShell.tsx
@src/features/tickets/TicketListPage.tsx
@src/features/tickets/IgnoredTicketsPage.tsx
@src/features/tickets/ticketStore.ts
@src/features/tickets/types.ts
@src/i18n/locales/en.json
@src/i18n/locales/sk.json

<interfaces>
<!-- Current types the executor needs -->

From src/features/tickets/types.ts:
```typescript
export type TriageState = 'new' | 'seen' | 'ignored' | 'copied';
export interface TriageEntry {
  state: TriageState;
  copiedKey: string | null;
}
```

From src/features/tickets/ticketStore.ts:
```typescript
// Store shape (relevant fields):
triageMap: Record<string, TriageEntry>;
tickets: JiraTicket[];
newCount: number;
selectedTicketKey: string | null;
```

Tab type currently in AppShell.tsx:
```typescript
activeTab?: 'tickets' | 'ignored';
onTabChange?: (tab: 'tickets' | 'ignored') => void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tab type, routing, filters, and create LinkedTicketsPage</name>
  <files>
    src/App.tsx
    src/components/ui/AppShell.tsx
    src/features/tickets/TicketListPage.tsx
    src/features/tickets/LinkedTicketsPage.tsx
    src/i18n/locales/en.json
    src/i18n/locales/sk.json
  </files>
  <action>
    **Tab type change (AppShell.tsx):**
    - Change tab type union from `'tickets' | 'ignored'` to `'new' | 'not-mine' | 'linked'` in AppShellProps interface (both `activeTab` and `onTabChange` parameter)
    - Update NAV_TABS array to 3 entries:
      - `{ id: 'new', label: t('nav.new') }`
      - `{ id: 'not-mine', label: t('nav.notMine') }`
      - `{ id: 'linked', label: t('nav.linked') }`

    **App.tsx routing:**
    - Change useState type from `'tickets' | 'ignored'` to `'new' | 'not-mine' | 'linked'`, default `'new'`
    - Import LinkedTicketsPage
    - Update render conditionals:
      - `currentTab === 'new'` renders `<TicketListPage />`
      - `currentTab === 'not-mine'` renders `<IgnoredTicketsPage />`
      - `currentTab === 'linked'` renders `<LinkedTicketsPage />`

    **TicketListPage.tsx filter fix:**
    - Change candidateTickets filter from:
      `tickets.filter((t) => triageMap[t.key]?.state !== 'ignored')`
      to:
      `tickets.filter((t) => { const s = triageMap[t.key]?.state; return s !== 'ignored' && s !== 'copied'; })`
    - This ensures copied tickets no longer appear in the New tab
    - Also update the useEffect that closes the detail panel: close when state is 'ignored' OR 'copied'
      (add `|| triageMap[selectedTicketKey]?.state === 'copied'` to the condition)

    **New LinkedTicketsPage.tsx:**
    - Create by copying IgnoredTicketsPage.tsx structure as starting point
    - Filter: `tickets.filter((t) => triageMap[t.key]?.state === 'copied')`
    - Same table columns as IgnoredTicketsPage (key, summary, status, priority, assignee, updated)
    - Replace the "Restore" action column with a "Linked Key" column that shows `triageMap[ticket.key]?.copiedKey` as a text badge (not a button)
    - Empty state: use i18n keys `linked.empty` / `linked.empty.hint`
    - Column header i18n keys: reuse `ignored.col.*` pattern but use `linked.col.*` keys (key, summary, status, priority, assignee, updated, linkedKey)
    - Sort by updated DESC like IgnoredTicketsPage

    **i18n updates (en.json):**
    - Add: `"nav.new": "New"`
    - Add: `"nav.notMine": "Not Mine"`
    - Add: `"nav.linked": "Already Linked"`
    - Add: `"linked.empty": "No linked tickets"`
    - Add: `"linked.empty.hint": "Tickets you copy to Company Jira will appear here."`
    - Add: `"linked.col.key": "Key"`, `"linked.col.summary": "Summary"`, `"linked.col.status": "Status"`, `"linked.col.priority": "Priority"`, `"linked.col.assignee": "Assignee"`, `"linked.col.updated": "Updated"`, `"linked.col.linkedKey": "Linked As"`
    - Keep existing `nav.tickets` and `nav.ignored` keys (other components may reference them) but they are no longer used in tabs

    **i18n updates (sk.json):**
    - Add: `"nav.new": "Nove"`
    - Add: `"nav.notMine": "Nie moje"`
    - Add: `"nav.linked": "Uz prelinkovane"`
    - Add: `"linked.empty": "Ziadne prelinkovane listky"`
    - Add: `"linked.empty.hint": "Listky skopirovane do Jira spolocnosti sa objavia tu."`
    - Add: `"linked.col.key": "Kluc"`, `"linked.col.summary": "Zhrnutie"`, `"linked.col.status": "Stav"`, `"linked.col.priority": "Priorita"`, `"linked.col.assignee": "Zodpovedny"`, `"linked.col.updated": "Aktualizovane"`, `"linked.col.linkedKey": "Prelinkovane ako"`
    - Use proper Slovak diacritics (matching existing file patterns with unicode escapes)
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - AppShell renders 3 tabs: New, Not Mine, Already Linked
    - App.tsx routes to correct page component per tab
    - TicketListPage only shows new+seen tickets (excludes ignored AND copied)
    - LinkedTicketsPage shows copied tickets with their copiedKey
    - All i18n keys present in both en.json and sk.json
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Redesigned 3-tab navigation (New / Not Mine / Already Linked) with proper ticket filtering by triage state</what-built>
  <how-to-verify>
    1. Run `npm run dev` (or `cargo tauri dev`) to start the app
    2. Fetch tickets — verify the "New" tab only shows new/seen tickets (not copied ones)
    3. Click "Not Mine" tab — verify it shows ignored tickets with Restore button
    4. Click "Already Linked" tab — verify it shows copied tickets with their linked Jira key
    5. From New tab, open a ticket detail and click "Not Mine" — verify ticket disappears from New and appears in Not Mine tab
    6. From New tab, open a ticket detail and copy it — verify ticket disappears from New and appears in Already Linked tab
    7. Switch language to Slovak — verify all 3 tab labels are translated
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All 3 tabs render without crashes
- Ticket counts in FetchBar reflect only new+seen tickets
</verification>

<success_criteria>
- 3-tab navigation replaces old 2-tab system
- Each tab shows correct subset of tickets based on triage state
- Tickets move between tabs when their triage state changes
- i18n complete for both English and Slovak
</success_criteria>

<output>
After completion, create `.planning/quick/260323-w2c-homepage-shows-only-new-tickets-with-not/260323-w2c-SUMMARY.md`
</output>
