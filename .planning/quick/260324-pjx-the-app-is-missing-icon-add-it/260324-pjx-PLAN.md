---
phase: quick
plan: 260324-pjx
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/icons/app-icon.svg
  - src-tauri/icons/icon.png
  - src-tauri/icons/128x128.png
  - src-tauri/icons/128x128@2x.png
  - src-tauri/icons/32x32.png
  - src-tauri/icons/icon.icns
  - src-tauri/icons/icon.ico
  - src-tauri/tauri.conf.json
  - index.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "App has a recognizable brand icon visible in OS dock/taskbar"
    - "App icon uses the brand red #c02232 color"
    - "All platform icon formats generated (png, ico, icns)"
    - "Tauri config references the generated icons"
  artifacts:
    - path: "src-tauri/icons/app-icon.svg"
      provides: "Source SVG icon for generation"
    - path: "src-tauri/tauri.conf.json"
      provides: "Bundle icon paths configured"
      contains: "icon.png"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/icons/"
      via: "bundle.icon array"
      pattern: "icon.*png"
---

<objective>
Create and configure a proper app icon for the pmkar Tauri application.

Purpose: The app currently has placeholder blue square icons and an empty icon array in tauri.conf.json. It needs a real branded icon that displays in the OS dock, taskbar, and window title bar.
Output: SVG source icon, all generated platform variants, configured Tauri bundle.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src-tauri/tauri.conf.json
@src/index.css (brand colors: --color-brand: #c02232)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SVG app icon and generate all platform variants</name>
  <files>src-tauri/icons/app-icon.svg, src-tauri/icons/* (generated)</files>
  <action>
Create an SVG icon for the pmkar app. The design should be:
- A rounded square shape (like modern app icons)
- Background: brand red #c02232
- Foreground: white letter "P" in a bold, modern sans-serif style (representing "pmkar")
- Clean, minimal, recognizable at small sizes (16x16 through 1024x1024)
- The "P" should be slightly stylized — perhaps with a subtle arrow or forward-motion element to suggest productivity/movement

Save as `src-tauri/icons/app-icon.svg` (1024x1024 viewBox).

Then run `npx tauri icon src-tauri/icons/app-icon.svg` to generate all platform icon variants (png, ico, icns) into `src-tauri/icons/`.

This replaces the existing placeholder blue square PNGs.
  </action>
  <verify>
    <automated>ls -la src-tauri/icons/*.png src-tauri/icons/*.ico src-tauri/icons/*.icns 2>/dev/null | wc -l | xargs test 3 -le</automated>
  </verify>
  <done>SVG source icon exists, tauri icon command generated all platform variants (at minimum: icon.png, icon.ico, icon.icns, 128x128.png, 128x128@2x.png, 32x32.png)</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tauri bundle and HTML favicon to use new icon</name>
  <files>src-tauri/tauri.conf.json, index.html</files>
  <action>
1. Update `src-tauri/tauri.conf.json` bundle.icon array to reference the generated icons:
   ```json
   "icon": [
     "icons/32x32.png",
     "icons/128x128.png",
     "icons/128x128@2x.png",
     "icons/icon.icns",
     "icons/icon.ico",
     "icons/icon.png"
   ]
   ```
   Include all PNG sizes that were generated by the tauri icon command. The paths are relative to src-tauri/.

2. Copy the smallest generated PNG (or the SVG) to `public/` for use as the web favicon. Update `index.html` to reference the app icon instead of the default Vite SVG:
   - Change `<link rel="icon" type="image/svg+xml" href="/vite.svg" />` to reference the new icon
   - If using SVG: copy app-icon.svg to public/app-icon.svg, reference as `/app-icon.svg`
   - If using PNG: copy 32x32.png to public/favicon.png, reference as `/favicon.png` with type `image/png`

3. Remove the default `/public/vite.svg` if it exists.
  </action>
  <verify>
    <automated>node -e "const c = require('./src-tauri/tauri.conf.json'); if (!c.bundle.icon.length) throw new Error('empty icon array'); console.log('icons configured:', c.bundle.icon.length)"</automated>
  </verify>
  <done>tauri.conf.json bundle.icon populated with all icon paths, index.html favicon points to app icon instead of vite.svg, vite.svg removed</done>
</task>

</tasks>

<verification>
- `npx tauri icon` generated all expected platform formats
- `src-tauri/tauri.conf.json` bundle.icon is non-empty and references real files
- `index.html` no longer references vite.svg
- All referenced icon files exist on disk
</verification>

<success_criteria>
- App icon is a branded red "P" icon, not a placeholder blue square
- All platform icon variants exist (png at multiple sizes, ico, icns)
- Tauri config references the icons so they are bundled in builds
- Web favicon updated to match app icon
</success_criteria>

<output>
After completion, create `.planning/quick/260324-pjx-the-app-is-missing-icon-add-it/260324-pjx-SUMMARY.md`
</output>
