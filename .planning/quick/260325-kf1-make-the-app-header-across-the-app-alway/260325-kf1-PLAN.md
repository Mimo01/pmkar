---
phase: quick
plan: 260325-kf1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/AppShell.tsx
  - src/App.tsx
autonomous: true
requirements: [QUICK-kf1]
must_haves:
  truths:
    - "App header (pmkar logo + icon toolbar) is always visible at the top of the viewport, never scrolls away"
    - "Header is visible on all app routes including wizard, settings, audit log, detail pages, and main ticket list"
    - "Content below the header scrolls independently while header stays pinned"
  artifacts:
    - path: "src/components/ui/AppShell.tsx"
      provides: "Fixed-height viewport shell with non-scrolling header"
    - path: "src/App.tsx"
      provides: "All routes wrapped in AppShell so header always shows"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/ui/AppShell.tsx"
      via: "Every render branch uses AppShell wrapper"
      pattern: "<AppShell"
---

<objective>
Make the app header always visible at the top of the viewport across all routes.

Purpose: Currently the header can scroll off-screen when content is tall, and the setup wizard route does not show the header at all. The header should be pinned to the top and visible everywhere.
Output: AppShell with fixed header, all App.tsx routes wrapped in AppShell.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/ui/AppShell.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin AppShell header to viewport top and ensure content scrolls independently</name>
  <files>src/components/ui/AppShell.tsx</files>
  <action>
In AppShell.tsx, change the root div from `min-h-screen` to `h-screen` and add `overflow-hidden`. This constrains the shell to exactly viewport height. The flex-col layout already makes the header take its natural height, and main with `flex-1 overflow-hidden` already clips. Child pages handle their own `overflow-y-auto` scrolling.

Specific change on line 32:
- FROM: `className="min-h-screen bg-brand-bg text-brand-text flex flex-col"`
- TO: `className="h-screen bg-brand-bg text-brand-text flex flex-col overflow-hidden"`

This single change pins the header because the container can no longer grow beyond the viewport — the header stays at top, main fills remaining space, and child content scrolls within main.

No other changes needed in AppShell — the flex layout already works correctly once the container is viewport-constrained.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>AppShell root is h-screen overflow-hidden; header will never scroll off-screen.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap wizard route in AppShell so header shows on all routes</name>
  <files>src/App.tsx</files>
  <action>
In App.tsx, the wizard branch (lines 113-121) renders SetupWizard without AppShell, so the header is absent. Wrap it in AppShell (without nav/audit/gear props, same as the settings and audit branches):

Change the wizard block from:
```tsx
return (
  <ErrorBoundary>
    <SetupWizard initialStep={initialStep} onComplete={() => setEditStep(null)} />
    <UpdateModal open={showUpdateModal} />
  </ErrorBoundary>
);
```

To:
```tsx
return (
  <ErrorBoundary>
    <AppShell>
      <SetupWizard initialStep={initialStep} onComplete={() => setEditStep(null)} />
    </AppShell>
    <UpdateModal open={showUpdateModal} />
  </ErrorBoundary>
);
```

The wizard currently uses its own `min-h-screen` wrapper (SetupWizard.tsx line 130). Since AppShell now provides the full-height container, the wizard's `min-h-screen` on its inner div will work within the flex-1 main area — no change needed in SetupWizard.tsx, the `flex items-center justify-center min-h-screen` will simply fill the available main area (which is full viewport minus header).

Actually, the wizard's `min-h-screen` could cause it to overflow. Change it to `min-h-0 flex-1` or just rely on the parent. Check SetupWizard.tsx root div — if it uses `min-h-screen`, that needs to become `flex-1` since it is now a child of AppShell's main (which is flex-1 flex flex-col overflow-hidden). Update SetupWizard.tsx line 130:
- FROM: `className="flex items-center justify-center min-h-screen bg-brand-bg"`
- TO: `className="flex items-center justify-center flex-1 bg-brand-bg"`

Files additionally modified: src/features/connections/SetupWizard.tsx
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -20 && grep -c "AppShell" src/App.tsx</automated>
  </verify>
  <done>All five App.tsx render branches use AppShell. The header (pmkar logo) is visible on every route including the setup wizard. Wizard content is centered within the remaining viewport space below the header.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Every return branch in App.tsx wraps content in `<AppShell>`
3. AppShell root div uses `h-screen overflow-hidden` (not min-h-screen)
4. Existing tests still pass: `npx vitest run --reporter=verbose 2>&1 | tail -20`
</verification>

<success_criteria>
- Header with "pmkar" logo is always pinned to the top of the viewport on every route
- Long content pages scroll within the main area, header stays fixed
- Setup wizard shows the header bar above the centered wizard content
- No TypeScript errors, existing tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260325-kf1-make-the-app-header-across-the-app-alway/260325-kf1-SUMMARY.md`
</output>
