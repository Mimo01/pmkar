---
phase: 260430-0l2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: true
requirements:
  - QT-260430-0l2-01
must_haves:
  truths:
    - "On the Watched Users settings panel, the user can read text that explicitly states the currently signed-in Jira user is automatically included in 'All watched users' results, even when not added to the watched list."
    - "Both English and Slovak locales render the updated hint text — switching language preserves the clarification."
  artifacts:
    - path: "src/i18n/locales/en.json"
      provides: "Updated `settings.watchedUsers.hint` string clarifying current-user auto-inclusion"
      contains: "settings.watchedUsers.hint"
    - path: "src/i18n/locales/sk.json"
      provides: "Slovak translation of the updated hint kept in sync with EN"
      contains: "settings.watchedUsers.hint"
  key_links:
    - from: "src/features/connections/SettingsPage.tsx (case 'watched-users', line ~786)"
      to: "src/i18n/locales/{en,sk}.json — settings.watchedUsers.hint"
      via: "t('settings.watchedUsers.hint')"
      pattern: "settings\\.watchedUsers\\.hint"
---

<objective>
Clarify the Watched Users settings hint so the user knows that their own Jira account is automatically included whenever the "All watched users" filter runs — even if they don't add themselves to the watched list.

Purpose: The current hint omits a real, code-level behavior (`build_poll_jql` `all_watched` arm in `src-tauri/src/poll_engine.rs` line 347 unions `build_mine_clauses(username)` with watched-user clauses). Users can't tell from the UI alone, which leads to confusion about why their own tickets appear and whether they need to add themselves to the list.

Output: Updated `settings.watchedUsers.hint` string in both `en.json` and `sk.json`. The existing render in `SettingsPage.tsx` (line 786) already uses the `text-[12px] text-brand-muted` styling pattern — no JSX changes required.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/i18n/locales/en.json
@src/i18n/locales/sk.json
@src/features/connections/SettingsPage.tsx

<interfaces>
<!-- The hint is rendered via i18next at SettingsPage.tsx line 786:

    <p className="text-[12px] text-brand-muted mb-6">{t('settings.watchedUsers.hint')}</p>

  No JSX/markup change needed. We simply update the i18n string content in both locales.

  Confirmed behavior in src-tauri/src/poll_engine.rs (line 347):

    "all_watched" => {
        let mine = build_mine_clauses(username);
        if watched_users.is_empty() {
            return wrap_with_project(&mine.join(" OR "), source_project_key);
        }
        let watched: Vec<String> = watched_users.iter().flat_map(...).collect();
        // mine clauses unioned with watched clauses
    }

  → Current Jira user is ALWAYS in the result set when preset = "all_watched", regardless
    of whether the watched list is empty or populated.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update settings.watchedUsers.hint in EN and SK locales</name>
  <files>src/i18n/locales/en.json, src/i18n/locales/sk.json</files>
  <action>
    Update the value of the `settings.watchedUsers.hint` key in BOTH locale files to clarify that the currently signed-in Jira user is automatically included in "All watched users" results.

    English (`src/i18n/locales/en.json` line 22) — replace:
      "settings.watchedUsers.hint": "Tickets assigned to these users appear in \"All watched users\" results."

    With:
      "settings.watchedUsers.hint": "Tickets assigned to these users appear in \"All watched users\" results. You are automatically included — there's no need to add yourself."

    Slovak (`src/i18n/locales/sk.json` line 22) — replace:
      "settings.watchedUsers.hint": "Tikety priradené týmto používateľom sa objavia vo výsledkoch \"Všetci sledovaní používatelia\"."

    With:
      "settings.watchedUsers.hint": "Tikety priradené týmto používateľom sa objavia vo výsledkoch \"Všetci sledovaní používatelia\". Vy ste automaticky zahrnutí — nemusíte sa pridávať."

    Notes:
    - Preserve full Slovak diacritics (project decision from quick task 260324-p39).
    - Do NOT touch SettingsPage.tsx — the existing `<p className="text-[12px] text-brand-muted mb-6">` already wraps `t('settings.watchedUsers.hint')` and the longer string fits the same style. This satisfies the "Reuse existing styling patterns" constraint.
    - Do NOT add a new key. Updating the existing key keeps the single source of truth and avoids JSX churn.
    - Keep keys in their original alphabetical/insertion order; do not reorder surrounding keys.
  </action>
  <verify>
    <automated>node -e "const en=require('./src/i18n/locales/en.json'); const sk=require('./src/i18n/locales/sk.json'); const enHint=en['settings.watchedUsers.hint']; const skHint=sk['settings.watchedUsers.hint']; if(!/automatically included/i.test(enHint)) throw new Error('EN hint missing auto-inclusion clarification: '+enHint); if(!/automaticky/i.test(skHint)) throw new Error('SK hint missing auto-inclusion clarification: '+skHint); if(!enHint.includes('All watched users')) throw new Error('EN hint dropped \"All watched users\" reference'); if(!skHint.includes('Všetci sledovaní používatelia')) throw new Error('SK hint dropped Slovak phrase reference'); console.log('OK',{enHint,skHint});"</automated>
  </verify>
  <done>
    - `settings.watchedUsers.hint` in en.json contains the phrase "automatically included" and still mentions "All watched users".
    - `settings.watchedUsers.hint` in sk.json contains "automaticky" and still mentions "Všetci sledovaní používatelia".
    - Both JSON files are valid JSON (no trailing commas, correct escaping of `\"`).
    - No other keys modified, no JSX/component files changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Updated the Watched Users settings hint in English and Slovak so it now states that the currently signed-in Jira user is automatically included when the "All watched users" filter runs.
  </what-built>
  <how-to-verify>
    1. Run the app: `npm run tauri dev`
    2. Open Settings → Watched users tab.
    3. Confirm the hint paragraph above the search input now reads (EN): "Tickets assigned to these users appear in \"All watched users\" results. You are automatically included — there's no need to add yourself."
    4. Switch language to Slovak (if applicable) and confirm the SK hint reads: "Tikety priradené týmto používateľom sa objavia vo výsledkoch \"Všetci sledovaní používatelia\". Vy ste automaticky zahrnutí — nemusíte sa pridávať."
    5. Confirm the styling matches the rest of the section — small muted gray text (`text-[12px] text-brand-muted`), with `mb-6` spacing before the search input. No layout regression.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any styling/wording issues.</resume-signal>
</task>

</tasks>

<verification>
- Both locale files are valid JSON and load successfully at app startup (i18next init does not throw).
- Hint paragraph renders without overflow or wrapping artifacts in the Watched Users settings panel.
- No regressions in other settings sections.
</verification>

<success_criteria>
- A user reading the Watched Users settings panel can determine, without consulting code, that they themselves are automatically included in "All watched users" results.
- Both English and Slovak locales communicate this fact.
- No code logic changed, no new i18n keys added, no JSX modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260430-0l2-add-info-to-the-settings-watched-users-t/260430-0l2-SUMMARY.md`
</output>
