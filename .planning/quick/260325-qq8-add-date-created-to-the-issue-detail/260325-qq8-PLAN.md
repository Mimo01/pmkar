---
phase: quick-260325-qq8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/tickets/TicketDetailPage.tsx
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: true
requirements: [quick-qq8]
must_haves:
  truths:
    - "Issue detail page shows the created date alongside the updated date"
  artifacts:
    - path: "src/features/tickets/TicketDetailPage.tsx"
      provides: "Created date display in issue header"
      contains: "detail.fields.created"
  key_links:
    - from: "src/features/tickets/TicketDetailPage.tsx"
      to: "detail.fields.created"
      via: "formatDate utility"
      pattern: "formatDate.*created"
---

<objective>
Add the "date created" to the issue detail page header, next to the existing "Updated X ago" line.

Purpose: Users need to see when an issue was originally created at a glance.
Output: Created date displayed in the issue detail header area.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/tickets/TicketDetailPage.tsx
@src/features/tickets/types.ts (fields.created: string exists)
@src/lib/format.ts (formatDate, formatRelativeTime available)
@src/i18n/locales/en.json
@src/i18n/locales/sk.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add created date to issue detail header and i18n keys</name>
  <files>src/features/tickets/TicketDetailPage.tsx, src/i18n/locales/en.json, src/i18n/locales/sk.json</files>
  <action>
1. In `en.json`, add key: `"tickets.card.created": "Created {{date}}"` near the existing `tickets.card.updated` key.
2. In `sk.json`, add key: `"tickets.card.created": "Vytvorene {{date}}"` (with proper diacritics: "Vytvorene" -> "Vytvorené") near the same location.
3. In `TicketDetailPage.tsx`, import `formatDate` from `../../lib/format` (already imports `formatRelativeTime`).
4. In the ticket header section (around line 199), modify the `<p>` tag that shows "Updated X ago" to also show the created date. Display both on the same line separated by a centered dot:
   ```
   Created {formatted date} · Updated {relative time}
   ```
   Use `formatDate(detail.fields.created)` for the created date (shows "25 March 2026" style) and the existing `formatRelativeTime(detail.fields.updated)` for updated.
   The line should read like: `t('tickets.card.created', { date: formatDate(detail.fields.created) })` + separator + existing updated text.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Issue detail header shows "Created {date} · Updated {relative time}" with both English and Slovak translations.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- Issue detail page renders created date alongside updated time
</verification>

<success_criteria>
- Created date visible in issue detail header
- Both en.json and sk.json have the new translation key
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260325-qq8-add-date-created-to-the-issue-detail/260325-qq8-SUMMARY.md`
</output>
