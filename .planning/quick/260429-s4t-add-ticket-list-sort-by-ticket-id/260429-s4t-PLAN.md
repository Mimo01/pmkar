---
quick_id: 260429-s4t
slug: add-ticket-list-sort-by-ticket-id
description: Add ticket list sort by ticket ID
date: 2026-04-29
must_haves:
  truths:
    - TicketFilterBar has sortField selector (Updated / Ticket ID) and direction toggle
    - TicketListPage sorts by numeric key suffix for 'key' field
    - Both locale files have the new translation keys
    - All existing TicketFilterBar tests pass
---

# Quick Task 260429-s4t: Add ticket list sort by ticket ID

## Task 1: Update TicketFilterBar component

**Files:** `src/features/tickets/TicketFilterBar.tsx`
**Action:** Add `sortField` + `onSortFieldChange` props; replace single sort toggle with `<select>` (Updated / Ticket ID) + direction arrow button
**Verify:** Component renders both controls; onChange fires for field change; direction button toggles
**Done:** sortField select and direction button render correctly

## Task 2: Update TicketListPage sort logic

**Files:** `src/features/tickets/TicketListPage.tsx`
**Action:** Add `sortField` state; update `sortedCandidates` to sort by numeric key suffix when `sortField === 'key'`; pass new props to TicketFilterBar
**Verify:** Tickets sort by `PROJ-123` numeric part correctly
**Done:** Sort logic handles both 'updated' and 'key' fields

## Task 3: Add translations and update tests

**Files:** `src/i18n/locales/en.json`, `src/i18n/locales/sk.json`, `src/features/tickets/__tests__/TicketFilterBar.test.tsx`
**Action:** Add sortKey, sortBy, sortAsc, sortDesc keys to both locales; update tests for new props and sort controls
**Verify:** All 25 TicketFilterBar tests pass
**Done:** Tests green
