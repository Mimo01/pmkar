---
phase: quick
plan: 260505-o6z
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [release-0.4.4]
must_haves:
  truths:
    - "Working tree is clean (no uncommitted tracked changes)"
    - "Version 0.4.4 is tagged and released with all artifacts"
    - "Tauri updater manifest (latest.json) points to v0.4.4"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Updated changelog with v0.4.4 entries"
      contains: "0.4.4"
  key_links:
    - from: "scripts/release.sh"
      to: "Mimo01/pmkar-releases"
      via: "GitHub API"
      pattern: "releases/tag/v0.4.4"
---

<objective>
Release pmkar v0.4.4 with all fixes and features since v0.4.3.

Purpose: Ship Phase 24 (copy-time audit loop + 'copied' outcome badge) and Phase 25 (preview-time wiki_to_adf and user field resolution) to users via a new release.
Output: GitHub release on Mimo01/pmkar-releases with macOS artifacts, updater manifest, and updated README.
</objective>

<context>
Current version in all config files: 0.4.3
Target version: 0.4.4

Working tree status: CLEAN — no uncommitted tracked changes (one untracked .planning/ file, which is fine).

Commits since v0.4.3 (the changes being released):
- feat: preview-time resolution of wiki_to_adf and user fields (Phase 25)
- feat: copy-time audit loop + 'copied' outcome badge (Phase 24)
- fix: gate Copy button on missing issue type; fix mock server assignee
- fix(25): harden user resolution in CopyPreviewPage (WR-01/02/03)
- fix(25): use stored_base_url from keychain in resolve_users_preview (CR-01)
- fix(24): thread previewCopyId through confirmCopy + guard targetProjectKey
- fix(24): 'copied' outcome i18n keys + catch blocks in AuditLogPage
- fix(23): WR-04 skip worklogs with invalid timeSpentSeconds
- fix(22/23): various modal, dep array, and copy gate fixes

Release script: scripts/release.sh <version>
- Requires clean working tree
- Bumps version across package.json, tauri.conf.json, Cargo.toml
- Regenerates CHANGELOG.md via git-cliff
- Builds macOS universal binary (+ Linux if Docker available)
- Creates GitHub release on Mimo01/pmkar-releases
- Uploads artifacts + latest.json updater manifest
- Pushes tag + commits to origin
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pre-flight state</name>
  <files>package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml</files>
  <action>
    Verify the working tree is clean and ready for release:

    1. Run `git diff-index --quiet HEAD -- && echo "CLEAN" || echo "HAS_CHANGES"` — must output CLEAN.
    2. Run `git status --short` to confirm only untracked .planning/ files exist.
    3. Confirm current version is 0.4.3 in package.json, src-tauri/tauri.conf.json, and src-tauri/Cargo.toml.
    4. Confirm v0.4.4 tag does not already exist: `git tag --list 'v0.4.4'` must return empty.

    Report findings. If any check fails, surface it clearly before proceeding.
  </action>
  <verify>
    <automated>git diff-index --quiet HEAD -- && echo "Working tree clean" && git tag --list 'v0.4.4' | grep -q . && echo "ERROR: v0.4.4 tag already exists" || echo "Tag v0.4.4 not yet created — ready to release"</automated>
  </verify>
  <done>Working tree clean, current version confirmed as 0.4.3, tag v0.4.4 does not exist</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run release script</name>
  <what-built>Pre-flight checks pass. Working tree is clean and ready for v0.4.4 release.</what-built>
  <how-to-verify>
    Run the release script from the project root:

    ```bash
    ./scripts/release.sh 0.4.4
    ```

    The script will:
    1. Pre-flight check (token, signing key, clean tree)
    2. Bump version to 0.4.4 across package.json, tauri.conf.json, Cargo.toml
    3. Regenerate CHANGELOG.md via git-cliff
    4. Build macOS universal binary (+ Linux if Docker available)
    5. Create GitHub release on Mimo01/pmkar-releases
    6. Upload all artifacts + latest.json
    7. Push tag v0.4.4 and commits to origin

    Verify the script completes successfully with the summary showing:
    - Release URL: https://github.com/Mimo01/pmkar-releases/releases/tag/v0.4.4
    - macOS artifacts uploaded (DMG, app.tar.gz, sig)
    - latest.json uploaded

    NOTE: This is a checkpoint:human-action because the release script requires the Tauri signing key and GitHub token from your keychain, which Claude cannot access. The script auto-detects these from macOS Keychain and ~/.tauri/pmkar.key.
  </how-to-verify>
  <resume-signal>Confirm "released" when the script completes successfully, or describe any errors</resume-signal>
</task>

</tasks>

<verification>
- `git tag --list 'v0.4.4'` shows the tag exists
- Release visible at https://github.com/Mimo01/pmkar-releases/releases/tag/v0.4.4
- latest.json in the release points to v0.4.4 artifacts
- package.json, src-tauri/tauri.conf.json, and src-tauri/Cargo.toml all show version 0.4.4
</verification>

<success_criteria>
- v0.4.4 release published on Mimo01/pmkar-releases with macOS artifacts
- Tauri updater manifest (latest.json) serves v0.4.4 to existing users
- All Phase 24 + Phase 25 commits included in the release
</success_criteria>

<output>
After completion, create `.planning/quick/260505-o6z-release-next-version-v0-4-4/260505-o6z-SUMMARY.md`
</output>
