---
phase: quick
plan: 260325-s97
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/tickets/tabs/OverviewTab.tsx
autonomous: true
requirements: [QUICK-s97]
must_haves:
  truths:
    - "Linked issues are grouped by relationship type (blocks, is blocked by, relates to, etc.)"
    - "Each group has a visible direction header label"
    - "Each linked issue row shows a monospace key badge, summary text, and status badge"
    - "Direction icons visually indicate relationship direction"
  artifacts:
    - path: "src/features/tickets/tabs/OverviewTab.tsx"
      provides: "Redesigned Linked Issues section with grouped layout"
  key_links:
    - from: "OverviewTab.tsx"
      to: "StatusBadge"
      via: "import and render per linked issue row"
      pattern: "<StatusBadge"
---

<objective>
Redesign the Linked Issues section in the ticket detail OverviewTab to group links by relationship type with card-like rows, monospace key badges, direction icons, and group headers.

Purpose: Replace the flat ungrouped list with a visually structured, scannable layout that makes relationship types immediately clear.
Output: Updated OverviewTab.tsx with redesigned Linked Issues section.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/tickets/tabs/OverviewTab.tsx
@src/features/tickets/StatusBadge.tsx
@src/features/tickets/types.ts

<interfaces>
From src/features/tickets/types.ts:
```typescript
export interface JiraIssueLink {
  id: string;
  type: { name: string; inward: string; outward: string };
  outwardIssue?: { key: string; fields: { summary: string; status: JiraStatus } };
  inwardIssue?: { key: string; fields: { summary: string; status: JiraStatus } };
}
```

From src/features/tickets/StatusBadge.tsx:
```typescript
export function StatusBadge({ status }: { status: string }): JSX.Element;
```

Existing imports in OverviewTab.tsx:
```typescript
import { StatusBadge } from '../StatusBadge';
```

Lucide React icons are available (used throughout the app per Phase 08 decisions).
Icon sizing convention: w-3.5 h-3.5 for small UI icons.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Linked Issues section with grouped layout and direction icons</name>
  <files>src/features/tickets/tabs/OverviewTab.tsx</files>
  <action>
Replace the Linked Issues section (lines 94-119) in OverviewTab.tsx with a redesigned grouped layout:

1. Add Lucide icon import at top of file: `import { ArrowUpRight, ArrowDownLeft, Link2 } from 'lucide-react';`

2. Inside the Linked Issues conditional block, group the issuelinks array by direction label:
   - Build a `Map<string, Array<{ key, summary, statusName, linkId }>>` by iterating `fields.issuelinks`
   - For each link, determine direction string (`link.type.outward` or `link.type.inward`) and the linked issue
   - Group entries under the same direction string

3. Render the grouped layout:
   - Keep the outer container: `<div className="px-5 py-4 border-t border-brand-border-subtle">`
   - Section heading: `<div className="text-xs font-semibold text-brand-muted mb-3">Linked Issues</div>`
   - For each group, render:
     - Group header row: `<div className="flex items-center gap-1.5 mb-1.5 mt-3 first:mt-0">` containing:
       - Direction icon: Use `ArrowUpRight` (w-3.5 h-3.5 text-brand-muted) for outward relationships ("blocks", "duplicates", "relates to"), `ArrowDownLeft` for inward relationships ("is blocked by", "is duplicated by"). If direction detection is ambiguous, use `Link2` as fallback.
       - Direction label: `<span className="text-xs font-medium text-brand-muted capitalize">{direction}</span>`
     - For each linked issue in the group, render a card-like row:
       ```
       <div className="flex items-center gap-2.5 py-1.5 px-3 rounded-md hover:bg-brand-surface-hover transition-colors">
         <span className="text-xs font-semibold font-mono text-brand-accent bg-brand-accent/10 px-1.5 py-0.5 rounded">
           {linkedIssue.key}
         </span>
         <span className="text-sm text-brand-text-secondary truncate flex-1">
           {linkedIssue.summary}
         </span>
         <StatusBadge status={linkedIssue.statusName} />
       </div>
       ```

4. To determine outward vs inward for icon selection: track whether each group came from `link.outwardIssue` being truthy (outward) or `link.inwardIssue` (inward). Store this as a boolean alongside the group key in the Map, e.g., `Map<string, { isOutward: boolean; items: Array<...> }>`.

5. Keep using the existing `StatusBadge` import -- no changes to StatusBadge component needed.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Linked Issues section groups links by relationship type with direction headers, Lucide arrow icons, monospace key badges, card-like hover rows, and status badges. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Visual: On a ticket detail page with linked issues, links are grouped under direction labels with arrow icons, each row has a monospace key badge and status pill
</verification>

<success_criteria>
- Linked issues grouped by relationship type (not flat list)
- Direction headers with appropriate arrow icons
- Issue keys shown in monospace badge style
- Status badges preserved on each row
- Hover state on rows for interactivity feel
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260325-s97-redesign-jira-links-section-on-ticket-de/260325-s97-SUMMARY.md`
</output>
