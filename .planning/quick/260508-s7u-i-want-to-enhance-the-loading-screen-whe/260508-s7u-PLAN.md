---
phase: quick-260508-s7u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/tickets/TicketListPage.tsx
autonomous: true
requirements:
  - loading-screen-fix
must_haves:
  truths:
    - "On re-fetch, only skeleton cards are visible — no stale ticket list underneath"
    - "After fetch completes, the ticket list scrolls back to the top"
    - "First-load behaviour is unchanged (skeleton → ticket list)"
    - "Empty state and error state still render correctly"
  artifacts:
    - path: "src/features/tickets/TicketListPage.tsx"
      provides: "Fixed mutual-exclusion guard and scroll reset"
      contains: "hasTickets && !isLoading"
  key_links:
    - from: "isLoading (fetchStatus === 'loading')"
      to: "card list conditional"
      via: "!isLoading guard added to hasTickets branch"
      pattern: "hasTickets && !isLoading"
    - from: "fetchStatus"
      to: "scrollRef.current.scrollTop"
      via: "useEffect watching fetchStatus"
      pattern: "scrollRef\\.current\\.scrollTop = 0"
---

<objective>
Fix the broken loading screen in TicketListPage: on re-fetch, the skeleton and the stale ticket list render simultaneously. Two isolated edits to a single file fix this and add a scroll-to-top reset when new data arrives.

Purpose: Loading state is visually broken — skeleton cards stack above old tickets on every manual re-fetch or background poll re-fetch.
Output: TicketListPage.tsx with mutual exclusion between skeleton and card list, plus scroll reset.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mimo/Documents/Projects/pmkar/.planning/STATE.md
@/Users/mimo/Documents/Projects/pmkar/.planning/quick/260508-s7u-i-want-to-enhance-the-loading-screen-whe/260508-s7u-CONTEXT.md
@/Users/mimo/Documents/Projects/pmkar/.planning/quick/260508-s7u-i-want-to-enhance-the-loading-screen-whe/260508-s7u-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix mutual exclusion and add scroll reset</name>
  <files>src/features/tickets/TicketListPage.tsx</files>
  <action>
Make four targeted edits to `src/features/tickets/TicketListPage.tsx`:

**Edit 0 — Add `useRef` to the React import** (line 4):

Before:
```tsx
import { useCallback, useEffect, useMemo, useState } from 'react';
```

After:
```tsx
import { useCallback, useEffect, useMemo, useRef, useState } from 'react';
```

**Edit 1 — Add scrollRef declaration** (insert after the `isLoading` derivation on line 87):

```tsx
const scrollRef = useRef<HTMLDivElement>(null);
```

**Edit 2 — Add scroll-reset effect** (insert after the 30s re-render interval `useEffect` that ends around line 98, before `handleFetch`):

```tsx
useEffect(() => {
  if (fetchStatus !== 'loading' && scrollRef.current) {
    scrollRef.current.scrollTop = 0;
  }
}, [fetchStatus]);
```

This fires every time `fetchStatus` changes. When loading ends (`'idle'` or `'error'`), it resets scroll. When loading starts it is a no-op (`fetchStatus !== 'loading'` is false). The `scrollRef.current` null guard is required because the div is conditionally rendered and will be null while `isLoading` is true.

**Edit 3 — Guard card list with `!isLoading`** (line 515, change condition):

Before:
```tsx
{hasTickets && (
  <div className="flex-1 overflow-y-auto">
```

After:
```tsx
{hasTickets && !isLoading && (
  <div className="flex-1 overflow-y-auto" ref={scrollRef}>
```

**Edit 4 — Attach ref to the scroll container div** (same line as Edit 3, add `ref={scrollRef}` to the `overflow-y-auto` div as shown above).

Do NOT change any other lines. Do NOT alter the `SkeletonCards` rendering, the `showEmptyState` condition, the error/truncation banners, or any other logic. The `aria-busy` and `role="status"` on `SkeletonCards` are already correct — leave them untouched (per D-04 in CONTEXT.md).
  </action>
  <verify>
    <automated>cd /Users/mimo/Documents/Projects/pmkar && grep -n "hasTickets && !isLoading" src/features/tickets/TicketListPage.tsx && grep -n "scrollRef" src/features/tickets/TicketListPage.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `useRef` is present in the React import on line 4
    - `hasTickets && !isLoading` appears in the card list condition
    - `scrollRef` appears at least 3 times (declaration, ref attachment, useEffect body)
    - `npx tsc --noEmit` exits without errors on the modified file
  </done>
</task>

</tasks>

<verification>
After the task completes:

1. TypeScript check passes: `npx tsc --noEmit` — no new errors.
2. Grep confirms `useRef` import: `grep "useRef" src/features/tickets/TicketListPage.tsx` returns a match on the import line.
3. Grep confirms mutual-exclusion guard: `grep "hasTickets && !isLoading" src/features/tickets/TicketListPage.tsx` returns one match at the card list conditional.
4. Grep confirms scroll reset: `grep -c "scrollRef" src/features/tickets/TicketListPage.tsx` returns 3 or more (declaration + ref prop + useEffect usage).
5. Manual smoke test: trigger a fetch while tickets are visible — only skeleton cards appear during loading, card list re-appears scrolled to top when done.
</verification>

<success_criteria>
- Re-fetch shows only skeleton (no stale list visible simultaneously)
- Post-fetch list starts at scroll position 0
- First-load, empty state, error state, and partial-failure warning remain unaffected
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/quick/260508-s7u-i-want-to-enhance-the-loading-screen-whe/260508-s7u-01-SUMMARY.md`
</output>
