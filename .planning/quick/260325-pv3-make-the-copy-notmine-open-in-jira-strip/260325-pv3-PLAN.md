---
phase: quick-260325-pv3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/tickets/TicketDetailPage.tsx
autonomous: true
requirements: [pv3-nicer-action-strip]
must_haves:
  truths:
    - "Action strip uses consistent Button component variants instead of raw styled buttons"
    - "Copy/Ignore and Open in Jira buttons are visually grouped with clear separation"
    - "The strip looks polished with proper spacing, visual hierarchy, and subtle divider between action groups"
  artifacts:
    - path: "src/features/tickets/TicketDetailPage.tsx"
      provides: "Redesigned action button strip"
  key_links: []
---

<objective>
Redesign the action button strip (Copy / Not Mine / Open in Jira) on TicketDetailPage to look cleaner and more polished.

Purpose: The current strip uses raw `<button>` elements with inconsistent inline styles. It should use the project's shadcn Button component with proper variants and visual grouping.
Output: A cleaner, more polished action strip in TicketDetailPage.tsx.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/tickets/TicketDetailPage.tsx
@src/components/ui/button.tsx
@src/components/ui/separator.tsx
@src/components/ui/badge.tsx
@src/components/ui/tooltip.tsx

<interfaces>
From src/components/ui/button.tsx:
```typescript
// Variants: default, destructive, outline, secondary, ghost, link
// Sizes: default (h-10 px-4), sm (h-9 px-3), lg (h-11 px-8), icon (h-10 w-10)
export const Button: React.ForwardRefExoticComponent<ButtonProps>;
```

From src/components/ui/separator.tsx:
```typescript
// Radix Separator, supports orientation="horizontal" | "vertical"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign the action button strip with proper Button variants and visual grouping</name>
  <files>src/features/tickets/TicketDetailPage.tsx</files>
  <action>
Refactor the action buttons section (lines ~219-286) in TicketDetailPage.tsx to look polished:

1. Replace all raw `<button>` elements with the shadcn `Button` component from `@/components/ui/button`:
   - "Copy to Company Jira" button: Use `variant="default"` `size="sm"` (primary action, brand-colored)
   - "Not for me" / "Ignored" button: Use `variant="ghost"` `size="sm"` (secondary, de-emphasized)
   - "Open in Jira" / "Open in Source Jira" / "Open in Company Jira" buttons: Use `variant="outline"` `size="sm"`
   - "Copied" badge stays as Badge component (already looks fine)

2. Add visual grouping with a vertical Separator between the main actions (Copy + Not Mine) and the Open in Jira actions:
   - Import `Separator` from `@/components/ui/separator`
   - Place `<Separator orientation="vertical" className="h-5" />` between the two groups
   - Only render the separator when there are buttons in both groups (i.e., not when only one side has content)

3. Wrap the entire strip in a styled container to give it a subtle card-like feel:
   - Use `rounded-lg border border-brand-border bg-brand-surface/50 px-3 py-2 inline-flex items-center gap-2 flex-wrap` on the container div
   - This gives it a contained, toolbar-like appearance rather than free-floating buttons

4. Keep all existing click handlers, disabled states, loading spinner, and conditional rendering logic exactly as-is. Only change the visual presentation.

5. Import Button at the top of the file alongside existing imports.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Action strip uses Button component with variant="default" for copy, variant="ghost" for ignore, variant="outline" for open-in-jira
    - Visual separator divides the action groups
    - Strip has a subtle contained toolbar appearance with rounded border
    - All functionality (copy, ignore, unignore, open in jira) preserved
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- Visual inspection: action strip on ticket detail page shows polished grouped buttons
</verification>

<success_criteria>
The Copy/NotMine/OpenInJira action strip on TicketDetailPage uses shadcn Button variants, has visual grouping with a separator, and looks like a polished toolbar rather than loose buttons.
</success_criteria>

<output>
After completion, create `.planning/quick/260325-pv3-make-the-copy-notmine-open-in-jira-strip/260325-pv3-SUMMARY.md`
</output>
