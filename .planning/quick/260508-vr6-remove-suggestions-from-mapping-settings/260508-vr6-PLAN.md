---
phase: quick-260508-vr6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/field-mapping/FieldMappingSection.tsx
  - src/features/field-mapping/SuggestionsPanel.tsx
  - src/features/field-mapping/heuristics.ts
  - src/features/field-mapping/__tests__/SuggestionsPanel.test.tsx
  - src/features/field-mapping/__tests__/heuristics.test.ts
  - src/features/field-mapping/__tests__/FieldMappingSection.test.tsx
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settings -> Field Mapping page no longer renders the Suggestions panel above the mapping table"
    - "Source files for the suggestions UI no longer exist (SuggestionsPanel.tsx, heuristics.ts) and their tests are removed"
    - "FieldMappingSection.tsx contains no references to Suggestion, SuggestionsPanel, findNameMatchSuggestion, suggestions useMemo, handleAcceptSuggestion, or handleDismissSuggestion"
    - "Translation keys for suggestions/accept/dismiss are removed from both en.json and sk.json"
    - "Existing dismissed-suggestion rows in the DB (targetFieldId='') still load without crashing — they simply appear as regular unmapped rows in the table"
    - "Frontend tests pass after removal (npm run test)"
    - "TypeScript compiles cleanly (no unused imports / dead refs)"
  artifacts:
    - path: "src/features/field-mapping/FieldMappingSection.tsx"
      provides: "Field mapping editor body with no suggestions block"
      contains_not: "SuggestionsPanel"
    - path: "src/i18n/locales/en.json"
      provides: "English translations without suggestions/accept/dismiss keys"
      contains_not: "settings.fieldMapping.suggestions"
    - path: "src/i18n/locales/sk.json"
      provides: "Slovak translations without suggestions/accept/dismiss keys"
      contains_not: "settings.fieldMapping.suggestions"
  key_links:
    - from: "src/features/field-mapping/FieldMappingSection.tsx"
      to: "(removed) SuggestionsPanel and heuristics"
      via: "no remaining import"
      pattern: "^(?!.*SuggestionsPanel)(?!.*heuristics).*$"
---

<objective>
Remove the heuristic Suggestions panel from the Field Mapping settings page.

Purpose: The user no longer wants the "Suggestions" block (auto-matched source→target pairs with Accept/Dismiss buttons) above the mapping table. The user manages mappings directly via the table + "Add field mapping" button.

Output:
- Deleted: SuggestionsPanel.tsx, heuristics.ts, and their two test files.
- Cleaned: FieldMappingSection.tsx no longer imports or renders the panel.
- Cleaned: 3 i18n keys removed from en.json and sk.json.
- Backend untouched: empty-string `target_field_id` rows remain valid in mapping.db (dismissed-suggestion sentinel persistence is unaffected; existing rows still load gracefully).
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@src/features/field-mapping/FieldMappingSection.tsx
@src/features/field-mapping/SuggestionsPanel.tsx
@src/features/field-mapping/heuristics.ts
@src/features/field-mapping/__tests__/FieldMappingSection.test.tsx
@src/i18n/locales/en.json
@src/i18n/locales/sk.json

<interfaces>
<!-- Key spots in FieldMappingSection.tsx that must change. -->

Imports to remove (lines 14, 16):
```typescript
import { findNameMatchSuggestion } from './heuristics';
import { type Suggestion, SuggestionsPanel } from './SuggestionsPanel';
```

Block to remove (lines ~233-252) — the `suggestions` useMemo:
```typescript
const suggestions = useMemo<Suggestion[]>(() => {
  const mappedSourceIds = new Set(mappingRows.map((r) => r.sourceFieldId));
  const out: Suggestion[] = [];
  for (const sf of sourceFields) {
    if (mappedSourceIds.has(sf.fieldId)) continue;
    const target = findNameMatchSuggestion(sf.fieldId, sf.name, targetFields);
    if (target) {
      out.push({ sourceFieldId: sf.fieldId, sourceName: sf.name, sourceSchema: sf.schema, target });
    }
  }
  return out;
}, [mappingRows, sourceFields, targetFields]);
```

Handlers to remove (lines ~266-301): `handleAcceptSuggestion` and `handleDismissSuggestion`.

JSX to remove (lines ~346-351):
```tsx
<SuggestionsPanel
  suggestions={suggestions}
  onAccept={handleAcceptSuggestion}
  onDismiss={handleDismissSuggestion}
/>
```

Translation keys to delete from both locales (en.json + sk.json lines 413-415):
- `settings.fieldMapping.suggestions`
- `settings.fieldMapping.accept`
- `settings.fieldMapping.dismiss`

Test file in __tests__/FieldMappingSection.test.tsx — the `[EDIT-02] SuggestionsPanel receives suggestions for unmapped source fields` test case (lines ~143-171) must be removed too because the panel will no longer exist.
</interfaces>

Backend note (do NOT modify): `field_mapping_db.rs` and `commands.rs` (lines 81, 1350, 1659, 2469) describe the empty-string `target_field_id` row as a "dismissed-suggestion sentinel". This is persistence behavior — rows with `target_field_id=''` continue to round-trip through the DB. Removing the UI does not break this; the DB layer remains agnostic about how dismissed rows got there. Existing dismissed rows in users' mapping.db simply render in the mapping table as ordinary unmapped rows (already-tested behavior in `[MAP-05] does NOT flag rows with empty-string targetFieldId... as drifted`).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete suggestion source + test files</name>
  <files>
    src/features/field-mapping/SuggestionsPanel.tsx (DELETE),
    src/features/field-mapping/heuristics.ts (DELETE),
    src/features/field-mapping/__tests__/SuggestionsPanel.test.tsx (DELETE),
    src/features/field-mapping/__tests__/heuristics.test.ts (DELETE)
  </files>
  <action>
    Delete the four files listed. Use `git rm` (or filesystem delete) for all four. After deletion, the `src/features/field-mapping/` directory should contain only: DriftWarning.tsx, FieldMappingSection.tsx, MappingRow.tsx, transformerOptions.ts, types.ts, and __tests__/ with FieldMappingSection.test.tsx + MappingRow.test.tsx (no Suggestion files).

    Do NOT touch types.ts — its comment "Empty string `""` is the dismissed-suggestion sentinel (CONTEXT.md D-07)" describes a backend-persistence invariant that remains true.
  </action>
  <verify>
    <automated>test ! -f src/features/field-mapping/SuggestionsPanel.tsx && test ! -f src/features/field-mapping/heuristics.ts && test ! -f src/features/field-mapping/__tests__/SuggestionsPanel.test.tsx && test ! -f src/features/field-mapping/__tests__/heuristics.test.ts && echo OK</automated>
  </verify>
  <done>All four files no longer exist on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Strip suggestions wiring from FieldMappingSection.tsx and update its test</name>
  <files>
    src/features/field-mapping/FieldMappingSection.tsx,
    src/features/field-mapping/__tests__/FieldMappingSection.test.tsx
  </files>
  <action>
    In `src/features/field-mapping/FieldMappingSection.tsx`:
    1. Delete the two imports:
       - `import { findNameMatchSuggestion } from './heuristics';`
       - `import { type Suggestion, SuggestionsPanel } from './SuggestionsPanel';`
    2. Delete the entire `// ── Heuristic suggestions (EDIT-02) ──...` `useMemo` block that produces the `suggestions` array (the `const suggestions = useMemo<Suggestion[]>(...)` plus its leading comment lines).
    3. Delete the two handler functions `handleAcceptSuggestion` and `handleDismissSuggestion` (and their leading blank lines).
    4. In the JSX `return (...)` block, delete the `<SuggestionsPanel ... />` element and the `{/* Suggestions panel (above table) */}` comment immediately preceding it. The first child of the outer `<div>` after the conditional guards will then be the column-headers grid (`<div className="grid grid-cols-[35fr_35fr_20fr_10fr] ...">`).
    5. Verify no orphan references remain: run `grep -n "Suggestion\|suggestion\|heuristic\|findNameMatch" src/features/field-mapping/FieldMappingSection.tsx` — expect zero matches.
    6. Verify the file still compiles (no unused imports, no undefined refs). The remaining imports `useMemo`, `useEffect`, `useState`, `useTranslation`, etc. are still all used (e.g. `useMemo` is still used by `driftedSourceFieldIds` and `usedTargetFieldIds`).

    In `src/features/field-mapping/__tests__/FieldMappingSection.test.tsx`:
    7. Delete the test case `it('[EDIT-02] SuggestionsPanel receives suggestions for unmapped source fields', ...)` (the entire `it(...)` block, roughly lines 143–171). All other test cases stay.

    Do NOT modify the backend (`src-tauri/`), MappingRow, DriftWarning, types.ts, transformerOptions.ts, or any other file.
  </action>
  <verify>
    <automated>grep -c -E "SuggestionsPanel|findNameMatchSuggestion|handleAcceptSuggestion|handleDismissSuggestion" src/features/field-mapping/FieldMappingSection.tsx | grep -q '^0$' && grep -c "SuggestionsPanel receives suggestions" src/features/field-mapping/__tests__/FieldMappingSection.test.tsx | grep -q '^0$' && npx tsc --noEmit -p tsconfig.json 2>&1 | tee /tmp/vr6-tsc.log | grep -q "error TS" && exit 1 || echo OK</automated>
  </verify>
  <done>FieldMappingSection.tsx contains zero suggestion-related references; the deleted test case is gone; `tsc --noEmit` reports no errors related to these changes.</done>
</task>

<task type="auto">
  <name>Task 3: Remove suggestion translation keys and run full test suite</name>
  <files>
    src/i18n/locales/en.json,
    src/i18n/locales/sk.json
  </files>
  <action>
    1. In `src/i18n/locales/en.json`, delete the three lines:
       - `"settings.fieldMapping.suggestions": "Suggestions ({{count}})",`
       - `"settings.fieldMapping.accept": "Accept",`
       - `"settings.fieldMapping.dismiss": "Dismiss",`
       (currently lines 413–415). Preserve JSON validity (commas + braces).
    2. In `src/i18n/locales/sk.json`, delete the matching three lines:
       - `"settings.fieldMapping.suggestions": "Návrhy ({{count}})",`
       - `"settings.fieldMapping.accept": "Prijať",`
       - `"settings.fieldMapping.dismiss": "Zamietnuť",`
       (also currently lines 413–415).
    3. Verify both JSON files parse: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8'));JSON.parse(require('fs').readFileSync('src/i18n/locales/sk.json','utf8'));console.log('OK')"`.
    4. Run the frontend test suite: `npm run test -- --run` (or project-equivalent vitest run command). All remaining tests must pass.
    5. Sanity-grep the whole frontend for orphan key usage:
       `grep -rn "fieldMapping.suggestions\|fieldMapping.accept\|fieldMapping.dismiss" src --include="*.ts" --include="*.tsx" --include="*.json"` — expect zero matches.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8'));JSON.parse(require('fs').readFileSync('src/i18n/locales/sk.json','utf8'));console.log('JSON OK')" && grep -rn "fieldMapping\.suggestions\|fieldMapping\.accept\|fieldMapping\.dismiss" src --include="*.ts" --include="*.tsx" --include="*.json" | grep -v "^$" && exit 1 || npm run test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>Both locale files parse as JSON; no source/test/json file references the three removed keys; the full vitest suite passes.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `npm run test -- --run` → all frontend tests pass.
2. `npx tsc --noEmit` → zero new TypeScript errors.
3. `cargo check --manifest-path src-tauri/Cargo.toml` → still compiles (backend untouched, but sanity check).
4. Manual sanity (optional, deferred): launch app, open Settings → Field Mapping, confirm:
   - No "Suggestions (N)" collapsible panel above the mapping table.
   - Mapping table, "Refresh schema" header button, and "Add field mapping" footer button still work.
   - If the user previously dismissed suggestions (rows with empty-string target in mapping.db), they appear in the table as ordinary rows with an empty target combobox — no crashes.
</verification>

<success_criteria>
- Suggestions UI is fully removed from the Field Mapping settings page.
- Four files deleted (SuggestionsPanel.tsx, heuristics.ts, and both their test files).
- FieldMappingSection.tsx and its test file have no remaining references to Suggestion, SuggestionsPanel, findNameMatchSuggestion, handleAcceptSuggestion, or handleDismissSuggestion.
- Three translation keys removed from both en.json and sk.json.
- Backend (src-tauri) is untouched; existing dismissed-suggestion rows in mapping.db continue to load.
- Frontend test suite passes; TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/quick/260508-vr6-remove-suggestions-from-mapping-settings/260508-vr6-SUMMARY.md` describing:
- Files deleted (4)
- Files modified (4: FieldMappingSection.tsx, FieldMappingSection.test.tsx, en.json, sk.json)
- Translation keys removed (3)
- Test results
- Confirmation that backend was untouched and dismissed-suggestion DB rows remain valid
</output>
