---
phase: quick-260325-t3p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/tauri.conf.json
  - src-tauri/Cargo.toml
  - CHANGELOG.md
autonomous: true
requirements: [DEPLOY-01]
must_haves:
  truths:
    - "Version 0.2.0 is set in package.json, tauri.conf.json, and Cargo.toml"
    - "v0.2.0 tag exists and is pushed to origin"
    - "GitHub release workflow is triggered by the tag push"
  artifacts:
    - path: "package.json"
      provides: "version field set to 0.2.0"
      contains: '"version": "0.2.0"'
    - path: "src-tauri/tauri.conf.json"
      provides: "version field set to 0.2.0"
      contains: '"version": "0.2.0"'
    - path: "src-tauri/Cargo.toml"
      provides: "version field set to 0.2.0"
      contains: 'version = "0.2.0"'
  key_links:
    - from: "v0.2.0 tag"
      to: ".github/workflows/release.yml"
      via: "tag push triggers on: push: tags: v*"
      pattern: "on.*push.*tags.*v\\*"
---

<objective>
Bump version from 0.1.0 to 0.2.0, commit, tag, and push to trigger the release workflow.

Purpose: Ship v0.2.0 release. The existing Tauri updater plugin and release workflow handle everything — existing users get auto-updates via the GitHub releases endpoint.
Output: v0.2.0 tag pushed, release workflow running.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@scripts/bump-version.mjs
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version to 0.2.0 and commit</name>
  <files>package.json, src-tauri/tauri.conf.json, src-tauri/Cargo.toml, CHANGELOG.md</files>
  <action>
    1. Run `node scripts/bump-version.mjs 0.2.0` — this updates version in package.json, tauri.conf.json, and Cargo.toml, and regenerates CHANGELOG.md
    2. Stage all changed files: `git add package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml CHANGELOG.md`
    3. Also stage Cargo.lock if it changed: `git add src-tauri/Cargo.lock 2>/dev/null || true`
    4. Commit: `git commit -m "chore: bump version to 0.2.0"`
    5. Verify the commit contains version 0.2.0 in all three files
  </action>
  <verify>
    <automated>grep '"version": "0.2.0"' package.json && grep '"version": "0.2.0"' src-tauri/tauri.conf.json && grep 'version = "0.2.0"' src-tauri/Cargo.toml</automated>
  </verify>
  <done>Version 0.2.0 set in all three files, changes committed</done>
</task>

<task type="auto">
  <name>Task 2: Tag and push to trigger release</name>
  <files></files>
  <action>
    1. Create annotated tag: `git tag v0.2.0 -m "Release v0.2.0"`
    2. Push the current branch to origin: `git push origin main`
    3. Push the tag: `git push origin v0.2.0`
    4. The tag push triggers .github/workflows/release.yml which builds macOS/Linux/Windows binaries and uploads to Mimo01/pmkar-releases
    5. Auto-updates for existing users are handled by the Tauri updater plugin pointing to the releases endpoint — no additional action needed
  </action>
  <verify>
    <automated>git tag -l v0.2.0 | grep v0.2.0</automated>
  </verify>
  <done>Tag v0.2.0 pushed to origin, release workflow triggered</done>
</task>

</tasks>

<verification>
- `git log --oneline -1` shows "chore: bump version to 0.2.0"
- `git tag -l v0.2.0` returns v0.2.0
- `gh run list --workflow=release.yml --limit=1` shows a running or completed workflow for v0.2.0
</verification>

<success_criteria>
- Version 0.2.0 in package.json, tauri.conf.json, Cargo.toml
- v0.2.0 tag exists locally and on remote
- GitHub Actions release workflow triggered by the tag push
</success_criteria>

<output>
After completion, create `.planning/quick/260325-t3p-deploy-the-app-bump-minor-version-and-re/260325-t3p-SUMMARY.md`
</output>
