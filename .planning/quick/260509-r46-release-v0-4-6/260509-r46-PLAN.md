---
quick_id: 260509-r46
slug: release-v0-4-6
description: Release version 0.4.6
date: 2026-05-09
status: planned
---

# Quick Task 260509-r46: Release version 0.4.6

## Objective

Bump version from 0.4.5 → 0.4.6 across all project files, commit the bump, create git tag v0.4.6, and publish a GitHub release with the changelog from commits since v0.4.5.

## Tasks

### Task 1: Bump version in all project files

**Files:**
- `package.json` — `"version": "0.4.5"` → `"0.4.6"`
- `src-tauri/Cargo.toml` — `version = "0.4.5"` → `"0.4.6"`
- `src-tauri/tauri.conf.json` — `"version": "0.4.5"` → `"0.4.6"`

**Action:** Edit all three files to replace 0.4.5 with 0.4.6.

**Verify:** All three files contain `0.4.6`.

### Task 2: Commit, tag, and release

**Action:**
1. Commit version bump: `chore(release): bump version to 0.4.6`
2. Create annotated tag: `git tag -a v0.4.6 -m "Release v0.4.6"`
3. Push tag: `git push origin v0.4.6`
4. Create GitHub release via `gh release create`

**Changelog (commits since v0.4.5):**
- feat: homescreen pre-fetch empty state with i18n strings
- feat: sort comments newest-first with toggle in ticket detail
- feat: expand mock fixtures to 40 issues with 2 new test users
- feat: improve ticket detail field display for unknown custom fields
- fix(tickets): persist session fetch state to survive navigation
- fix(tickets): exclude done tickets from preset JQL queries
- fix(tickets): use session-local flag for pre-fetch vs no-results empty state
- fix(copy-preview): resolve user_name from source displayName, skip Cloud lookup
- fix: improve field rendering — HTML, Java toString, progress, noise suppression
- fix: resolve custom field display names via expand=names fallback
- fix: strip HTML from custom field values and suppress LexoRank noise
- fix: prevent double-count in ticket loader progress
- fix: mutual exclusion between skeleton and stale ticket list on re-fetch
- fix: user domain search always queried Jira Cloud instead of Server
- fix: remove auto-fetch on homescreen open
- feat(mock): add duedate, environment, and standard fields to mock server
- chore: remove suggestions panel and heuristics from mapping settings
