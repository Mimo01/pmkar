---
phase: quick-260325-qw2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
  - src/features/tickets/IgnoredTicketsPage.tsx
  - src/components/ui/AppShell.tsx
  - src/features/connections/connectionStore.ts
  - src/features/tickets/TicketDetailPage.tsx
  - src/features/tickets/TicketDetailPanel.tsx
  - src/features/tickets/CopyPreviewPage.tsx
  - src/features/tickets/copyStore.ts
  - src-tauri/src/triage_db.rs
  - src-tauri/src/commands.rs
  - src/features/connections/SettingsPage.tsx
autonomous: true
requirements: [QW2-01, QW2-02]

must_haves:
  truths:
    - "Nav tab says 'Dismissed' instead of 'Not Mine'"
    - "Dismiss action button says 'Dismiss' instead of 'Not for me'"
    - "Dismissed tab shows an info card explaining the flow"
    - "Buttons and labels across the app show project names instead of generic 'Source Jira' / 'Company Jira'"
    - "Project names are persisted and available after app restart"
  artifacts:
    - path: "src/i18n/locales/en.json"
      provides: "Updated translation keys for Dismissed tab and project-name-aware labels"
    - path: "src/features/tickets/IgnoredTicketsPage.tsx"
      provides: "Dismissable info card at top of Dismissed tab"
    - path: "src/features/connections/connectionStore.ts"
      provides: "sourceProjectName and targetProjectName state"
    - path: "src-tauri/src/triage_db.rs"
      provides: "source_project_name and target_project_name DB columns"
    - path: "src-tauri/src/commands.rs"
      provides: "ProjectConfig with name fields"
  key_links:
    - from: "src/features/connections/connectionStore.ts"
      to: "src-tauri/src/commands.rs"
      via: "invoke get_project_config / set_project_config"
      pattern: "invoke.*project_config"
    - from: "src/features/tickets/TicketDetailPage.tsx"
      to: "src/features/connections/connectionStore.ts"
      via: "useConnectionStore sourceProjectName/targetProjectName"
      pattern: "useConnectionStore.*ProjectName"
    - from: "src/features/connections/SettingsPage.tsx"
      to: "src/features/connections/connectionStore.ts"
      via: "saveProjectConfig with name"
      pattern: "saveProjectConfig"
---

<objective>
Two improvements: (1) Rename "Not Mine" to "Dismissed" across the app with clearer UX copy and an info card, (2) Store and display Jira project names alongside keys so labels say "Open in PROJECTNAME" instead of generic "Source Jira" / "Company Jira".

Purpose: Make the dismiss flow self-explanatory on first use, and give users contextual project names instead of generic labels.
Output: Updated translations, info card component, project name storage and display.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/i18n/locales/en.json
@src/i18n/locales/sk.json
@src/features/tickets/IgnoredTicketsPage.tsx
@src/components/ui/AppShell.tsx
@src/features/connections/connectionStore.ts
@src/features/connections/SettingsPage.tsx
@src/features/tickets/TicketDetailPage.tsx
@src/features/tickets/TicketDetailPanel.tsx
@src/features/tickets/CopyPreviewPage.tsx
@src/features/tickets/copyStore.ts
@src-tauri/src/triage_db.rs
@src-tauri/src/commands.rs

<interfaces>
From src/features/connections/connectionStore.ts:
```typescript
interface ConnectionState {
  sourceProjectKey: string | null;
  targetProjectKey: string | null;
  loadProjectConfig: () => Promise<void>;
  saveProjectConfig: (source: string | null, target: string | null) => Promise<void>;
}
```

From src-tauri/src/commands.rs:
```rust
pub struct ProjectConfig {
    pub source_project_key: Option<String>,
    pub target_project_key: Option<String>,
}
pub fn get_project_config(...) -> Result<ProjectConfig, AppError>;
pub fn set_project_config(source_project_key: Option<String>, target_project_key: Option<String>, ...) -> Result<(), AppError>;
```

From src/features/connections/SettingsPage.tsx:
```typescript
interface JiraProject { key: string; name: string; }
// ProjectSelector fetches projects via invoke('fetch_server_projects'/'fetch_cloud_projects')
// and calls onSelect(key) — needs to also pass name
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename "Not Mine" to "Dismissed" and add info card</name>
  <files>
    src/i18n/locales/en.json,
    src/i18n/locales/sk.json,
    src/components/ui/AppShell.tsx,
    src/features/tickets/IgnoredTicketsPage.tsx
  </files>
  <action>
    1. In en.json, update these translation keys:
       - "nav.notMine": "Not Mine" -> "Dismissed"
       - "detail.ignore": "Not for me" -> "Dismiss"
       - "ignored.confirm.title": "Ignore ticket" -> "Dismiss ticket"
       - "ignored.confirm.body": update to "Dismiss this ticket? You can restore it from the Dismissed tab at any time."
       - "ignored.confirm.action": "Ignore Ticket" -> "Dismiss Ticket"
       - "ignored.heading": "Ignored Tickets" -> "Dismissed Tickets"
       - "ignored.empty.heading": "No ignored tickets" -> "No dismissed tickets"
       - "ignored.empty.body": update to "Tickets you dismiss will appear here. You can restore them at any time."
       - "ignored.restore": keep as "Restore"
       - Add new key "dismissed.infoCard": "Tickets you've dismissed appear here. They won't show up in your New tab. You can restore any ticket at any time."
       - Keep all "ignored.*" key names unchanged (they are code identifiers, not user-facing)

    2. In sk.json, update corresponding keys with Slovak translations:
       - "nav.notMine": "Zamietnuté"
       - "detail.ignore": "Zamietnuť"
       - "ignored.confirm.title": "Zamietnuť tiket"
       - "ignored.confirm.body": "Zamietnuť tento tiket? Môžete ho obnoviť z karty Zamietnuté kedykoľvek."
       - "ignored.confirm.action": "Zamietnuť tiket"
       - "ignored.heading": "Zamietnuté tikety"
       - "ignored.empty.heading": "Žiadne zamietnuté tikety"
       - "ignored.empty.body": "Tu sa zobrazia tikety, ktoré zamietnete. Kedykoľvek ich môžete obnoviť."
       - Add "dismissed.infoCard": "Tu sa zobrazujú zamietnuté tikety. Nezobrazia sa v karte Nové. Kedykoľvek môžete ktorýkoľvek tiket obnoviť."

    3. In IgnoredTicketsPage.tsx, add a dismissable info card between the filter bar and the card list:
       - Add a `useState<boolean>` for `showInfoCard` defaulting to `true`
       - Render a subtle card with: light brand-tinted bg (bg-brand/5 border border-brand/10 rounded-lg), the `dismissed.infoCard` text, and an X button to dismiss it
       - When X is clicked, set `showInfoCard` to false (session-only, no persistence needed)
       - Only show when `ignoredTickets.length > 0` (no point showing when empty state already explains)
       - Use Info icon from lucide-react for the card

    No changes needed to AppShell.tsx — it already uses `t('nav.notMine')` which will pick up the new translation.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Nav tab reads "Dismissed" in English, "Zamietnuté" in Slovak
    - Dismiss button reads "Dismiss" instead of "Not for me"
    - Confirmation dialog uses "Dismiss" language
    - Dismissed tab shows info card with explanation text and close button
    - Info card disappears when closed
  </done>
</task>

<task type="auto">
  <name>Task 2: Store and display project names across the app</name>
  <files>
    src-tauri/src/triage_db.rs,
    src-tauri/src/commands.rs,
    src/features/connections/connectionStore.ts,
    src/features/connections/SettingsPage.tsx,
    src/features/tickets/TicketDetailPage.tsx,
    src/features/tickets/TicketDetailPanel.tsx,
    src/features/tickets/CopyPreviewPage.tsx,
    src/features/tickets/copyStore.ts,
    src/i18n/locales/en.json,
    src/i18n/locales/sk.json
  </files>
  <action>
    **Backend (Rust):**
    1. In triage_db.rs:
       - Add two new ALTER TABLE migration constants for source_project_name and target_project_name columns on app_config
       - Run them in open() with `let _ = conn.execute_batch(...)` (same pattern as existing project key migrations)
       - Update `get_project_keys` to return a 4-tuple: `(Option<String>, Option<String>, Option<String>, Option<String>)` for (source_key, target_key, source_name, target_name)
       - Add `set_source_project_name(&self, name: Option<&str>)` and `set_target_project_name(&self, name: Option<&str>)` methods following the same pattern as set_source_project_key

    2. In commands.rs:
       - Add `source_project_name: Option<String>` and `target_project_name: Option<String>` to `ProjectConfig` struct
       - Update `get_project_config` to read all 4 values from the DB method
       - Update `set_project_config` to accept `source_project_name` and `target_project_name` parameters and persist them

    **Frontend (connectionStore):**
    3. In connectionStore.ts:
       - Add `sourceProjectName: string | null` and `targetProjectName: string | null` to ConnectionState
       - Add setters: `setSourceProjectName`, `setTargetProjectName`
       - Update `loadProjectConfig` to read and set names from the config response
       - Update `saveProjectConfig` signature to accept `(sourceKey, targetKey, sourceName, targetName)` and pass all four to the invoke

    **Frontend (SettingsPage ProjectSelector):**
    4. In SettingsPage.tsx:
       - Change `ProjectSelector`'s `onSelect` prop type from `(key: string) => void` to `(key: string, name: string) => void`
       - In onSelect callback for "All Projects" option, pass empty string for name
       - In onSelect callback for project items, pass `project.name`
       - Update the source ProjectSelector's onSelect to call `setSourceProjectName(name)` and pass name to `saveProjectConfig`
       - Update the target ProjectSelector's onSelect similarly

    **Frontend (display labels):**
    5. In en.json, update translation keys to use interpolation:
       - "detail.openInSourceJira": "Open in Source Jira" -> "Open in {{name}}"
       - "detail.openInCompanyJira": "Open in Company Jira" -> "Open in {{name}}"
       - "detail.copy": "Copy to Company Jira" -> "Copy to {{name}}"
       - "copy.preview.confirm": "Copy to Company Jira" -> "Copy to {{name}}"
       - "copy.result.openInJira": "Open in Company Jira" -> "Open in {{name}}"
       - "copy.progress.copyingFields": "Creating ticket in Company Jira" -> "Creating ticket in {{name}}"
       - "linked.empty.hint": update to use {{name}}
       - "settings.sourceLabel": "Source (Customer Jira)" -> "Source ({{name}})" with fallback
       - "settings.destLabel": "Destination (Company Jira)" -> "Destination ({{name}})" with fallback
       Do the same for sk.json equivalents.

    6. In TicketDetailPage.tsx:
       - Import useConnectionStore
       - Read sourceProjectName and targetProjectName (already imports useConnectionStore — just add the selectors)
       - Pass `{ name: sourceProjectName || t('wizard.source.subtitle') }` as interpolation to t('detail.openInSourceJira')
       - Pass `{ name: targetProjectName || t('wizard.destination.subtitle') }` to t('detail.openInCompanyJira') and t('detail.copy')

    7. In TicketDetailPanel.tsx: same pattern as TicketDetailPage — read project names from store and pass to translation interpolation for openInSourceJira / openInCompanyJira

    8. In CopyPreviewPage.tsx: read targetProjectName from connectionStore and pass to t('copy.preview.confirm') and t('copy.progress.copyingFields')

    9. In copyStore.ts: update the error message for target field loading failure to not hardcode "Company Jira" — use a generic message like "Could not load target fields. Check your destination connection in Settings."

    **Fallback behavior:** When project name is null (not yet selected), fall back to the generic labels ("Customer Jira Server" / "Company Jira Cloud" from wizard.source.subtitle / wizard.destination.subtitle).
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Backend stores and retrieves project names alongside keys
    - Connection store has sourceProjectName and targetProjectName
    - SettingsPage ProjectSelector passes project name on selection
    - "Open in" buttons show actual project name (e.g., "Open in MYPROJECT")
    - "Copy to" button shows actual project name
    - Falls back to generic labels when no project selected
    - Both en.json and sk.json updated with interpolation-based labels
  </done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `npx tsc --noEmit`
2. Rust compiles: `cargo check --manifest-path src-tauri/Cargo.toml`
3. Tests pass: `npx vitest run`
4. Visual: Nav tab says "Dismissed", dismiss flow uses "Dismiss" language, info card appears on Dismissed tab
5. Visual: Buttons show project names when projects are selected, fall back to generic labels when not
</verification>

<success_criteria>
- "Not Mine" renamed to "Dismissed" everywhere in UI
- Info card on Dismissed tab explains the flow and can be closed
- Project names stored in DB and surfaced in connectionStore
- All "Source Jira" / "Company Jira" labels replaced with actual project names via interpolation
- Graceful fallback when no project is selected
</success_criteria>

<output>
After completion, create `.planning/quick/260325-qw2-make-not-mine-ignored-clearer-to-users-a/260325-qw2-SUMMARY.md`
</output>
