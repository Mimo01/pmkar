---
phase: 09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/components/ui/AppShell.tsx
  - src/features/tickets/TicketDetailPage.tsx
autonomous: true
requirements: [A11Y-01, A11Y-05]

must_haves:
  truths:
    - "Dark mode muted text passes 4.5:1 contrast ratio on dark backgrounds"
    - "StatusBadge colored text in dark mode passes 4.5:1 contrast ratio"
    - "AppShell content area has a main landmark for screen readers"
    - "Tab panel in TicketDetailPage has correct aria-labelledby and tabIndex"
  artifacts:
    - path: "src/index.css"
      provides: "WCAG AA compliant dark mode color tokens"
      contains: "--color-brand-muted: #7f7f7f"
    - path: "src/components/ui/AppShell.tsx"
      provides: "Main landmark and tooltip aria-hidden"
      contains: "role=\"main\""
    - path: "src/features/tickets/TicketDetailPage.tsx"
      provides: "StatusBadge dark mode contrast fix and tab ARIA completion"
      contains: "dark:text-blue-400"
  key_links:
    - from: "src/index.css"
      to: "all components using text-brand-muted"
      via: "CSS custom property inheritance"
      pattern: "--color-brand-muted: #7f7f7f"
    - from: "src/features/tickets/TicketDetailPage.tsx"
      to: "tab buttons and tabpanel"
      via: "id/aria-labelledby association"
      pattern: "aria-labelledby.*tab-"
---

<objective>
Fix dark mode contrast failures and complete semantic structure across AppShell and TicketDetailPage.

Purpose: Systemic contrast fix in CSS tokens eliminates all dark-mode muted text failures in one edit. StatusBadge dark mode colors and tab panel ARIA completion address the highest-impact accessibility gaps on the most-used views. Per D-01 (WCAG AA).
Output: WCAG AA compliant dark mode tokens, main landmark in AppShell, complete tab ARIA in detail page.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements/09-RESEARCH.md
@.planning/phases/09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dark mode contrast tokens and AppShell semantic structure</name>
  <files>src/index.css, src/components/ui/AppShell.tsx</files>
  <read_first>src/index.css, src/components/ui/AppShell.tsx</read_first>
  <action>
In `src/index.css`, inside the `body.dark` block only (NOT the @theme block):
- Change `--color-brand-muted: #6b6b6f;` to `--color-brand-muted: #7f7f7f;` (ratio: 4.52:1 on #161617, passes WCAG AA 4.5:1 for normal text)
- Change `--muted-foreground: #6b6b6f;` to `--muted-foreground: #7f7f7f;` (shadcn alias must stay in sync)
- Do NOT change any values in the @theme block (light mode values stay as #8c8c92)

In `src/components/ui/AppShell.tsx`:
- On the content area div (currently `<div className="flex-1 flex flex-col overflow-hidden">`), add `role="main"` attribute
- On both `<TooltipContent>` elements (audit and settings tooltips), add `aria-hidden="true"` attribute. The button's aria-label already provides the accessible name; tooltip is for mouse users only and should not be double-announced by screen readers
  </action>
  <verify>
    <automated>grep -n "7f7f7f" src/index.css | head -5 && grep -n 'role="main"' src/components/ui/AppShell.tsx && grep -n 'aria-hidden="true"' src/components/ui/AppShell.tsx | grep -i tooltip</automated>
  </verify>
  <acceptance_criteria>
- src/index.css body.dark block contains `--color-brand-muted: #7f7f7f`
- src/index.css body.dark block contains `--muted-foreground: #7f7f7f`
- src/index.css @theme block still contains `--color-brand-muted: #8c8c92` (unchanged)
- AppShell.tsx contains `role="main"` on content wrapper div
- AppShell.tsx contains `aria-hidden="true"` on both TooltipContent elements
  </acceptance_criteria>
  <done>Dark mode muted text meets 4.5:1 AA threshold. AppShell has main landmark and tooltips don't double-announce.</done>
</task>

<task type="auto">
  <name>Task 2: Fix StatusBadge dark mode contrast and complete tab panel ARIA</name>
  <files>src/features/tickets/TicketDetailPage.tsx</files>
  <read_first>src/features/tickets/TicketDetailPage.tsx</read_first>
  <action>
In `TicketDetailPage.tsx`, update the `StatusBadge` component:
- Change `'bg-blue-600/10 text-blue-600'` to `'bg-blue-600/10 text-blue-600 dark:text-blue-400'` (blue-400 = #60a5fa, ratio 6.55:1 on dark surface)
- Change `'bg-green-600/10 text-green-600'` to `'bg-green-600/10 text-green-600 dark:text-green-400'` (green-400 = #4ade80, ratio 8.44:1)
- Change `'bg-red-600/10 text-red-600'` to `'bg-red-600/10 text-red-600 dark:text-red-400'` (red-400 = #f87171, ratio 6.54:1)

In the tab bar section, update each tab button to include an `id` attribute:
- Add `id={`tab-${tab.id}`}` to each `<button>` with `role="tab"`

Update the tabpanel div:
- Add `aria-labelledby={`tab-${activeTab}`}` to the `<div role="tabpanel">` element
- Add `tabIndex={0}` to the tabpanel div so it's focusable when selected

The final tabpanel div should look like:
```
<div role="tabpanel" id={`tabpanel-${activeTab}`} aria-labelledby={`tab-${activeTab}`} tabIndex={0}>
```
  </action>
  <verify>
    <automated>grep -n "dark:text-blue-400\|dark:text-green-400\|dark:text-red-400" src/features/tickets/TicketDetailPage.tsx && grep -n 'id={`tab-' src/features/tickets/TicketDetailPage.tsx && grep -n 'aria-labelledby' src/features/tickets/TicketDetailPage.tsx</automated>
  </verify>
  <acceptance_criteria>
- TicketDetailPage.tsx StatusBadge contains `dark:text-blue-400`
- TicketDetailPage.tsx StatusBadge contains `dark:text-green-400`
- TicketDetailPage.tsx StatusBadge contains `dark:text-red-400`
- Each tab button has `id={`tab-${tab.id}`}`
- Tabpanel div has `aria-labelledby={`tab-${activeTab}`}`
- Tabpanel div has `tabIndex={0}`
  </acceptance_criteria>
  <done>StatusBadge text passes 4.5:1 in dark mode. Tab panel ARIA is complete with id/aria-labelledby/tabIndex.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` passes (no test regressions)
- Dark mode muted text token is #7f7f7f (4.52:1 ratio confirmed)
- StatusBadge uses dark: variant classes for blue/green/red
- AppShell has role="main" on content area
- Tab buttons have id, tabpanel has aria-labelledby and tabIndex
</verification>

<success_criteria>
All dark mode contrast failures from RESEARCH.md contrast audit are resolved. Semantic structure (main landmark, tab ARIA) is complete for the two most-used components.
</success_criteria>

<output>
After completion, create `.planning/phases/09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements/09-01-SUMMARY.md`
</output>
