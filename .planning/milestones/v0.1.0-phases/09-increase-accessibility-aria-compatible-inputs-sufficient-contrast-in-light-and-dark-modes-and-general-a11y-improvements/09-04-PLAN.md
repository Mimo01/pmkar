---
phase: 09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/connections/SettingsPage.tsx
autonomous: true
requirements: [A11Y-05]

must_haves:
  truths:
    - "Settings sidebar nav has an accessible label"
    - "JQL preset buttons have radiogroup/radio ARIA semantics"
    - "Watched users search input has an accessible label and combobox hints"
    - "Suggestion dropdown items have listbox/option roles"
  artifacts:
    - path: "src/features/connections/SettingsPage.tsx"
      provides: "Accessible settings nav, JQL radiogroup, and combobox pattern"
      contains: "role=\"radiogroup\""
  key_links:
    - from: "SettingsPage.tsx radiogroup wrapper"
      to: "SettingsPage.tsx radio buttons"
      via: "role=radiogroup/radio + aria-checked"
      pattern: "role=\"radio\".*aria-checked"
---

<objective>
Add ARIA semantics to SettingsPage: sidebar nav label, JQL preset radiogroup pattern, and watched users combobox hints.

Purpose: SettingsPage sidebar nav lacks aria-label (screen readers announce it as a generic nav). JQL preset buttons act as a radio group but lack role semantics. Watched users search input relies on placeholder only — has no accessible label or combobox ARIA attributes.
Output: Fully accessible SettingsPage with proper landmark labels, radiogroup semantics, and combobox hints.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements/09-RESEARCH.md
@.planning/phases/09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsPage nav label and JQL preset radiogroup semantics</name>
  <files>src/features/connections/SettingsPage.tsx</files>
  <read_first>src/features/connections/SettingsPage.tsx</read_first>
  <action>
**Sidebar nav label:**

Add `aria-label` to the sidebar `<nav>` element (currently at line ~492):
```tsx
<nav className="w-40 flex-shrink-0 border-r border-brand-border bg-brand-surface py-4 px-2 overflow-y-auto" aria-label="Settings navigation">
```

**JQL preset buttons — radiogroup semantics:**

In the `jql-presets` case of `renderContent()`, wrap the preset buttons div with radiogroup role:
```tsx
<div className="space-y-1.5" role="radiogroup" aria-label="JQL presets">
```

On each preset `<button>`, add `role="radio"` and `aria-checked`:
```tsx
<button
  key={opt.value}
  type="button"
  role="radio"
  aria-checked={jqlPreset === opt.value}
  onClick={() => handlePresetChange(opt.value)}
  className={...}
>
```

The visual radio dot indicator span (the `w-[15px] h-[15px] rounded-full border-2` span) should get `aria-hidden="true"` since the `aria-checked` attribute conveys the state:
```tsx
<span className={`flex-shrink-0 w-[15px] h-[15px] rounded-full border-2 flex items-center justify-center transition-colors duration-150 ${
  jqlPreset === opt.value ? 'border-brand' : 'border-brand-border'
}`} aria-hidden="true">
```

**Watched users search — combobox ARIA:**

On the search `<input>` element (the one with `ref={inputRef}`), add:
- `aria-label="Search users to watch"` (since there's no visible label — only placeholder)
- `aria-expanded={showSuggestions && suggestions.length > 0}` — true when dropdown is visible
- `aria-haspopup="listbox"` — indicates dropdown type
- `aria-autocomplete="list"` — indicates suggestions behavior

```tsx
<input
  ref={inputRef}
  type="text"
  value={userQuery}
  onChange={(e) => setUserQuery(e.target.value)}
  onKeyDown={handleKeyDown}
  onFocus={() => { if (suggestions.length > 0) setShowSuggestions(true); }}
  onBlur={() => setTimeout(() => setShowSuggestions(false), 200)}
  className="flex-1 bg-transparent text-[13px] text-brand-text placeholder-brand-muted focus:outline-none"
  placeholder={t('settings.watchedUsers.searchPlaceholder')}
  aria-label="Search users to watch"
  aria-expanded={showSuggestions && suggestions.length > 0}
  aria-haspopup="listbox"
  aria-autocomplete="list"
/>
```

On the suggestions dropdown `<div>`:
```tsx
<div className="absolute left-0 right-0 top-full mt-1 z-10 border border-brand-border rounded-lg bg-brand-surface shadow-lg overflow-hidden" role="listbox">
```

On each suggestion `<button>`:
```tsx
<button
  key={user.name}
  type="button"
  role="option"
  aria-selected={i === selectedIdx}
  onMouseDown={(e) => { e.preventDefault(); handleAddUser(user.name); }}
  className={...}
>
```
  </action>
  <verify>
    <automated>grep -n 'aria-label="Settings navigation"' src/features/connections/SettingsPage.tsx && grep -n 'role="radiogroup"' src/features/connections/SettingsPage.tsx && grep -n 'role="radio"' src/features/connections/SettingsPage.tsx && grep -n 'aria-checked' src/features/connections/SettingsPage.tsx && grep -n 'aria-haspopup="listbox"' src/features/connections/SettingsPage.tsx && grep -n 'role="listbox"' src/features/connections/SettingsPage.tsx && grep -n 'role="option"' src/features/connections/SettingsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
- SettingsPage.tsx sidebar `<nav>` has `aria-label="Settings navigation"`
- SettingsPage.tsx JQL preset wrapper div has `role="radiogroup"` and `aria-label="JQL presets"`
- SettingsPage.tsx each JQL preset button has `role="radio"` and `aria-checked={jqlPreset === opt.value}`
- SettingsPage.tsx visual radio dot span has `aria-hidden="true"`
- SettingsPage.tsx search input has `aria-label="Search users to watch"`
- SettingsPage.tsx search input has `aria-expanded={showSuggestions && suggestions.length > 0}`
- SettingsPage.tsx search input has `aria-haspopup="listbox"` and `aria-autocomplete="list"`
- SettingsPage.tsx suggestion dropdown has `role="listbox"`
- SettingsPage.tsx each suggestion item has `role="option"` and `aria-selected={i === selectedIdx}`
  </acceptance_criteria>
  <done>SettingsPage has accessible nav label, proper radiogroup semantics for JQL presets, and combobox ARIA pattern for user search.</done>
</task>

</tasks>

<verification>
- `npm test -- --run` passes (no test regressions)
- SettingsPage sidebar nav has aria-label
- JQL preset buttons have role=radio and aria-checked
- Watched users search has combobox ARIA attributes
- Suggestions dropdown has role=listbox with role=option items
</verification>

<success_criteria>
SettingsPage ARIA is complete: nav labeled, radio group semantics correct, combobox pattern provides keyboard and screen reader discovery for user search.
</success_criteria>

<output>
After completion, create `.planning/phases/09-increase-accessibility-aria-compatible-inputs-sufficient-contrast-in-light-and-dark-modes-and-general-a11y-improvements/09-04-SUMMARY.md`
</output>
