---
phase: 11-add-deployment-auto-updates-and-release-management
plan: "01"
subsystem: infra
tags: [tauri, tauri-plugin-updater, tauri-plugin-process, auto-update, capabilities, acl]

# Dependency graph
requires: []
provides:
  - "Tauri updater plugin registered in Rust setup closure under #[cfg(desktop)] gate"
  - "tauri-plugin-process registered for app restart after update"
  - "src-tauri/capabilities/main.json with updater and process ACL permissions"
  - "tauri.conf.json createUpdaterArtifacts: true and plugins.updater with endpoint and placeholder pubkey"
  - "Frontend npm packages @tauri-apps/plugin-updater and @tauri-apps/plugin-process installed"
affects: [11-02, 11-03, 11-04]

# Tech tracking
tech-stack:
  added: [tauri-plugin-updater@2.10, tauri-plugin-process@2.3, "@tauri-apps/plugin-updater@2.10", "@tauri-apps/plugin-process@2.3"]
  patterns:
    - "Desktop-only plugin registration via #[cfg(desktop)] gate in Tauri setup closure"
    - "Tauri v2 ACL capabilities file pattern — all plugin commands require explicit grant in capabilities/main.json"

key-files:
  created:
    - "src-tauri/capabilities/main.json"
  modified:
    - "src-tauri/Cargo.toml"
    - "src-tauri/src/main.rs"
    - "src-tauri/tauri.conf.json"
    - "package.json"
    - "package-lock.json"
    - "src-tauri/gen/schemas/acl-manifests.json"
    - "src-tauri/gen/schemas/capabilities.json"
    - "src-tauri/gen/schemas/desktop-schema.json"
    - "src-tauri/gen/schemas/macOS-schema.json"

key-decisions:
  - "Used cfg(not(android, ios)) gate for Rust crates and #[cfg(desktop)] for runtime registration — app must support mobile compile target if ever added"
  - "Placeholder pubkey in tauri.conf.json — real keypair must be generated by user before first release (documented for Plan 02)"
  - "Updater endpoint points to pmkar-releases GitHub repo — matches D-01 release architecture using a dedicated public releases repo"
  - "Generated ACL schemas committed alongside plugin registration — schemas change when plugins are added and must stay in sync"

patterns-established:
  - "Tauri v2 ACL pattern: every plugin command requires explicit permission in capabilities/main.json; without it commands fail silently"
  - "Platform-gated plugins: desktop-only Rust crates under [target.'cfg(not(android, ios))'.dependencies] table"

requirements-completed: [D-03, D-09, D-10, D-11]

# Metrics
duration: 4min
completed: 2026-03-25
---

# Phase 11 Plan 01: Plugin Foundation Summary

**Tauri updater and process plugins wired: Rust crates, npm packages, capabilities ACL, and tauri.conf.json updater config — app compiles cleanly, ready for frontend update UI in Plan 02.**

## Performance

- **Duration:** ~4 min
- **Started:** 2026-03-25T07:03:18Z
- **Completed:** 2026-03-25T07:06:58Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments
- Both Tauri plugin crates added to Cargo.toml under desktop-only platform gate
- Frontend npm packages installed and package.json updated
- Plugin registration added to main.rs setup closure under `#[cfg(desktop)]`
- Capabilities file created granting all required updater and process permissions
- tauri.conf.json configured with `createUpdaterArtifacts: true` and updater endpoint
- Generated ACL manifests updated by cargo check to reflect new plugin capabilities
- `cargo check` exits 0 — app compiles with no errors

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Rust and npm plugin dependencies** - `c941bc2` (chore)
2. **Task 2: Register plugins, create capabilities, configure updater in tauri.conf.json** - `3f3b100` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified
- `src-tauri/capabilities/main.json` - Tauri v2 ACL capabilities granting updater and process permissions to main window
- `src-tauri/Cargo.toml` - Added tauri-plugin-updater and tauri-plugin-process under desktop cfg gate
- `src-tauri/src/main.rs` - Plugin registration in setup closure under #[cfg(desktop)]
- `src-tauri/tauri.conf.json` - createUpdaterArtifacts: true, plugins.updater section with endpoint and placeholder pubkey
- `package.json` - @tauri-apps/plugin-updater and @tauri-apps/plugin-process added to dependencies
- `package-lock.json` - Updated with new npm packages
- `src-tauri/gen/schemas/*.json` - ACL manifests regenerated by cargo check to include new plugin schemas

## Decisions Made
- Used `#[cfg(desktop)]` for plugin registration (not just `cfg(not(any(target_os = "android", target_os = "ios")))`) — cleaner and idiomatic for Tauri v2 desktop apps
- Committed updated gen/schemas since Tauri generates these deterministically from plugin registration; they need to stay in sync with Cargo.toml
- Placeholder pubkey in tauri.conf.json intentional — user must run `tauri signer generate` before first release; Plan 02 documents this step

## Deviations from Plan

None - plan executed exactly as written. The only additional action was committing updated `src-tauri/gen/schemas/` files that cargo check regenerated automatically when new plugins were registered — these are required for the app to build correctly.

## Issues Encountered
None — cargo check succeeded on first attempt. npm install added both packages without conflicts.

## User Setup Required
None at this stage. A real pubkey must be generated before first release; this is documented in Plan 02 as a user setup step.

## Next Phase Readiness
- Plugin registration complete — frontend can now import and call `@tauri-apps/plugin-updater` and `@tauri-apps/plugin-process`
- Plan 02 (GitHub Actions CI/CD) can proceed independently
- Plan 03 (Update UI component) can now import the updater plugin and invoke `check()`, `downloadAndInstall()` etc.
- User must generate signing keypair (`tauri signer generate`) and replace the placeholder pubkey before any release artifacts are signed

---
*Phase: 11-add-deployment-auto-updates-and-release-management*
*Completed: 2026-03-25*
