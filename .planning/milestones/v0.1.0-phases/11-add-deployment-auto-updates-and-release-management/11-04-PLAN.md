---
phase: 11-add-deployment-auto-updates-and-release-management
plan: 04
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified: []
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11]

must_haves:
  truths:
    - "All tests pass with no regressions"
    - "Settings page About section is visually correct in both themes"
    - "Update modal renders correctly in available/downloading states"
    - "Release workflow file exists with correct matrix configuration"
  artifacts: []
  key_links: []
---

<objective>
Final verification of all Phase 11 deliverables: run full test suite, lint checks, and visual verification of update UI.

Purpose: Ensure all three preceding plans integrate correctly and the UI matches the design contract.
Output: Verified phase completion.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-add-deployment-auto-updates-and-release-management/11-CONTEXT.md
@.planning/phases/11-add-deployment-auto-updates-and-release-management/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gate checks</name>
  <files></files>
  <read_first>
    - src-tauri/Cargo.toml (verify plugin deps)
    - src-tauri/capabilities/main.json (verify permissions)
    - .github/workflows/release.yml (verify workflow structure)
  </read_first>
  <action>
Run the complete quality gate suite to verify no regressions:

1. `npm run lint` — Biome check exits 0
2. `npx tsc --noEmit` — TypeScript type check exits 0
3. `npx vitest run --reporter=verbose` — All tests pass
4. `cd src-tauri && cargo clippy -- -D warnings` — No clippy warnings
5. `cd src-tauri && cargo fmt --check` — Format check passes

If any check fails, fix the issue before proceeding to human verification.

Also verify structural requirements:
- `grep -c 'about\.' src/i18n/locales/en.json` should be >= 8 (about.* keys)
- `grep -c 'update\.modal\.' src/i18n/locales/en.json` should be >= 9 (update.modal.* keys)
- Same counts in sk.json
- release.yml contains all 3 platform matrix entries
- capabilities/main.json contains updater permissions
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run lint && npx tsc --noEmit && npx vitest run && cd src-tauri && cargo clippy -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 with all tests passing
    - `cargo clippy -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
  </acceptance_criteria>
  <done>All automated quality gates pass with zero warnings or errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 11 deliverables</name>
  <files></files>
  <action>
Present the completed Phase 11 deliverables to the user for visual and functional verification. All automated work is complete — this checkpoint confirms the human-verifiable aspects: UI appearance, interaction behavior, and workflow configuration.
  </action>
  <what-built>
Complete Phase 11 deliverables:
1. Tauri updater + process plugins registered with capabilities
2. Release workflow (.github/workflows/release.yml) for multi-platform builds on tag push
3. Changelog config (cliff.toml) and version bump script (scripts/bump-version.mjs)
4. Update Zustand store with full state machine
5. About/Updates section in Settings sidebar
6. Blocking update modal with progress bar
7. English + Slovak translations for all update strings
  </what-built>
  <how-to-verify>
1. Run `npm run tauri dev` to launch the app
2. Navigate to Settings (gear icon)
3. Verify sidebar shows "About" section at the bottom after Appearance group
4. Click "About" in sidebar — verify it shows:
   - Current version (0.1.0)
   - "Last checked: Never" text
   - "Check for updates" button
5. Click "Check for updates" — should show brief loading then either error (expected in dev, no published releases) or "up to date"
6. Toggle dark mode in Theme section, then return to About — verify colors and contrast are correct
7. Switch to Slovak language — verify all update strings appear in Slovak
8. Review `.github/workflows/release.yml` — confirm it has macOS universal, Windows, and Linux matrix entries
9. Test version bump: `node scripts/bump-version.mjs 0.1.0` (should be no-op since already at 0.1.0)
  </how-to-verify>
  <verify>User approves visual and functional verification</verify>
  <done>User has approved the Phase 11 UI, workflow configuration, and overall integration.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All automated checks pass
- Human verifies About section visual appearance
- Human verifies update check behavior in dev mode
- Human verifies i18n translations
- Human reviews release workflow structure
</verification>

<success_criteria>
Phase 11 is complete: the app has updater infrastructure, a release workflow for multi-platform distribution, update UI in settings, and a blocking update modal. All quality gates pass. Human has verified the visual appearance.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-deployment-auto-updates-and-release-management/11-04-SUMMARY.md`
</output>
