---
phase: 10-improve-codebase-quality
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - package.json
  - vite.config.ts
  - vitest.config.ts
  - tsconfig.json
  - src-tauri/Cargo.toml
autonomous: true
requirements: [D-10, D-11]

must_haves:
  truths:
    - "All npm dependencies are at latest compatible versions"
    - "All Rust crates are updated within semver ranges"
    - "npm test passes with all 114+ tests after upgrades"
    - "npm run build succeeds after upgrades"
    - "cargo build succeeds in src-tauri/ after crate updates"
    - "npm run lint still passes after upgrades"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency versions"
      contains: "vite"
    - path: "src-tauri/Cargo.toml"
      provides: "Rust dependencies (unchanged semver ranges, cargo update handles patches)"
  key_links:
    - from: "package.json"
      to: "vite.config.ts"
      via: "vite version compatibility"
      pattern: "vite"
    - from: "package.json"
      to: "vitest.config.ts"
      via: "vitest version compatibility"
      pattern: "vitest"
---

<objective>
Update all npm dependencies and Rust crates to latest compatible versions, fixing any breaking changes.

Purpose: Bring the project to current dependency versions. Vite 6->8, TypeScript 5->6, and @vitejs/plugin-react 4->6 are the major bumps. Rust crates are already current and just need `cargo update` for patch-level updates.
Output: Updated package.json, lock files, and any config changes needed for breaking API changes.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-CONTEXT.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update npm dependencies (major + minor + patch)</name>
  <files>package.json, vite.config.ts, vitest.config.ts, tsconfig.json</files>
  <read_first>
    - package.json (current versions)
    - vite.config.ts (check for build.rollupOptions — rename to rolldownOptions if present for Vite 8)
    - vitest.config.ts (check for any Vite-specific config that might break)
    - tsconfig.json (check moduleResolution and other TS6-sensitive settings)
    - .planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md (dependency upgrade risk table)
  </read_first>
  <action>
1. **Upgrade major dependencies one group at a time, testing between each:**

   Group A — Vite ecosystem (upgrade together per peer dep requirements):
   ```bash
   npm install vite@^8.0.0 @vitejs/plugin-react@^6.0.0 @tailwindcss/vite@latest
   ```
   After install, check `vite.config.ts`:
   - If it contains `build.rollupOptions`, rename to `build.rolldownOptions` (current config does NOT have this, so likely no change needed)
   - Run `npm run build` to verify Vite 8 works

   Group B — TypeScript:
   ```bash
   npm install -D typescript@^6.0.0
   ```
   After install:
   - Run `npx tsc --noEmit` to check for TypeScript 6 errors
   - The project uses `moduleResolution: "bundler"` (compatible), `target: "ES2020"` (compatible), `skipLibCheck: true` (safe)
   - Fix any new type errors if they appear

   Group C — Vitest ecosystem:
   ```bash
   npm install -D vitest@latest @vitest/ui@latest
   ```
   After install:
   - Run `npm test` to verify all 114 tests still pass

   Group D — All remaining dependencies (minor/patch, low risk):
   ```bash
   npm update
   ```
   This updates all packages within their semver ranges in package.json.

   For packages that need major bumps beyond the caret range:
   ```bash
   npm install lucide-react@latest i18next@latest react-i18next@latest jsdom@latest @testing-library/react@latest @testing-library/jest-dom@latest @types/node@latest @types/react@latest @types/react-dom@latest
   ```

2. **Verify everything works after all upgrades:**
   - `npm run build` (tsc + vite build)
   - `npm test` (all tests pass)
   - `npm run lint` (biome still clean)

3. If any test failures occur after upgrades, fix the test or source code to be compatible with the new version. Document what changed and why.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run build && npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls vite` shows version 8.x
    - `npm ls typescript` shows version 6.x
    - `npm ls @vitejs/plugin-react` shows version 6.x
    - `npm run build` exits 0
    - `npm test` exits 0 with 114+ tests passing
    - `npm run lint` exits 0
    - No `build.rollupOptions` in vite.config.ts (was never there, but confirm)
  </acceptance_criteria>
  <done>All npm dependencies updated to latest compatible versions per D-10. Vite 8, TypeScript 6, plugin-react 6 all working. Build, tests, and lint pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update Rust crates and verify build</name>
  <files>src-tauri/Cargo.toml</files>
  <read_first>
    - src-tauri/Cargo.toml (current crate versions and semver ranges)
  </read_first>
  <action>
1. Run `cargo update` in `src-tauri/` to update all crates within their existing semver ranges. This handles patch-level updates for all dependencies.

2. Check if any crates have newer major versions available by comparing current Cargo.toml ranges with latest crates.io versions. The research indicates all Rust crates are already on current major versions, so `cargo update` should suffice.

3. Run `cargo build` in `src-tauri/` to verify compilation.

4. Run `cargo test` in `src-tauri/` to verify Rust tests (from Plan 02) still pass.

5. Run `cargo clippy -- -D warnings` in `src-tauri/` to verify clippy still passes.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar/src-tauri && cargo build 2>&1 | tail -3 && cargo test 2>&1 | tail -5 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0 in src-tauri/
    - `cargo test` exits 0 in src-tauri/
    - `cargo clippy -- -D warnings` exits 0 in src-tauri/
    - Cargo.lock is updated (committed)
  </acceptance_criteria>
  <done>All Rust crates updated within semver ranges per D-11. Build, tests, and clippy pass.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `npm test` exits 0
- `npm run lint` exits 0
- `cd src-tauri && cargo build` exits 0
- `cd src-tauri && cargo test` exits 0
- `cd src-tauri && cargo clippy -- -D warnings` exits 0
</verification>

<success_criteria>
All npm and Rust dependencies are at latest compatible versions. Build, test, and lint pipelines pass for both frontend and backend. No regressions from upgrades.
</success_criteria>

<output>
After completion, create `.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-03-SUMMARY.md`
</output>
