---
phase: 10-improve-codebase-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/rustfmt.toml
  - src-tauri/src/audit.rs
  - src-tauri/src/triage_db.rs
  - src-tauri/src/error.rs
  - src-tauri/src/jira_client.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/mock_server.rs
  - src-tauri/src/fixtures.rs
  - src-tauri/src/keychain.rs
  - src-tauri/src/main.rs
autonomous: true
requirements: [D-03, D-06]

must_haves:
  truths:
    - "cargo clippy -- -D warnings exits 0 in src-tauri/"
    - "cargo fmt --check exits 0 in src-tauri/"
    - "cargo test exits 0 in src-tauri/ with new unit tests for triage_db and audit"
    - "Clippy pedantic lint group is enabled with appropriate allows"
  artifacts:
    - path: "src-tauri/rustfmt.toml"
      provides: "Rust formatting configuration"
      contains: "max_width = 100"
    - path: "src-tauri/Cargo.toml"
      provides: "Clippy pedantic lint configuration"
      contains: "pedantic"
    - path: "src-tauri/src/triage_db.rs"
      provides: "Unit tests for triage database"
      contains: "#[cfg(test)]"
    - path: "src-tauri/src/audit.rs"
      provides: "Unit tests for audit database"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src-tauri/Cargo.toml"
      to: "src-tauri/src/*.rs"
      via: "[lints.clippy] section"
      pattern: "pedantic"
---

<objective>
Configure Rust quality tooling (clippy pedantic + rustfmt) and add unit tests for core Rust modules.

Purpose: Match the frontend quality bar with Rust-side linting and establish test coverage for the pure-logic Rust modules (triage_db, audit) that can be tested without the Tauri runtime.
Output: rustfmt.toml, Cargo.toml lint config, clean clippy/fmt checks, unit tests for triage_db and audit.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-CONTEXT.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure clippy pedantic and rustfmt, fix all Rust lint/format violations</name>
  <files>src-tauri/Cargo.toml, src-tauri/rustfmt.toml, src-tauri/src/*.rs</files>
  <read_first>
    - src-tauri/Cargo.toml (current config)
    - src-tauri/src/lib.rs (module structure)
    - src-tauri/src/commands.rs (largest source file likely)
    - src-tauri/src/jira_client.rs (complex module)
    - src-tauri/src/error.rs (error types)
    - src-tauri/src/audit.rs (audit module)
    - src-tauri/src/triage_db.rs (triage module)
    - src-tauri/src/mock_server.rs (mock server)
    - src-tauri/src/fixtures.rs (fixtures)
    - src-tauri/src/keychain.rs (keychain)
    - src-tauri/src/main.rs (entry point)
  </read_first>
  <action>
1. Create `src-tauri/rustfmt.toml` with this exact content:
   ```toml
   edition = "2021"
   max_width = 100
   use_small_heuristics = "Default"
   imports_granularity = "Crate"
   group_imports = "StdExternalCrate"
   ```

2. Add `[lints.clippy]` section to `src-tauri/Cargo.toml` (after `[features]` section):
   ```toml
   [lints.clippy]
   pedantic = "warn"
   module_name_repetitions = "allow"
   must_use_candidate = "allow"
   missing_errors_doc = "allow"
   missing_panics_doc = "allow"
   missing_docs_in_private_items = "allow"
   ```

3. Run `cargo fmt` in `src-tauri/` to auto-format all Rust files.

4. Run `cargo clippy -- -D warnings` in `src-tauri/` to see all pedantic violations.

5. Fix each clippy violation. Common fixes for Tauri projects:
   - Add `#[allow(clippy::unused_async)]` on async Tauri command functions that must be async for the framework but don't use await internally
   - Replace `.clone()` with borrows where clippy suggests
   - Add `must_use` attributes or allow individual functions
   - Fix needless borrows, redundant closures, manual string operations
   - Use `if let` instead of `match` with single arm where flagged
   - For legitimate suppressions, use `#[allow(clippy::specific_lint)]` with a comment explaining why

6. Iterate `cargo clippy -- -D warnings` until it exits 0.

7. Verify build still works: `cargo build` in `src-tauri/`.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar/src-tauri && cargo fmt --check && cargo clippy -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/rustfmt.toml` exists and contains `max_width = 100`
    - `src-tauri/Cargo.toml` contains `[lints.clippy]` section with `pedantic = "warn"`
    - `cargo fmt --check` exits 0 in src-tauri/
    - `cargo clippy -- -D warnings` exits 0 in src-tauri/
    - `cargo build` succeeds in src-tauri/
  </acceptance_criteria>
  <done>Clippy pedantic and rustfmt configured per D-03, all Rust code passes both checks, project builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Add Rust unit tests for triage_db and audit modules</name>
  <files>src-tauri/src/triage_db.rs, src-tauri/src/audit.rs</files>
  <read_first>
    - src-tauri/src/triage_db.rs (full file — understand TriageDb struct, methods, SQL schema)
    - src-tauri/src/audit.rs (full file — understand AuditDb struct, methods, SQL schema)
  </read_first>
  <action>
1. Add a `#[cfg(test)]` module at the bottom of `src-tauri/src/triage_db.rs` with these tests:
   - `test_new_creates_in_memory_db`: Create TriageDb with `:memory:`, verify it doesn't panic
   - `test_set_and_get_triage_state`: Insert a triage entry, retrieve it, verify fields match
   - `test_get_all_triage_returns_all`: Insert 3 entries with different states, verify get_all returns all 3
   - `test_update_triage_state`: Set state to "seen", update to "ignored", verify update persists
   - `test_set_and_get_fetch_config`: Store a fetch config, retrieve it, verify fields match
   - `test_invalid_triage_state_rejected`: Attempt to set an invalid state string, verify error

   Each test should create a fresh `TriageDb::new(":memory:")` instance (in-memory SQLite, no filesystem). Use `#[test]` (not `#[tokio::test]` — TriageDb is synchronous).

2. Add a `#[cfg(test)]` module at the bottom of `src-tauri/src/audit.rs` with these tests:
   - `test_new_creates_in_memory_db`: Create AuditDb with `:memory:`, verify no panic
   - `test_insert_and_get_entries`: Insert an audit entry, retrieve all, verify the entry exists with correct method/url/status
   - `test_get_count`: Insert 5 entries, verify count returns 5
   - `test_entries_ordered_by_timestamp`: Insert entries with different timestamps, verify they come back in expected order
   - `test_authorization_header_not_stored`: Insert an entry whose headers contain "Authorization", verify the stored entry has the header redacted (if redaction happens at insert time) or test that the header value is "[REDACTED]"

   Same pattern: `AuditDb::new(":memory:")` for each test.

3. Run `cargo test` in `src-tauri/` — all new tests must pass alongside any existing tests.

4. Run `cargo clippy -- -D warnings` to ensure new test code passes clippy too.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar/src-tauri && cargo test 2>&1 | tail -10 && cargo clippy -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `src-tauri/src/triage_db.rs` contains `#[cfg(test)]` module
    - `src-tauri/src/audit.rs` contains `#[cfg(test)]` module
    - `cargo test` in src-tauri/ exits 0 with at least 10 new tests passing
    - `cargo clippy -- -D warnings` still exits 0
    - Tests use `:memory:` SQLite (no filesystem side effects)
  </acceptance_criteria>
  <done>Rust unit tests exist for triage_db (6 tests) and audit (5 tests), all passing, clippy clean per D-06.</done>
</task>

</tasks>

<verification>
- `cd src-tauri && cargo fmt --check` exits 0
- `cd src-tauri && cargo clippy -- -D warnings` exits 0
- `cd src-tauri && cargo test` exits 0 with 11+ tests
- `src-tauri/rustfmt.toml` exists
- `src-tauri/Cargo.toml` contains `[lints.clippy]`
</verification>

<success_criteria>
Clippy pedantic and rustfmt are configured and passing. triage_db.rs and audit.rs have comprehensive unit test suites. All Rust code compiles, passes lint, and tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-02-SUMMARY.md`
</output>
