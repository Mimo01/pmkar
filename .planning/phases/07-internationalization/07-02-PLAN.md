---
phase: 07-internationalization
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
  - src/components/ui/AppShell.tsx
  - src/features/connections/SettingsPage.tsx
  - src/features/connections/ConnectionCard.tsx
  - src/features/connections/ConnectionForm.tsx
  - src/features/connections/SecretInput.tsx
  - src/features/connections/SetupWizard.tsx
  - src/features/connections/StepProgress.tsx
  - src/features/connections/SummaryStep.tsx
  - src/features/connections/TestResult.tsx
  - src/features/connections/WizardStep.tsx
  - src/features/tickets/TicketListPage.tsx
  - src/features/tickets/TicketTable.tsx
  - src/features/tickets/TicketDetailPanel.tsx
  - src/features/tickets/IgnoredTicketsPage.tsx
  - src/features/tickets/AuditLogPage.tsx
  - src/features/tickets/CopyPreviewModal.tsx
  - src/features/tickets/CopyResultModal.tsx
  - src/features/tickets/TriageIndicator.tsx
  - src/features/tickets/tabs/OverviewTab.tsx
  - src/features/tickets/tabs/CommentsTab.tsx
  - src/features/tickets/tabs/WorkLogTab.tsx
  - src/features/tickets/tabs/AttachmentsTab.tsx
  - src/features/tickets/tabs/HistoryTab.tsx
  - src/features/dev/DevStatusPanel.tsx
  - src/components/ui/StatusBadge.tsx
  - src/lib/format.ts
autonomous: true
requirements:
  - I18N-01
  - I18N-02
  - I18N-03

must_haves:
  truths:
    - "All UI chrome strings come from t() calls, not hardcoded literals"
    - "en.json contains every translation key used by every component"
    - "sk.json contains every key in en.json with Slovak translations"
    - "Language switcher dropdown appears in Settings page below Appearance section"
    - "Switching language changes all visible UI text immediately"
    - "Date and time displays use locale-aware formatting"
    - "Jira field names and API values are NOT translated (per D-05)"
  artifacts:
    - path: "src/i18n/locales/en.json"
      provides: "Complete English translation pack"
      min_lines: 80
    - path: "src/i18n/locales/sk.json"
      provides: "Complete Slovak translation pack"
      min_lines: 80
    - path: "src/features/connections/SettingsPage.tsx"
      provides: "LanguageSection component with dropdown"
      contains: "LanguageSection"
  key_links:
    - from: "src/components/ui/AppShell.tsx"
      to: "src/i18n/locales/en.json"
      via: "useTranslation hook"
      pattern: "t\\('nav\\."
    - from: "src/features/connections/SettingsPage.tsx"
      to: "src/i18n/languageStore.ts"
      via: "useLanguageStore for language dropdown"
      pattern: "useLanguageStore"
---

<objective>
Extract all hardcoded UI strings from every component into translation keys, create complete en.json and sk.json translation packs, add the LanguageSection dropdown to SettingsPage, and replace hardcoded relativeTime functions with the shared locale-aware formatRelativeTime utility.

Purpose: After this plan, every visible UI string in the app comes from translation files. Switching language in Settings immediately changes all text throughout the app.

Output: Complete en.json and sk.json (~100+ keys each), all ~25 component files updated to use t() calls, LanguageSection in SettingsPage, locale-aware date formatting throughout.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-internationalization/07-CONTEXT.md
@.planning/phases/07-internationalization/07-RESEARCH.md
@.planning/phases/07-internationalization/07-UI-SPEC.md
@.planning/phases/07-internationalization/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (executor should verify these exist) -->

From src/i18n/index.ts:
```typescript
import i18n from 'i18next';
// i18n instance initialized with en/sk resources
export default i18n;
```

From src/i18n/languageStore.ts:
```typescript
export type Language = 'en' | 'sk';
export const useLanguageStore: UseBoundStore<StoreApi<LanguageState>>;
// LanguageState has: language: Language, setLanguage: (lang: Language) => void
```

From src/lib/format.ts:
```typescript
export function formatDate(iso: string, options?: Intl.DateTimeFormatOptions): string;
export function formatRelativeTime(iso: string): string;
export function formatTimestamp(iso: string): string;
```

Usage pattern in components:
```typescript
import { useTranslation } from 'react-i18next';
function MyComponent() {
  const { t } = useTranslation();
  return <h1>{t('key.name')}</h1>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete en.json and sk.json translation packs</name>
  <read_first>
    - src/i18n/locales/en.json
    - src/i18n/locales/sk.json
    - src/components/ui/AppShell.tsx
    - src/features/connections/SettingsPage.tsx
    - src/features/connections/ConnectionCard.tsx
    - src/features/connections/ConnectionForm.tsx
    - src/features/connections/SecretInput.tsx
    - src/features/connections/SetupWizard.tsx
    - src/features/connections/StepProgress.tsx
    - src/features/connections/SummaryStep.tsx
    - src/features/connections/TestResult.tsx
    - src/features/connections/WizardStep.tsx
    - src/features/tickets/TicketListPage.tsx
    - src/features/tickets/TicketTable.tsx
    - src/features/tickets/TicketDetailPanel.tsx
    - src/features/tickets/IgnoredTicketsPage.tsx
    - src/features/tickets/AuditLogPage.tsx
    - src/features/tickets/CopyPreviewModal.tsx
    - src/features/tickets/CopyResultModal.tsx
    - src/features/tickets/TriageIndicator.tsx
    - src/features/tickets/tabs/OverviewTab.tsx
    - src/features/tickets/tabs/CommentsTab.tsx
    - src/features/tickets/tabs/WorkLogTab.tsx
    - src/features/tickets/tabs/AttachmentsTab.tsx
    - src/features/tickets/tabs/HistoryTab.tsx
    - src/features/dev/DevStatusPanel.tsx
    - src/components/ui/StatusBadge.tsx
    - .planning/phases/07-internationalization/07-UI-SPEC.md
  </read_first>
  <files>
    src/i18n/locales/en.json
    src/i18n/locales/sk.json
  </files>
  <action>
    Read EVERY component file listed in read_first. For each file, extract every hardcoded English string that is rendered as visible UI text. This includes:
    - Element text content (headings, paragraphs, buttons, labels, placeholders, aria-labels)
    - Error messages rendered to the user (D-04)
    - Empty state text
    - Table column headers
    - Modal titles and button text
    - Navigation labels
    - Section headings

    Do NOT extract (per D-05):
    - Jira field names that come from API data (these are rendered from ticket objects)
    - CSS class strings
    - Technical identifiers (tab IDs, route names)
    - Strings only used in test files

    **Key naming convention:** Flat dot-separated keys grouped by feature area.
    Namespaces: `nav.*`, `settings.*`, `tickets.*`, `detail.*`, `copy.*`, `audit.*`, `wizard.*`, `connection.*`, `error.*`, `common.*`

    **en.json must include AT MINIMUM these keys from the UI-SPEC copywriting contract:**

    Navigation:
    - `nav.tickets`: "Tickets"
    - `nav.ignored`: "Ignored"
    - `nav.apiCalls`: "{{count}} API calls" (with interpolation)

    Settings:
    - `settings.heading`: "Settings"
    - `settings.back`: "Back"
    - `settings.connections`: "Connections"
    - `settings.language`: "Language"
    - `settings.language.english`: "English"
    - `settings.language.slovak`: "Slovak"
    - `settings.whatToFetch`: "What to fetch"
    - `settings.watchedUsers`: "Watched users"
    - `settings.watchedUsers.count`: "({{count}})"
    - `settings.watchedUsers.hint`: "Tickets assigned to these users appear in \"All watched users\" results."
    - `settings.watchedUsers.searchPlaceholder`: "Search Jira users..."
    - `settings.watchedUsers.noResults`: "No users found matching \"{{query}}\""
    - `settings.watchedUsers.empty`: "No watched users yet"
    - `settings.watchedUsers.remove`: "Remove {{user}}"
    - `settings.appearance`: "Appearance"
    - `settings.theme.light`: "Light"
    - `settings.theme.dark`: "Dark"
    - `settings.theme.system`: "System"
    - `settings.noConnections`: "No connections configured"
    - `settings.noConnections.hint`: "Run setup to configure your Jira connections."
    - `settings.jql.placeholder`: "assignee = currentUser() ORDER BY updated DESC"
    - `settings.jql.reset`: "Reset to default"
    - `settings.preset.assigned`: "Assigned to me"
    - `settings.preset.mentioned`: "Mentioned"
    - `settings.preset.allWatched`: "All watched users"
    - `settings.preset.custom`: "Custom query"
    - `settings.editSource`: "Edit Source Connection"
    - `settings.editDestination`: "Edit Destination Connection"
    - `settings.cancel`: "Cancel"
    - `settings.sourceLabel`: "Source (Customer Jira)"
    - `settings.destLabel`: "Destination (Company Jira)"

    Tickets:
    - `tickets.fetchButton`: "Fetch Tickets"
    - `tickets.fetching`: "Fetching..."
    - `tickets.lastFetched`: "Last fetched: {{time}}"
    - `tickets.notYetFetched`: "Not yet fetched"
    - `tickets.candidates`: "{{count}} candidates"
    - `tickets.new`: "{{count}} new"
    - `tickets.empty`: "No candidates found"
    - `tickets.empty.hint`: "Your JQL returned no results. Try adjusting your fetch settings."
    - `tickets.fetchError`: "Could not fetch tickets"

    Ticket table headers:
    - `tickets.col.key`: "Key"
    - `tickets.col.summary`: "Summary"
    - `tickets.col.status`: "Status"
    - `tickets.col.priority`: "Priority"
    - `tickets.col.assignee`: "Assignee"
    - `tickets.col.updated`: "Updated"

    Detail panel:
    - `detail.close`: "Close detail panel"
    - `detail.tab.overview`: "Overview"
    - `detail.tab.comments`: "Comments"
    - `detail.tab.worklog`: "Work Log"
    - `detail.tab.attachments`: "Attachments"
    - `detail.tab.history`: "History"
    - `detail.copy`: "Copy to Company Jira"
    - `detail.copied`: "Copied"
    - `detail.ignore`: "Ignore"
    - `detail.ignored`: "Ignored"
    - `detail.loading`: "Loading..."

    Copy preview/result:
    - `copy.preview.title`: "Copy Preview"
    - `copy.preview.source`: "Source"
    - `copy.preview.target`: "Target"
    - `copy.preview.discard`: "Discard"
    - `copy.preview.confirm`: "Copy to Company Jira"
    - `copy.preview.copying`: "Copying..."
    - `copy.result.complete`: "Copy Complete"
    - `copy.result.withErrors`: "Copy Finished with Errors"
    - `copy.result.openInJira`: "Open in Jira"
    - `copy.result.close`: "Close"
    - `copy.step.coreFields`: "Core fields copied"
    - `copy.step.coreFieldsFailed`: "Core fields — failed to create ticket"
    - `copy.step.descConverted`: "Description converted"
    - `copy.step.descFailed`: "Description — conversion failed, plain text used"
    - `copy.step.imagesUploaded`: "Inline images uploaded"
    - `copy.step.imagesFailed`: "Inline images — upload failed"
    - `copy.step.remoteLinkAdded`: "Origin link added"
    - `copy.step.remoteLinkFailed`: "Origin link — could not be added"
    - `copy.step.attached`: "{{filename}} — attached"
    - `copy.step.attachFailed`: "{{filename}} — {{detail}}"
    - `copy.step.commentCopied`: "Comment {{n}} copied"
    - `copy.step.commentFailed`: "Comment {{n}} — {{detail}}"
    - `copy.step.worklogCopied`: "Work log entry {{n}} copied"
    - `copy.step.worklogFailed`: "Work log {{n}} — {{detail}}"
    - `copy.step.subtaskCreated`: "Sub-task {{key}} — {{detail}}"
    - `copy.step.subtaskFailed`: "Sub-task {{key}} — {{detail}}"

    Ignored page:
    - `ignored.heading`: "Ignored Tickets"
    - `ignored.empty`: "No ignored tickets"
    - `ignored.restore`: "Restore"
    - `ignored.col.key`: "Key"
    - `ignored.col.summary`: "Summary"
    - `ignored.col.updated`: "Updated"
    - `ignored.col.actions`: "Actions"

    Audit log:
    - `audit.heading`: "Audit Log"
    - `audit.back`: "Back"
    - `audit.empty`: "No API calls recorded"
    - `audit.clear`: "Clear log"
    - `audit.col.time`: "Time"
    - `audit.col.method`: "Method"
    - `audit.col.url`: "URL"
    - `audit.col.status`: "Status"
    - `audit.response`: "Response"

    Error messages (per D-04):
    - `error.connectionFailed`: "Connection failed"
    - `error.authFailed`: "Authentication failed. Check your Source connection credentials in Settings."
    - `error.networkError`: "Could not reach the server. Check your network and Source connection URL."
    - `error.rateLimited`: "Rate limited by Jira. Try again in a few seconds."
    - `error.serverError`: "Jira returned a server error. Try again or check server status."
    - `error.generic`: "An error occurred: {{message}}"

    Wizard/Connection:
    - `wizard.title`: "Setup"
    - `wizard.step.source`: "Source"
    - `wizard.step.destination`: "Destination"
    - `wizard.step.done`: "Done"
    - `connection.testButton`: "Test Connection"
    - `connection.testing`: "Testing..."
    - `connection.baseUrl`: "Base URL"
    - `connection.baseUrl.placeholder.server`: "https://jira.example.com"
    - `connection.baseUrl.placeholder.cloud`: "https://your-domain.atlassian.net"
    - `connection.pat`: "Personal Access Token"
    - `connection.pat.hint`: "Create one in your Jira profile"
    - `connection.email`: "Email"
    - `connection.apiToken`: "API Token"
    - `connection.apiToken.hint`: "Create one at id.atlassian.com"

    Common:
    - `common.loading`: "Loading..."
    - `common.error`: "Error"
    - `common.retry`: "Retry"
    - `common.settings`: "Settings"

    Plus any additional strings found during component reading. The executor MUST read every component file to catch all strings.

    **sk.json must contain the Slovak translation for every key in en.json.** Use the UI-SPEC copywriting contract for canonical translations. For keys not explicitly listed in the UI-SPEC, translate appropriately. Key Slovak translations from the UI-SPEC:

    Navigation: "Listky", "Ignorovane"
    Settings: "Nastavenia", "Pripojenia", "Jazyk", "Anglictina", "Slovencina", "Co nacitat", "Sledovani pouzivatelia", "Vzhfad"
    Tickets: "Nacitat listky", "Ziadne listky"
    Errors: "Chyba pripojenia", "Autentifikacia zlyhala (401)"

    Note: Slovak text uses diacritical marks (hacky, dlzne, makcene). Use proper Unicode:
    - c with hacek: c (U+010D)
    - s with hacek: s (U+0161)
    - z with hacek: z (U+017E)
    - a with dlzen: a (U+00E1)
    - e with dlzen: e (U+00E9)
    - i with dlzen: i (U+00ED)
    - o with dlzen: o (U+00F3)
    - u with dlzen: u (U+00FA)
    - l with dlzen: l (U+013E)
    - n with hacek: n (U+0148)
    - t with hacek: t (U+0165)
    - d with hacek: d (U+010F)
    - y with dlzen: y (U+00FD)
    - r with hacek: r (U+0159) (not used in Slovak — avoid)

    Proper Slovak examples: "Listky" = "Listky" with proper diacritics, "Nastavenia", "Pripojenia", "Sledovani pouzivatelia" = "Sledovani pouzivatelia" with proper diacritics.

    IMPORTANT: The UI-SPEC shows ASCII-only approximations. The actual sk.json MUST use proper Unicode Slovak characters.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && node -e "const en=require('./src/i18n/locales/en.json'); const sk=require('./src/i18n/locales/sk.json'); const enKeys=Object.keys(en).sort(); const skKeys=Object.keys(sk).sort(); const missing=enKeys.filter(k=>!skKeys.includes(k)); const extra=skKeys.filter(k=>!enKeys.includes(k)); console.log('EN keys:', enKeys.length, 'SK keys:', skKeys.length); if(missing.length) console.log('MISSING from SK:', missing); if(extra.length) console.log('EXTRA in SK:', extra); if(missing.length===0 && extra.length===0 && enKeys.length>=80) console.log('PASS: key sets match and >=80 keys'); else console.log('FAIL');"</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/locales/en.json has at least 80 keys
    - src/i18n/locales/sk.json has exactly the same keys as en.json (no missing, no extra)
    - en.json contains `"nav.tickets": "Tickets"`
    - en.json contains `"settings.language": "Language"`
    - en.json contains `"settings.language.english": "English"`
    - en.json contains `"settings.language.slovak": "Slovak"`
    - en.json contains `"tickets.fetchButton"`
    - en.json contains `"copy.preview.title"`
    - en.json contains `"audit.heading"`
    - en.json contains `"error.authFailed"`
    - sk.json contains `"nav.tickets"` with a Slovak value (not English)
    - sk.json contains `"settings.language"` with value `"Jazyk"`
    - sk.json values use proper Unicode diacritics (not ASCII approximations)
  </acceptance_criteria>
  <done>Complete English and Slovak translation packs with matched key sets covering all UI chrome strings, error messages, and interactive text across every component</done>
</task>

<task type="auto">
  <name>Task 2: Extract strings from all components, add LanguageSection, replace relativeTime with shared utility</name>
  <read_first>
    - src/i18n/locales/en.json (from Task 1)
    - src/i18n/languageStore.ts
    - src/lib/format.ts
    - src/components/ui/AppShell.tsx
    - src/features/connections/SettingsPage.tsx
    - src/features/tickets/TicketListPage.tsx
    - src/features/tickets/TicketTable.tsx
    - src/features/tickets/TicketDetailPanel.tsx
    - src/features/tickets/IgnoredTicketsPage.tsx
    - src/features/tickets/AuditLogPage.tsx
    - src/features/tickets/CopyPreviewModal.tsx
    - src/features/tickets/CopyResultModal.tsx
    - src/features/tickets/TriageIndicator.tsx
    - src/features/tickets/tabs/OverviewTab.tsx
    - src/features/tickets/tabs/CommentsTab.tsx
    - src/features/tickets/tabs/WorkLogTab.tsx
    - src/features/tickets/tabs/AttachmentsTab.tsx
    - src/features/tickets/tabs/HistoryTab.tsx
    - src/features/connections/ConnectionCard.tsx
    - src/features/connections/ConnectionForm.tsx
    - src/features/connections/SecretInput.tsx
    - src/features/connections/SetupWizard.tsx
    - src/features/connections/StepProgress.tsx
    - src/features/connections/SummaryStep.tsx
    - src/features/connections/TestResult.tsx
    - src/features/connections/WizardStep.tsx
    - src/features/dev/DevStatusPanel.tsx
    - src/components/ui/StatusBadge.tsx
    - .planning/phases/07-internationalization/07-UI-SPEC.md
  </read_first>
  <files>
    src/components/ui/AppShell.tsx
    src/features/connections/SettingsPage.tsx
    src/features/connections/ConnectionCard.tsx
    src/features/connections/ConnectionForm.tsx
    src/features/connections/SecretInput.tsx
    src/features/connections/SetupWizard.tsx
    src/features/connections/StepProgress.tsx
    src/features/connections/SummaryStep.tsx
    src/features/connections/TestResult.tsx
    src/features/connections/WizardStep.tsx
    src/features/tickets/TicketListPage.tsx
    src/features/tickets/TicketTable.tsx
    src/features/tickets/TicketDetailPanel.tsx
    src/features/tickets/IgnoredTicketsPage.tsx
    src/features/tickets/AuditLogPage.tsx
    src/features/tickets/CopyPreviewModal.tsx
    src/features/tickets/CopyResultModal.tsx
    src/features/tickets/TriageIndicator.tsx
    src/features/tickets/tabs/OverviewTab.tsx
    src/features/tickets/tabs/CommentsTab.tsx
    src/features/tickets/tabs/WorkLogTab.tsx
    src/features/tickets/tabs/AttachmentsTab.tsx
    src/features/tickets/tabs/HistoryTab.tsx
    src/features/dev/DevStatusPanel.tsx
    src/components/ui/StatusBadge.tsx
  </files>
  <action>
    This task has three parts: (A) AppShell NAV_TABS, (B) LanguageSection in SettingsPage, (C) string extraction for all components.

    **Part A — AppShell.tsx:**
    Per UI-SPEC section "Modified: AppShell NAV_TABS":
    1. Add `import { useTranslation } from 'react-i18next';`
    2. Move `NAV_TABS` from module-level constant to INSIDE the `AppShell` component function body
    3. Use `useTranslation()` hook to get `t` function
    4. Replace hardcoded labels: `{ id: 'tickets', label: t('nav.tickets') }`, `{ id: 'ignored', label: t('nav.ignored') }`
    5. Replace the audit footer text: `{auditCount ?? 0} API calls` becomes `{t('nav.apiCalls', { count: auditCount ?? 0 })}`
    6. Replace `aria-label="Settings"` with `aria-label={t('common.settings')}`
    7. Replace `aria-label="Open audit log"` with `aria-label={t('audit.open')}`

    **Part B — SettingsPage.tsx LanguageSection:**
    Per UI-SPEC Component Inventory and D-01:
    1. Add imports: `import { useTranslation } from 'react-i18next';` and `import { useLanguageStore, type Language } from '../../i18n/languageStore';`
    2. Create a `LanguageSection` function component (same pattern as `ThemeSection`):
       ```tsx
       function LanguageSection() {
         const { t } = useTranslation();
         const language = useLanguageStore((s) => s.language);
         const setLanguage = useLanguageStore((s) => s.setLanguage);
         return (
           <section>
             <h2 className="text-xs font-semibold text-brand-muted uppercase tracking-wider mb-3">
               {t('settings.language')}
             </h2>
             <select
               value={language}
               onChange={(e) => setLanguage(e.target.value as Language)}
               className="w-full rounded-lg border border-brand-border bg-brand-surface text-brand-text px-3 py-2.5 text-sm focus:outline-none focus:border-brand/50 focus:ring-1 focus:ring-brand/20 transition-colors duration-200"
             >
               <option value="en">{t('settings.language.english')}</option>
               <option value="sk">{t('settings.language.slovak')}</option>
             </select>
           </section>
         );
       }
       ```
    3. Add `<LanguageSection />` BELOW `<ThemeSection />` in the `space-y-8` div (per UI-SPEC: "Below ThemeSection")
    4. Also translate ALL existing hardcoded strings in SettingsPage: section headings ("Connections", "What to fetch", "Watched users"), preset labels ("Assigned to me", etc.), button text ("Cancel", "Reset to default"), placeholder text, empty state text, aria-labels

    **Part C — String extraction for ALL component files:**

    For EVERY component file listed in `<files>`, do the following:
    1. Add `import { useTranslation } from 'react-i18next';` at the top
    2. Add `const { t } = useTranslation();` as the first line inside the component function (or inside each component function if the file has multiple)
    3. Replace every hardcoded English string with the corresponding `t('key')` call using keys from en.json
    4. For strings with dynamic values, use i18next interpolation: `t('key', { variable: value })`
    5. For strings that are constants outside components (like `PRESET_OPTIONS` labels in SettingsPage or `THEME_OPTIONS` labels), move them inside the component function body where `t()` is available, OR compute them inside the render using `t()`

    **Specific changes per file group:**

    **Ticket pages (TicketListPage, TicketTable, IgnoredTicketsPage):**
    - DELETE the local `relativeTime()` function from each file
    - Import `formatRelativeTime` from `../../lib/format` (or `../lib/format` depending on path depth)
    - Replace all `relativeTime(...)` calls with `formatRelativeTime(...)`
    - Replace all hardcoded strings with t() calls

    **AuditLogPage:**
    - Replace the local `formatTimestamp()` function with import from `../../lib/format`
    - Replace all hardcoded strings with t() calls (heading, column headers, empty state, button text)

    **CopyPreviewModal and CopyResultModal:**
    - Replace all hardcoded strings in labels, buttons, headings with t() calls
    - The `stepLabel()` function in CopyResultModal should use t() for each step type

    **Connection components (ConnectionForm, SecretInput, TestResult, SetupWizard, SummaryStep, etc.):**
    - Replace all field labels, placeholders, button text, error messages with t() calls

    **TriageIndicator, StatusBadge, OverviewTab, CommentsTab, WorkLogTab, AttachmentsTab, HistoryTab:**
    - Replace any hardcoded UI strings with t() calls

    **DevStatusPanel:**
    - Replace any hardcoded UI strings with t() calls

    **CRITICAL RULES:**
    - Do NOT translate Jira field VALUES (status text, priority text, assignee names, ticket summaries, descriptions) — per D-05 these come from Jira
    - Do NOT translate technical strings (CSS classes, Tauri command names, type identifiers)
    - DO translate: headings, labels, buttons, placeholders, empty states, error messages, aria-labels, table column headers
    - The `py-2.5` on the language `<select>` is mandatory per UI-SPEC (project-wide inherited deviation)
    - Visual appearance must be pixel-perfect identical to before (same Tailwind classes, same layout)

    **ThemeSection labels:**
    The `THEME_OPTIONS` array in SettingsPage has hardcoded labels ("Light", "Dark", "System"). Move these inside the ThemeSection component and use `t('settings.theme.light')`, `t('settings.theme.dark')`, `t('settings.theme.system')`.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/AppShell.tsx contains `useTranslation` import
    - src/components/ui/AppShell.tsx contains `t('nav.tickets')`
    - src/components/ui/AppShell.tsx does NOT contain `label: 'Tickets'` as a hardcoded string constant
    - src/features/connections/SettingsPage.tsx contains `function LanguageSection`
    - src/features/connections/SettingsPage.tsx contains `useLanguageStore`
    - src/features/connections/SettingsPage.tsx contains `<LanguageSection />`
    - src/features/connections/SettingsPage.tsx contains `py-2.5` on the select element
    - src/features/tickets/TicketListPage.tsx does NOT contain `function relativeTime`
    - src/features/tickets/TicketListPage.tsx contains `formatRelativeTime` import
    - src/features/tickets/TicketTable.tsx does NOT contain `function relativeTime`
    - src/features/tickets/TicketTable.tsx contains `formatRelativeTime` import
    - src/features/tickets/IgnoredTicketsPage.tsx does NOT contain `function relativeTime`
    - src/features/tickets/IgnoredTicketsPage.tsx contains `formatRelativeTime` import
    - src/features/tickets/TicketListPage.tsx contains `useTranslation`
    - src/features/tickets/CopyResultModal.tsx contains `useTranslation`
    - src/features/tickets/AuditLogPage.tsx contains `useTranslation`
    - src/features/connections/ConnectionForm.tsx contains `useTranslation`
    - All existing vitest tests pass (npx vitest run exits 0)
  </acceptance_criteria>
  <done>Every component uses t() for UI strings, LanguageSection dropdown renders in Settings below Appearance, relativeTime replaced with shared locale-aware formatRelativeTime, all existing tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` passes (all existing tests green with i18n)
- `grep -rL "useTranslation" src/features/ src/components/ui/AppShell.tsx --include="*.tsx" | grep -v test | grep -v types | grep -v main` returns no production component files with visible text
- `node -e "const en=require('./src/i18n/locales/en.json'); const sk=require('./src/i18n/locales/sk.json'); console.log('Match:', JSON.stringify(Object.keys(en).sort()) === JSON.stringify(Object.keys(sk).sort()), 'Keys:', Object.keys(en).length);"` shows Match: true with 80+ keys
- No hardcoded `'en'` locale in Intl.RelativeTimeFormat calls
</verification>

<success_criteria>
- Complete en.json and sk.json with matched key sets (80+ keys each)
- Every visible UI string comes from t() calls (no hardcoded English in production components)
- LanguageSection dropdown in SettingsPage below Appearance section per D-01
- Native select element with `py-2.5` per UI-SPEC
- relativeTime functions replaced with shared formatRelativeTime utility
- Jira field values remain untranslated per D-05
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-internationalization/07-02-SUMMARY.md`
</output>
