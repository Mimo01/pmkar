---
phase: 02-connection-setup
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/ui/AppShell.tsx
  - src/features/connections/SettingsPage.tsx
  - src/features/connections/ConnectionCard.tsx
  - src/App.tsx
  - src/features/connections/ConnectionForm.test.tsx
  - src/features/connections/TestResult.test.tsx
  - src/features/connections/SetupWizard.test.tsx
  - src/features/connections/SecretInput.test.tsx
autonomous: false
requirements: [CONN-04, CONN-05, CONN-06]

must_haves:
  truths:
    - "Gear icon in AppShell header opens Settings page"
    - "Settings page shows two ConnectionCard components with status dot, URL, last tested, Edit button"
    - "Edit button opens wizard at relevant step for re-testing"
    - "Frontend tests verify wizard step progression, form field rendering, and error message display"
  artifacts:
    - path: "src/components/ui/AppShell.tsx"
      provides: "Gear icon button in header with onGearClick callback"
      contains: "onGearClick"
    - path: "src/features/connections/SettingsPage.tsx"
      provides: "Settings page with two ConnectionCard components"
      min_lines: 20
    - path: "src/features/connections/ConnectionCard.tsx"
      provides: "Card showing connection status, URL, last tested, Edit button"
      min_lines: 20
    - path: "src/features/connections/ConnectionForm.test.tsx"
      provides: "Tests for CONN-01, CONN-02: correct fields per type, URL validation, invoke calls"
    - path: "src/features/connections/TestResult.test.tsx"
      provides: "Tests for CONN-04, CONN-05: success text, each error copy string, roles"
    - path: "src/features/connections/SetupWizard.test.tsx"
      provides: "Tests for CONN-06: step progression, Next gating, wizard completion"
    - path: "src/features/connections/SecretInput.test.tsx"
      provides: "Tests for eye toggle, aria-label changes"
  key_links:
    - from: "src/App.tsx"
      to: "src/features/connections/SettingsPage.tsx"
      via: "showSettings state toggle from AppShell onGearClick"
      pattern: "setShowSettings"
    - from: "src/components/ui/AppShell.tsx"
      to: "App.tsx"
      via: "onGearClick callback prop"
      pattern: "onGearClick"
---

<objective>
Add the Settings page with connection cards, gear icon navigation in AppShell, and comprehensive frontend tests for all wizard components.

Purpose: Completes the post-setup access pattern (gear icon -> Settings page -> Edit) and validates the entire wizard flow with automated tests. The checkpoint verifies the complete visual flow end-to-end.

Output: Updated AppShell with gear icon, SettingsPage + ConnectionCard components, 4 test files, visual verification.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-connection-setup/02-CONTEXT.md
@.planning/phases/02-connection-setup/02-RESEARCH.md
@.planning/phases/02-connection-setup/02-UI-SPEC.md
@.planning/phases/02-connection-setup/02-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 outputs -->

From src/features/connections/types.ts:
```typescript
export type ConnectionStatus = 'unconfigured' | 'ok' | 'error';
export interface ConnectionMeta {
  baseUrl: string;
  username: string;
  serverVersion: string;
  lastTestedAt: string;
  status: ConnectionStatus;
}
export interface ConnectionTestResult {
  success: boolean;
  username: string | null;
  serverVersion: string | null;
  errorKind: string | null;
  retryAfterSecs: number | null;
}
export type ConnectionType = 'server' | 'cloud';
```

From src/features/connections/connectionStore.ts:
```typescript
export const useConnectionStore = create<ConnectionState>((set, get) => ({
  serverConnection: ConnectionMeta | null,
  cloudConnection: ConnectionMeta | null,
  setServerConnection: (meta: ConnectionMeta) => void,
  setCloudConnection: (meta: ConnectionMeta) => void,
  clearConnections: () => void,
  hasCompletedSetup: () => boolean,
}));
```

From src/components/ui/AppShell.tsx (current):
```typescript
interface AppShellProps { children: ReactNode; }
export function AppShell({ children }: AppShellProps) { ... }
```

From src/App.tsx (after Plan 02):
```typescript
// Has useState(false) for showSettings, conditional wizard/app render
```

From src/App.test.tsx (existing mock pattern):
```typescript
vi.mock('@tauri-apps/api/core', () => ({
  invoke: vi.fn((cmd: string) => { ... }),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gear icon to AppShell, build SettingsPage and ConnectionCard, wire App.tsx navigation</name>
  <files>
    src/components/ui/AppShell.tsx,
    src/features/connections/ConnectionCard.tsx,
    src/features/connections/SettingsPage.tsx,
    src/App.tsx
  </files>
  <read_first>
    - src/components/ui/AppShell.tsx (current component, props interface)
    - src/App.tsx (current state after Plan 02 — wizard/app toggle, showSettings state)
    - src/features/connections/connectionStore.ts (store shape, how to read serverConnection/cloudConnection)
    - src/features/connections/types.ts (ConnectionMeta interface, ConnectionStatus type)
    - .planning/phases/02-connection-setup/02-UI-SPEC.md (Settings page layout, ConnectionCard spec, gear icon spec, Copywriting Contract section)
    - .planning/phases/02-connection-setup/02-CONTEXT.md (Post-setup access decisions: gear icon, card layout, Edit re-test)
  </read_first>
  <action>
**1. Update `AppShell.tsx`** to accept optional gear icon callback:

- Change props interface to: `{ children: ReactNode; onGearClick?: () => void }`
- When `onGearClick` is provided, render a gear icon button in the header (top-right)
- Add a header bar: `<header className="flex items-center justify-between px-6 py-3 border-b border-slate-200 dark:border-slate-700">`
- Left side: app name "Pmkar" in `text-lg font-semibold`
- Right side: gear icon button with `w-11 h-11` (44px hit area), inline SVG gear icon, `focus-visible:outline-2 focus-visible:outline-blue-600 focus-visible:outline-offset-2`
- `aria-label="Settings"` on gear button
- If `onGearClick` is not provided, do not render the gear button (graceful degradation for wizard mode)

**2. Create `ConnectionCard.tsx`:**

- Props: `{ label: string; connection: ConnectionMeta | null; onEdit: () => void }`
- Card container: `bg-slate-100 dark:bg-slate-800 rounded-lg p-4 mb-4`
- Status dot: 8px circle (`w-2 h-2 rounded-full inline-block mr-2`), colors:
  - `connection.status === 'ok'` -> `bg-green-600 dark:bg-green-400`
  - `connection.status === 'error'` -> `bg-red-600 dark:bg-red-400`
  - `connection === null` or `status === 'unconfigured'` -> `bg-slate-400 dark:bg-slate-500`
- Connection label: `text-base font-semibold` (e.g., "Source (Customer Jira)")
- Base URL: `text-sm text-slate-500 dark:text-slate-400`, show `connection.baseUrl` or empty
- Last tested: `text-sm text-slate-400 dark:text-slate-500`
  - If `connection?.lastTestedAt`: compute relative time string "Last tested: X ago" (use simple computation: compare ISO timestamp to now, show "X minutes ago", "X hours ago", "X days ago")
  - If no connection: show "Not yet tested"
- Edit button: `text-sm text-blue-600 hover:text-blue-700 dark:text-blue-400 font-semibold`, text "Edit"
- Layout: status dot + label on top line, URL below, last tested below that, Edit button right-aligned

**3. Create `SettingsPage.tsx`:**

- Props: `{ onClose: () => void }`
- Container: `max-w-[640px] mx-auto px-6 py-8`
- Page title: "Connections" in `text-2xl font-semibold mb-6`
- Back/close button or arrow at top to return to main app (calls `onClose`)
- Read from store: `const serverConn = useConnectionStore((s) => s.serverConnection)`; same for cloudConn
- Render two `ConnectionCard` components:
  - `<ConnectionCard label="Source (Customer Jira)" connection={serverConn} onEdit={() => handleEdit('server')} />`
  - `<ConnectionCard label="Destination (Company Jira)" connection={cloudConn} onEdit={() => handleEdit('cloud')} />`
- `handleEdit` for now: set a state to show the wizard at the relevant step (can be a simple re-render of SetupWizard with initial step). For Phase 2 MVP: clicking Edit navigates to the SetupWizard starting at step 1 (server) or step 2 (cloud). This means: `onEdit` calls a callback that sets `showSettings=false` and shows wizard in "edit mode". Implementation: add an `editStep` state, when set, render `SetupWizard` with `initialStep={editStep}`. Pass `initialStep` as optional prop to SetupWizard.
- If both connections null: show empty state: "No connections configured" / "Run setup to configure your Jira connections." in `text-slate-500 text-center py-12`

**4. Update `App.tsx`** to wire settings navigation:

- Import `SettingsPage`
- The `showSettings` state should already exist from Plan 02. Wire it:
  - Pass `onGearClick={() => setShowSettings(true)}` to `<AppShell>`
  - When `showSettings && hasSetup`: render `<SettingsPage onClose={() => setShowSettings(false)} />`
  - Otherwise when `hasSetup`: render `<DevStatusPanel />`
- Add `editStep` state for Edit flow: when SettingsPage triggers edit, set `editStep` and show wizard in edit mode
- Full conditional: `!hasSetup || editStep` -> SetupWizard, `hasSetup && showSettings` -> SettingsPage, `hasSetup && !showSettings` -> DevStatusPanel
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AppShell.tsx props interface includes `onGearClick?: () => void`
    - AppShell.tsx contains `aria-label="Settings"` on gear button
    - AppShell.tsx renders gear icon only when `onGearClick` is provided
    - ConnectionCard.tsx has props: label, connection, onEdit
    - ConnectionCard.tsx renders status dot with `bg-green-600` for ok, `bg-red-600` for error, `bg-slate-400` for null/unconfigured
    - ConnectionCard.tsx displays "Not yet tested" when no connection
    - ConnectionCard.tsx displays "Edit" button that calls onEdit
    - SettingsPage.tsx renders two ConnectionCard with labels "Source (Customer Jira)" and "Destination (Company Jira)"
    - SettingsPage.tsx reads from `useConnectionStore`
    - App.tsx passes `onGearClick` to AppShell
    - App.tsx conditionally renders SettingsPage when showSettings is true
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Gear icon in AppShell opens Settings page. Settings page shows both connections as cards. Edit button triggers re-entry to wizard.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend tests for wizard components</name>
  <files>
    src/features/connections/ConnectionForm.test.tsx,
    src/features/connections/TestResult.test.tsx,
    src/features/connections/SetupWizard.test.tsx,
    src/features/connections/SecretInput.test.tsx
  </files>
  <read_first>
    - src/App.test.tsx (existing vi.mock pattern for @tauri-apps/api/core, testing-library usage)
    - src/features/connections/ConnectionForm.tsx (component props, invoke calls, field names)
    - src/features/connections/TestResult.tsx (component props, copy strings, role attributes)
    - src/features/connections/SetupWizard.tsx (step state, Next button, store interactions)
    - src/features/connections/SecretInput.tsx (eye toggle, aria-label values)
    - src/features/connections/types.ts (ConnectionTestResult shape for mock data)
    - .planning/phases/02-connection-setup/02-RESEARCH.md (Validation Architecture section, Wave 0 Gaps section for test descriptions)
  </read_first>
  <action>
Create 4 test files using vitest + @testing-library/react. Mock `@tauri-apps/api/core` invoke in each test file.

**1. `SecretInput.test.tsx`:**

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { SecretInput } from './SecretInput';
```

Tests:
- "renders as password input by default": query `<input>` with `type="password"`
- "eye icon toggles to text and back": click toggle button, verify `type="text"`, click again, verify `type="password"`
- "aria-label changes on toggle": initially "Show token", after click "Hide token"
- "renders help link with correct href": pass helpUrl, verify `<a>` has correct href and `target="_blank"`
- "disabled state applies opacity": pass `disabled={true}`, verify input has `disabled` attribute

**2. `TestResult.test.tsx`:**

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { TestResult } from './TestResult';
import type { ConnectionTestResult } from './types';
```

Tests:
- "renders nothing when result is null": render with `result={null}`, container should be empty
- "renders success message with username and version for server": pass `{ success: true, username: 'jdoe', serverVersion: '8.20.0', errorKind: null, retryAfterSecs: null }` with `connectionType="server"`, verify text contains "Connected as jdoe — Jira Server v8.20.0", verify `role="status"`
- "renders success message for cloud": pass success result with `connectionType="cloud"`, verify text contains "Jira Cloud v"
- "renders auth error": pass `{ success: false, errorKind: 'auth', ... }`, verify text = "Authentication failed — check your PAT is valid", verify `role="alert"`
- "renders forbidden error": errorKind='forbidden' -> "PAT lacks required permissions"
- "renders rate_limit error with seconds": errorKind='rate_limit', retryAfterSecs=30 -> "Rate limited — try again in 30 seconds"
- "renders server_error": errorKind='server_error' -> "Server error — try again later"
- "renders network error": errorKind='network' -> "Cannot reach server — check URL"

**3. `ConnectionForm.test.tsx`:**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { ConnectionForm } from './ConnectionForm';

vi.mock('@tauri-apps/api/core', () => ({
  invoke: vi.fn(),
}));
import { invoke } from '@tauri-apps/api/core';
```

Tests:
- "renders Base URL and PAT fields for server type": render with `connectionType="server"`, verify labels "Base URL" and "Personal Access Token" are present
- "renders Base URL, Email, and API Token fields for cloud type": render with `connectionType="cloud"`, verify all 3 labels present
- "shows URL validation error on blur for non-https URL": type "http://example.com" in Base URL, fire blur, verify error text "URL must start with https://"
- "strips trailing slash on blur": type "https://jira.example.com/", fire blur, verify input value is "https://jira.example.com"
- "Test Connection calls correct invoke for server": mock invoke to resolve with success result, click "Test Connection", verify `invoke` called with `'test_jira_server_connection'` and `{ baseUrl, pat }`
- "Test Connection calls correct invoke for cloud": same for cloud with `'test_jira_cloud_connection'` and `{ baseUrl, email, apiToken }`
- "Test Connection button disabled during test": mock invoke with a pending promise, click button, verify button is disabled

**4. `SetupWizard.test.tsx`:**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { SetupWizard } from './SetupWizard';

vi.mock('@tauri-apps/api/core', () => ({
  invoke: vi.fn(),
}));
```

Tests:
- "renders Step 1 (Source Connection) initially": verify "Source Connection" heading visible
- "Next button not visible before test success": verify no button with text "Next" in DOM
- "Step progress shows Source as current": verify step progress renders with correct aria-label indicating step 1 current
- "advances to Step 2 after test success and Next click": mock invoke to return success, click "Test Connection", wait for success, then click "Next", verify "Destination Connection" heading visible
- "renders Summary step after both connections tested": advance through step 1 and step 2 with mocked success, verify "All Set" heading visible
- "Done button completes wizard": on summary step, verify "Done" button present

All tests must mock `invoke` for both test commands AND `store_credential`. Reset mocks in `beforeEach`.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm test -- --reporter=verbose src/features/connections 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - SecretInput.test.tsx contains at least 4 test cases covering toggle, aria-label, help link, disabled
    - TestResult.test.tsx contains tests for null result, success (server), success (cloud), and all 5 error kinds
    - ConnectionForm.test.tsx verifies correct fields per connection type and invoke call with correct command name
    - SetupWizard.test.tsx verifies step progression and Next button gating
    - All test files import from `vitest` and `@testing-library/react`
    - All test files that use invoke have `vi.mock('@tauri-apps/api/core')`
    - `npm test -- src/features/connections` exits with 0 (all tests pass)
  </acceptance_criteria>
  <done>All frontend tests pass. Test coverage validates CONN-01 (server form fields), CONN-02 (cloud form fields), CONN-04 (test result display), CONN-05 (error messages), CONN-06 (wizard flow).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete wizard flow and settings page</name>
  <files>src/App.tsx</files>
  <action>
Human verifies the complete connection setup flow visually by running the app against mock servers. This checkpoint validates all UI interactions, copy strings, error states, and navigation that cannot be fully covered by unit tests.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Human has confirmed the wizard flow, settings page, and error handling all work correctly in the running app.</done>
  <what-built>Complete connection setup wizard with 3-step flow, Settings page with gear icon, and all error handling states</what-built>
  <how-to-verify>
    1. Run `cd /Users/mimo/Desktop/pmkar && npm run tauri dev` to launch the app
    2. The Setup Wizard should appear (first launch, no connections configured)
    3. Verify Step 1 shows "Source Connection" / "Customer Jira Server" with progress dots
    4. Enter Base URL: `https://127.0.0.1:8080` (mock server)
    5. Enter any text in PAT field — verify it is masked, click eye icon to reveal/hide
    6. Click "Test Connection" — verify spinner appears, fields lock, then success message: "Connected as jdoe — Jira Server v8.20.0"
    7. Verify "Next" button appears after success
    8. Edit the URL field — verify "Next" disappears (re-test required)
    9. Re-test, then click "Next" to advance to Step 2
    10. Step 2 shows "Destination Connection" / "Company Jira Cloud"
    11. Enter Base URL: `https://127.0.0.1:8081`, Email: any, API Token: any
    12. Click "Test Connection" — verify success message with Cloud version
    13. Click "Next" to advance to Step 3 (Summary)
    14. Verify summary shows both connections with green checkmarks and "Done" button
    15. Click "Done" — wizard should disappear, main app (DevStatusPanel) should render
    16. Verify gear icon appears in header — click it to open Settings page
    17. Verify two connection cards with green status dots, base URLs, "Last tested: X ago"
    18. Try entering an http:// URL — verify inline error "URL must start with https://"
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm test -- src/features/connections` passes all tests
- `npm test` passes full suite (no regressions)
- `npx tsc --noEmit` passes
- Visual verification of complete wizard flow against mock servers
</verification>

<success_criteria>
- Gear icon in AppShell header navigates to Settings page
- Settings page shows both connection cards with correct status, URL, and "Last tested" info
- Edit button on connection card triggers re-entry to wizard
- All frontend component tests pass
- Complete end-to-end wizard flow verified visually against mock servers
</success_criteria>

<output>
After completion, create `.planning/phases/02-connection-setup/02-03-SUMMARY.md`
</output>
