---
phase: 27-add-static-value-mapping-to-configurable-field-mapping
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - src/features/field-mapping/types.ts
  - src/features/field-mapping/transformerOptions.ts
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: true
requirements:
  - STATIC-I18N-01

must_haves:
  truths:
    - "FieldMappingRow TypeScript interface mirrors the Rust struct's new staticValue field"
    - "TransformerKind union includes 'static'"
    - "All 11 new i18n keys exist in both en.json and sk.json with proper Slovak diacritics"
    - "Existing translations test still passes after the keys land"
  artifacts:
    - path: "src/features/field-mapping/types.ts"
      provides: "FieldMappingRow.staticValue?: string optional field"
      contains: "staticValue?: string"
    - path: "src/features/field-mapping/transformerOptions.ts"
      provides: "TransformerKind union extended with 'static' literal"
      contains: "'static'"
    - path: "src/i18n/locales/en.json"
      provides: "11 new settings.fieldMapping.static* and settings.transformer.static.* keys"
      contains: "settings.fieldMapping.addStaticRow"
    - path: "src/i18n/locales/sk.json"
      provides: "11 new settings.fieldMapping.static* and settings.transformer.static.* keys in Slovak"
      contains: "settings.fieldMapping.addStaticRow"
  key_links:
    - from: "src/features/field-mapping/types.ts"
      to: "src-tauri/src/field_transform/mod.rs FieldMappingRow struct"
      via: "Tauri serde camelCase boundary"
      pattern: "staticValue"
    - from: "src/i18n/locales/sk.json"
      to: "src/i18n/locales/en.json"
      via: "matching key set in both locale files"
      pattern: "settings.fieldMapping.staticBadge"
---

<objective>
Land the small TypeScript and i18n foundation that the UI (Plan 04) builds on top of: extend the `FieldMappingRow` TS interface with `staticValue?: string`, add the `'static'` literal to the `TransformerKind` union, and add all 11 new copy keys to both `en.json` and `sk.json` with proper Slovak translations per the UI-SPEC copywriting contract.

Purpose: Plan 04 imports `FieldMappingRow`, `TransformerKind`, and `t('settings.fieldMapping.*')` everywhere. Landing these foundation pieces in a small parallel plan keeps Plan 04's UI work focused on rendering and behavior, not type/translation plumbing.
Output: Type definitions and locale files updated; the existing translations test (`src/i18n/__tests__/translations.test.ts`) continues to pass with the expanded key set.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-CONTEXT.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md
@src/features/field-mapping/types.ts
@src/features/field-mapping/transformerOptions.ts
@src/i18n/locales/en.json
@src/i18n/locales/sk.json
@src/i18n/__tests__/translations.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/features/field-mapping/types.ts (lines 12–20):
- `export interface FieldMappingRow { sourceFieldId: string; targetFieldId: string; transformerKind: TransformerKind; sourceSchema: FieldSchemaType; targetSchema: FieldSchemaType; }`
- Add `staticValue?: string;` as the final optional field. The `?` matches the Rust `Option<String>` + `skip_serializing_if = "Option::is_none"` semantics from Plan 02.

From src/features/field-mapping/transformerOptions.ts (lines 3–10):
- `export type TransformerKind = 'identity' | 'user' | 'user_name' | 'version' | 'component' | 'wiki_to_adf' | 'priority';`
- Add `| 'static'` as the last union member. Do NOT add a new entry to `getTransformerOptions` — static rows do not render the transformer combobox in Plan 04; the function continues to be called only by `MappingRow`, not `StaticMappingRow`.

From src/i18n/locales/en.json (line 437–438, end of settings.fieldMapping block):
- Existing keys: `settings.fieldMapping.addRow`, `.saved`, `.saveError`, `.loadError`, `.deleteError`, `.refreshError`, `.emptyHeading`, `.emptyBody`, `.allMapped`, `.noIssueTypes`, `.driftRemove`, ... — flat dotted-key JSON (not nested)
- Add the 11 new keys (9 settings.fieldMapping.static* + 2 settings.transformer.static.*) immediately after the last existing settings.fieldMapping.* entry / settings.transformer.* entry respectively (around line 438)

From .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md (lines 219–235, i18n Key Contract):
- Authoritative list of EN values to land. SK translations to be provided in this plan per project convention (sk.json uses Slovak diacritics; see existing keys for style).

From src/i18n/__tests__/translations.test.ts:
- Existing translations test that diffs en.json and sk.json key sets — every new key in en.json must have a matching key in sk.json or the test fails.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TypeScript types — FieldMappingRow and TransformerKind</name>
  <files>src/features/field-mapping/types.ts, src/features/field-mapping/transformerOptions.ts</files>
  <read_first>
    - src/features/field-mapping/types.ts (full file)
    - src/features/field-mapping/transformerOptions.ts (full file — confirm union shape and getTransformerOptions signature)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md (types.ts and transformerOptions.ts sections)
  </read_first>
  <behavior>
    - `FieldMappingRow` constructed without `staticValue` continues to type-check (optional field)
    - `FieldMappingRow` constructed with `staticValue: 'hello'` type-checks
    - `TransformerKind` accepts the literal `'static'` and rejects unknown strings
    - `getTransformerOptions(...)` is unchanged in behavior — existing callers (MappingRow) are not affected
  </behavior>
  <action>
    In src/features/field-mapping/types.ts, add `staticValue?: string;` as the final optional field of the `FieldMappingRow` interface (after `targetSchema: FieldSchemaType;`). Include a docstring comment on the line above: `/** Phase 27 — present only when transformerKind === 'static'. Stored as a JSON-encoded string the pipeline parses with serde_json::from_str. For option fields the UI stores the pre-serialized JSON write-shape (e.g. JSON.stringify({ id: opt.id })) so the pipeline pass-through emits the correct {"id":"..."} object to Jira. */`.

    In src/features/field-mapping/transformerOptions.ts, append `| 'static'` to the `TransformerKind` union as the last member. The full union becomes `'identity' | 'user' | 'user_name' | 'version' | 'component' | 'wiki_to_adf' | 'priority' | 'static'`. Do NOT modify `getTransformerOptions` — static rows in Plan 04 use a dedicated `StaticValueWidget` instead of the transformer combobox. The transformer options array is consumed by `MappingRow` only.

    Run `npx tsc --noEmit` to confirm the codebase type-checks. There may be unused-import warnings; ignore them. There must be no type errors caused by the new `staticValue` field — every existing site that constructs a `FieldMappingRow` literal continues to satisfy the interface because the field is optional.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/field-mapping/types.ts contains `staticValue?: string`
    - src/features/field-mapping/transformerOptions.ts contains the literal `'static'` inside the TransformerKind union declaration
    - `npx tsc --noEmit` exits 0 with no type errors
    - `grep -c "'static'" src/features/field-mapping/transformerOptions.ts` is `>= 1`
    - The `getTransformerOptions` function body is unchanged — diff shows only the union extension
  </acceptance_criteria>
  <done>
    TypeScript foundation in place. Plan 04 can construct `FieldMappingRow { ..., staticValue }` and use `transformerKind: 'static'` without type errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 11 new i18n keys to en.json and sk.json</name>
  <files>src/i18n/locales/en.json, src/i18n/locales/sk.json</files>
  <read_first>
    - src/i18n/locales/en.json (settings.fieldMapping block — lines ~410–438)
    - src/i18n/locales/sk.json (matching block — find the same key set for SK style reference)
    - src/i18n/__tests__/translations.test.ts (to confirm the key-parity assertion this plan must satisfy)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md (i18n Key Contract — authoritative EN values)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md (en.json/sk.json sections)
  </read_first>
  <behavior>
    - Every new EN key has a matching SK key with a proper Slovak translation (diacritics preserved per project convention — see existing translations like "Pridať mapovanie polí" for `addRow`)
    - The translations test passes: `npx vitest run src/i18n/__tests__/translations.test.ts` exits 0
    - JSON is valid in both files: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8'))"` and same for sk.json exit 0
  </behavior>
  <action>
    Add exactly 11 new keys to BOTH src/i18n/locales/en.json and src/i18n/locales/sk.json. Both files use flat dotted-key JSON (not nested objects). Place the `settings.fieldMapping.*` keys immediately after the last existing `settings.fieldMapping.*` key in each file. Place the `settings.transformer.static.*` keys near the existing `settings.transformer.*` block. Preserve trailing comma rules — the previous last entry's terminating comma must be present before the first new key.

    The 11 keys and EN values (authoritative — from UI-SPEC.md i18n Key Contract):

    1. `settings.fieldMapping.addStaticRow` → "Add static value"
    2. `settings.fieldMapping.staticBadge` → "Static"
    3. `settings.fieldMapping.staticBadgeTooltip` → "This field is always set to a fixed value, regardless of the source ticket."
    4. `settings.fieldMapping.staticTargetPlaceholder` → "Pick a target field"
    5. `settings.fieldMapping.staticValuePlaceholder` → "Enter value"
    6. `settings.fieldMapping.staticOptionPlaceholder` → "Pick a value"
    7. `settings.fieldMapping.staticMultiHint` → "Separate values with commas"
    8. `settings.fieldMapping.staticUnsupported` → "Not supported in this phase"
    9. `settings.fieldMapping.deleteStaticAriaLabel` → "Remove static mapping for {{field}}"
    10. `settings.transformer.static.label` → "Static"
    11. `settings.transformer.static.description` → "Always writes a fixed value to this field"

    All 11 keys must be added to both en.json and sk.json. SK translations (Slovak with diacritics):

    1. `addStaticRow` → "Pridať statickú hodnotu"
    2. `staticBadge` → "Statická"
    3. `staticBadgeTooltip` → "Toto pole je vždy nastavené na pevnú hodnotu bez ohľadu na zdrojový tiket."
    4. `staticTargetPlaceholder` → "Vyberte cieľové pole"
    5. `staticValuePlaceholder` → "Zadajte hodnotu"
    6. `staticOptionPlaceholder` → "Vyberte hodnotu"
    7. `staticMultiHint` → "Hodnoty oddeľte čiarkami"
    8. `staticUnsupported` → "V tejto fáze nie je podporované"
    9. `deleteStaticAriaLabel` → "Odstrániť statické mapovanie pre {{field}}"
    10. `transformer.static.label` → "Statická"
    11. `transformer.static.description` → "Vždy zapíše pevnú hodnotu do tohto poľa"

    Place the `settings.transformer.static.*` keys near the existing `settings.transformer.*` block in both files (search for `settings.transformer.` to locate). Place the `settings.fieldMapping.static*` keys near the existing `settings.fieldMapping.*` block.

    After editing, parse both JSON files to confirm validity. Run the translations test.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8'));JSON.parse(require('fs').readFileSync('src/i18n/locales/sk.json','utf8'));console.log('json valid')" && npx vitest run src/i18n/__tests__/translations.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "settings.fieldMapping.addStaticRow" src/i18n/locales/en.json` is 1
    - `grep -c "settings.fieldMapping.addStaticRow" src/i18n/locales/sk.json` is 1
    - `grep -c "settings.fieldMapping.staticBadge" src/i18n/locales/en.json` is 1 (matches only one key — `staticBadge`, not `staticBadgeTooltip`; tighten via `grep -c "\"settings.fieldMapping.staticBadge\":" src/i18n/locales/en.json` returns 1)
    - All 11 new keys present in en.json: `for k in addStaticRow staticBadge staticBadgeTooltip staticTargetPlaceholder staticValuePlaceholder staticOptionPlaceholder staticMultiHint staticUnsupported deleteStaticAriaLabel; do grep -c "\"settings.fieldMapping.$k\":" src/i18n/locales/en.json; done` returns 1 nine times; same for `settings.transformer.static.label` and `settings.transformer.static.description`
    - Same 11 keys present in sk.json
    - SK values use Slovak diacritics (ľ, ý, é, á, etc.) — at least 8 of the 11 SK values contain a Unicode codepoint > 0x7F
    - `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en.json','utf8'))"` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/sk.json','utf8'))"` exits 0
    - `npx vitest run src/i18n/__tests__/translations.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Locale files updated with all 11 keys in both EN and SK. JSON validates and translations test passes. Plan 04 can call `t('settings.fieldMapping.addStaticRow')` etc. without "missing key" warnings in either locale.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend i18n strings | User-facing copy. Translations are static at build time — no runtime user input is reflected. |
| Tauri command serde boundary | The new `staticValue?: string` field flows TS → Rust via serde camelCase rename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | i18n key drift | mitigate | The existing translations test enforces key-parity between en.json and sk.json. Verify: `npx vitest run src/i18n/__tests__/translations.test.ts` passes after this plan. |
| T-27-03-02 | Information disclosure | n/a — static copy | accept | i18n strings are static; no PII or secret material exposed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0
- `npx vitest run src/i18n/__tests__/translations.test.ts` exits 0
- Both en.json and sk.json parse as valid JSON
- 11 new keys present in each file
</verification>

<success_criteria>
TypeScript types and i18n copy ready for Plan 04. No production behavior changes — pure foundation work.
</success_criteria>

<output>
Create `.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-03-SUMMARY.md` when done.
</output>
