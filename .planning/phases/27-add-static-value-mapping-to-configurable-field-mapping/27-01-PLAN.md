---
phase: 27-add-static-value-mapping-to-configurable-field-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/field_mapping_db.rs
  - src-tauri/src/field_transform/pipeline.rs
  - src/features/field-mapping/__tests__/StaticMappingRow.test.tsx
  - src/features/field-mapping/__tests__/StaticValueWidget.test.tsx
autonomous: true
requirements:
  - STATIC-DB-01
  - STATIC-DB-02
  - STATIC-PIPE-01
  - STATIC-PIPE-02
  - STATIC-UI-01
  - STATIC-UI-02
  - STATIC-UI-03
  - STATIC-UI-04

must_haves:
  truths:
    - "Failing Rust tests exist for static_value column round-trip in field_mapping_db"
    - "Failing Rust tests exist for apply_mapping `static` transformer branch in pipeline"
    - "Failing React tests exist for StaticMappingRow rendering, value change save, and delete"
    - "Failing React tests exist for StaticValueWidget per-schema-type dispatch"
    - "Every Wave 0 test file is registered with the test runner and runs (currently failing or `#[ignore]` with a clear pending stub)"
  artifacts:
    - path: "src-tauri/src/field_mapping_db.rs"
      provides: "New #[test] fns: upsert_mapping_row_round_trips_static_value, static_value_column_added_by_migration"
      contains: "fn upsert_mapping_row_round_trips_static_value"
    - path: "src-tauri/src/field_transform/pipeline.rs"
      provides: "New #[tokio::test] fns: apply_mapping_static_emits_stored_value, apply_mapping_static_with_none_value_emits_nothing"
      contains: "async fn apply_mapping_static_emits_stored_value"
    - path: "src/features/field-mapping/__tests__/StaticMappingRow.test.tsx"
      provides: "describe('StaticMappingRow') with tests for render, value change invoke, delete invoke"
      contains: "describe('StaticMappingRow'"
    - path: "src/features/field-mapping/__tests__/StaticValueWidget.test.tsx"
      provides: "describe('StaticValueWidget') with tests for per-schema-type widget selection"
      contains: "describe('StaticValueWidget'"
  key_links:
    - from: "src/features/field-mapping/__tests__/StaticMappingRow.test.tsx"
      to: "../StaticMappingRow"
      via: "ES module import"
      pattern: "from '../StaticMappingRow'"
    - from: "src/features/field-mapping/__tests__/StaticValueWidget.test.tsx"
      to: "../StaticValueWidget"
      via: "ES module import"
      pattern: "from '../StaticValueWidget'"
---

<objective>
Create failing test scaffolding for Phase 27 across all four implementation layers (Rust DB, Rust pipeline, React StaticMappingRow, React StaticValueWidget) before any production code is written. This enforces Nyquist sampling so every downstream plan has automated tests in place when implementation begins.

Purpose: Tests define the contract before the code; downstream plans go from RED to GREEN, not from "no signal" to "manually verified."
Output: Two new Rust `#[test]` / `#[tokio::test]` functions in existing files, and two new React test files with module-level mocks ready for the components being built in plan 27-04.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-CONTEXT.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-RESEARCH.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-VALIDATION.md
@src/features/field-mapping/__tests__/MappingRow.test.tsx
@src-tauri/src/field_mapping_db.rs
@src-tauri/src/field_transform/pipeline.rs

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/features/field-mapping/types.ts:
- `FieldMappingRow` interface — fields: sourceFieldId, targetFieldId, transformerKind, sourceSchema, targetSchema; Plan 03 adds `staticValue?: string`. Tests in this plan must use the property name `staticValue` (camelCase) so they will compile after Plan 03 lands and continue to fail until Plan 04 implements the components.

From src-tauri/src/field_transform/mod.rs:
- `pub struct FieldMappingRow { source_field_id, target_field_id, transformer_kind: String, source_schema: FieldSchemaType, target_schema: FieldSchemaType }` — Plan 02 adds `pub static_value: Option<String>`. Rust tests in this plan must reference `static_value: Some(...)` and will only compile after Plan 02 adds the field; until then they may sit alongside the test file but be guarded by `#[cfg(test)]` or simply fail to compile and be enabled by Plan 02. Use the `// TODO Plan 02` marker in any line that depends on `static_value`.

From src-tauri/src/field_mapping_db.rs:1303 helper `make_dismissed`:
- Test helper pattern for constructing `FieldMappingRow` rows in tests — copy this shape.

From src-tauri/src/field_transform/pipeline.rs:389 helper `row_with_kind` (line 389–403):
- Helper that builds a `FieldMappingRow` with a given `transformer_kind`. Use this helper, then mutate `static_value` on the returned struct for static tests.

From src/features/field-mapping/__tests__/MappingRow.test.tsx:
- Module-level mocks for `@tauri-apps/api/core`, `sonner`, and `VirtualizedCombobox` — copy verbatim into the new static test files.
- `renderWithI18n` helper at `../../../test-utils/renderWithI18n` (note `.tsx` extension on filename, but import without extension).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing Rust tests for DB static_value round-trip and pipeline static branch</name>
  <files>src-tauri/src/field_mapping_db.rs, src-tauri/src/field_transform/pipeline.rs</files>
  <read_first>
    - src-tauri/src/field_mapping_db.rs (lines 940–1020, the existing #[cfg(test)] mod tests block and the `upsert_mapping_row_replaces_existing` test on line 952 — copy its shape)
    - src-tauri/src/field_transform/pipeline.rs (lines 380–470, the existing helpers `row`, `row_with_kind`, `make_resolvers`, `ctx_with_map`, and the `apply_mapping_returns_resolved_fields_for_text_only_mapping` test on line 441)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-RESEARCH.md (Validation Architecture and Wave 0 Gaps sections — they list STATIC-DB-01, STATIC-DB-02, STATIC-PIPE-01, STATIC-PIPE-02)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md (`field_mapping_db.rs` test pattern at lines 122–141 of PATTERNS and `pipeline.rs` test pattern at lines 230–249)
  </read_first>
  <action>
    Add four new test functions across two Rust files. These tests intentionally reference fields and behaviors that do not yet exist; they will fail to compile or fail at runtime until Plans 02/03 land. Tag each test with a comment referencing its requirement ID.

    In src-tauri/src/field_mapping_db.rs, inside the existing `#[cfg(test)] mod tests` block (after the existing `upsert_mapping_row_replaces_existing` function around line 991), add:

    1. `#[test] fn static_value_column_added_by_migration()` — STATIC-DB-01. Open `FieldMappingDb::open_in_memory()`, query `PRAGMA table_info(field_mapping)`, collect column names into `Vec<String>`, assert the vec contains `"static_value"`. This test verifies the migration applied successfully. It will fail until Plan 02 adds the `migrate_static_value_column` call.

    2. `#[test] fn upsert_mapping_row_round_trips_static_value()` — STATIC-DB-02. Build a `FieldMappingRow` with `source_field_id: "__static__customfield_10050".into()`, `target_field_id: "customfield_10050".into()`, `transformer_kind: "static".into()`, `source_schema: FieldSchemaType::Any`, `target_schema: FieldSchemaType::Any`, and `static_value: Some("hello".into())`. Call `db.upsert_mapping_row(&row)`. Call `db.get_all_mapping_rows()`. Assert the returned vec contains exactly one row whose `static_value == Some("hello".into())`. Use the same `FieldMappingDb::open_in_memory()` setup as the existing tests.

    In src-tauri/src/field_transform/pipeline.rs, inside the existing `#[cfg(test)] mod tests` block (alongside the other `apply_mapping_*` tests), add:

    3. `#[tokio::test] async fn apply_mapping_static_emits_stored_value()` — STATIC-PIPE-01. Construct a source issue `json!({"fields": {}})` (empty — proves the static branch does not look up source). Construct a single-row mapping vec via `row_with_kind("__static__customfield_10050", "customfield_10050", FieldSchemaType::Any, FieldSchemaType::Any, "static")`, then set `.static_value = Some("\"10001\"".into())` on the returned row (the value is stored as serialized JSON — here just a JSON string literal). Build resolvers/ctx with the existing `make_resolvers()`/`ctx_with_map()` helpers. Call `apply_mapping(&issue, &mapping, &ctx).await.expect("ok")`. Assert `out.fields.get("customfield_10050") == Some(&json!("10001"))`. Assert `out.gaps.is_empty()`.

    4. `#[tokio::test] async fn apply_mapping_static_with_none_value_emits_nothing()` — STATIC-PIPE-02. Same construction as test 3 except leave `static_value: None`. Call `apply_mapping`. Assert `out.fields.get("customfield_10050").is_none()` and `out.gaps.is_empty()`. This proves a pending static row (target picked but value not yet entered) does not pollute the output.

    The `row_with_kind` helper currently returns a `FieldMappingRow` without a `static_value` field. Until Plan 02 adds the field to the struct, these two pipeline tests will not compile. That is acceptable — Plan 02 is responsible for the struct update. Mark each newly added test with a `// PHASE 27 — Wave 0; goes green when Plan 02 lands` comment.

    Do NOT modify any production code. Do NOT add `#[ignore]` to these tests — they must be runnable as soon as their dependencies (Plan 02) land.
  </action>
  <verify>
    <automated>cargo test -p pmkar-lib --no-run 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/field_mapping_db.rs contains a `fn static_value_column_added_by_migration` test annotated with `#[test]` and referencing PRAGMA table_info
    - src-tauri/src/field_mapping_db.rs contains a `fn upsert_mapping_row_round_trips_static_value` test using `static_value: Some("hello".into())`
    - src-tauri/src/field_transform/pipeline.rs contains an `async fn apply_mapping_static_emits_stored_value` test annotated with `#[tokio::test]`
    - src-tauri/src/field_transform/pipeline.rs contains an `async fn apply_mapping_static_with_none_value_emits_nothing` test annotated with `#[tokio::test]`
    - Each new test has a comment containing the string "PHASE 27" so a grep can locate the Wave 0 set
    - `grep -c "PHASE 27" src-tauri/src/field_mapping_db.rs src-tauri/src/field_transform/pipeline.rs` returns 4 (lines, not occurrences — use `grep -rn "PHASE 27" src-tauri/src/field_mapping_db.rs src-tauri/src/field_transform/pipeline.rs | wc -l` to confirm)
    - No existing test is removed or modified
  </acceptance_criteria>
  <done>
    Four new Rust tests committed, two per file. Compilation may fail until Plan 02 adds `static_value` to `FieldMappingRow` — that is expected and recorded in the task summary.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add failing React tests for StaticMappingRow and StaticValueWidget</name>
  <files>src/features/field-mapping/__tests__/StaticMappingRow.test.tsx, src/features/field-mapping/__tests__/StaticValueWidget.test.tsx</files>
  <read_first>
    - src/features/field-mapping/__tests__/MappingRow.test.tsx (full file — copy module-level mocks verbatim)
    - src/features/field-mapping/MappingRow.tsx (to confirm prop names: row, targetFields, usedTargetFieldIds, onRowUpdate, onRowDelete)
    - src/test-utils/renderWithI18n.tsx (to confirm the renderer signature)
    - src/types/fieldSchema.ts (for `FieldSchema` and `FieldSchemaType` shapes used in test fixtures)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md (Smart Value Widget table for per-schema-type dispatch tests)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md (StaticMappingRow.test.tsx analog section)
  </read_first>
  <action>
    Create two new React test files. Both import from components that do not yet exist (`../StaticMappingRow`, `../StaticValueWidget`) — the imports will fail until Plan 04 creates those files. That is expected.

    File 1: src/features/field-mapping/__tests__/StaticMappingRow.test.tsx

    Module-level setup (copy verbatim from MappingRow.test.tsx lines 1–49):
    - Import `fireEvent`, `screen`, `waitFor` from `@testing-library/react`
    - Import `beforeEach`, `describe`, `expect`, `it`, `vi` from `vitest`
    - Import `renderWithI18n` from `../../../test-utils/renderWithI18n`
    - Import `FieldSchema` type from `../../../types/fieldSchema`
    - Import `FieldMappingRow` type from `../types`
    - Import `StaticMappingRow` from `../StaticMappingRow` (does not exist yet — Plan 04 creates it)
    - `vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn() }))`
    - `vi.mock('sonner', () => ({ toast: { error: vi.fn(), success: vi.fn() } }))`
    - `vi.mock('@/features/field-renderers/components/VirtualizedCombobox', ...)` — copy the same `<select>` fallback mock used by MappingRow.test.tsx

    Test fixtures:
    - `baseStaticRow: FieldMappingRow` with `sourceFieldId: '__static__customfield_10050'`, `targetFieldId: 'customfield_10050'`, `transformerKind: 'static'`, `sourceSchema: { type: 'any' }`, `targetSchema: { type: 'option' }`, `staticValue: '10001'`
    - `targetFields: FieldSchema[]` with at least one entry matching `customfield_10050` plus an option-type schema with `allowedValues: [{ id: '10001', value: 'Blocker' }, { id: '10002', value: 'High' }]`
    - `usedTargetFieldIds: new Set<string>(['customfield_10050'])`

    `beforeEach`: `vi.clearAllMocks()`; pull the invoke mock via `vi.mocked(invoke).mockResolvedValue(undefined)`.

    `describe('StaticMappingRow', () => { ... })` with three tests:

    1. STATIC-UI-01 `it('renders the Static badge and target combobox')` — render `<StaticMappingRow row={baseStaticRow} targetFields={targetFields} usedTargetFieldIds={usedTargetFieldIds} onRowUpdate={vi.fn()} onRowDelete={vi.fn()} />`. Assert `screen.getByText('Static')` is visible (the badge from i18n key `settings.fieldMapping.staticBadge`). Assert a combobox is rendered (use the `<select aria-label>` mock — query by `aria-label` containing the target field name or sentinel id).

    2. STATIC-UI-03 `it('persists value changes via set_field_mapping')` — render with the baseStaticRow. Find the value input by `aria-label` (matches the smart widget's `ariaLabel` prop derived from target field name, or by role/placeholder). `fireEvent.change(input, { target: { value: '10002' } })` and `fireEvent.blur(input)`. `await waitFor(() => expect(vi.mocked(invoke)).toHaveBeenCalledWith('set_field_mapping', { row: expect.objectContaining({ staticValue: '10002', transformerKind: 'static' }) }))`.

    3. STATIC-UI-04 `it('deletes via delete_field_mapping with sentinel sourceFieldId')` — render with baseStaticRow. Click the delete button (find by `aria-label` matching `settings.fieldMapping.deleteStaticAriaLabel`). `await waitFor(() => expect(vi.mocked(invoke)).toHaveBeenCalledWith('delete_field_mapping', { sourceFieldId: '__static__customfield_10050' }))`. Also assert `onRowDelete` was called with `'__static__customfield_10050'`.

    File 2: src/features/field-mapping/__tests__/StaticValueWidget.test.tsx

    Same module-level setup as File 1 (mocks for `@tauri-apps/api/core` and `VirtualizedCombobox`). Import `StaticValueWidget` from `../StaticValueWidget`.

    `describe('StaticValueWidget', () => { ... })` with four tests covering STATIC-UI-02:

    1. `it('renders a VirtualizedCombobox for option-type fields')` — pass `field` with `schema: { type: 'option' }` and `allowedValues: [{ id: '10001', value: 'Blocker' }]`. Assert `screen.getByRole('combobox')` or the mocked `<select>` is in the document.

    2. `it('renders a text input for string-type fields')` — pass `field` with `schema: { type: 'string' }`. Assert `screen.getByPlaceholderText` matches the EN string for `settings.fieldMapping.staticValuePlaceholder` ("Enter value"), and the element is an `<input type="text">`.

    3. `it('renders a comma-separated text input with hint for array-of-string fields')` — pass `field` with `schema: { type: 'array', items: 'string' }`. Assert an `<input>` is rendered and `screen.getByText` matches the EN string for `settings.fieldMapping.staticMultiHint` ("Separate values with commas").

    4. `it('renders a disabled input for unsupported user-type fields')` — pass `field` with `schema: { type: 'user' }`. Assert the rendered `<input>` has the `disabled` attribute and its placeholder matches `settings.fieldMapping.staticUnsupported` ("Not supported in this phase").

    Use `onChange={vi.fn()}` and `value={''}` (or appropriate sample) in every render call.

    Mark every newly added test file with a top-of-file comment containing "PHASE 27 — Wave 0; components implemented in Plan 04".
  </action>
  <verify>
    <automated>npx vitest run src/features/field-mapping/__tests__/StaticMappingRow.test.tsx src/features/field-mapping/__tests__/StaticValueWidget.test.tsx 2>&1 | tail -30 || true</automated>
  </verify>
  <acceptance_criteria>
    - File src/features/field-mapping/__tests__/StaticMappingRow.test.tsx exists
    - File src/features/field-mapping/__tests__/StaticValueWidget.test.tsx exists
    - StaticMappingRow.test.tsx contains `describe('StaticMappingRow'` literal
    - StaticValueWidget.test.tsx contains `describe('StaticValueWidget'` literal
    - StaticMappingRow.test.tsx contains the literal string `'set_field_mapping'` (asserts invoke args)
    - StaticMappingRow.test.tsx contains the literal string `'__static__customfield_10050'` (sentinel id assertion)
    - StaticValueWidget.test.tsx contains at least four `it(` test declarations: `grep -c "  it(" src/features/field-mapping/__tests__/StaticValueWidget.test.tsx` is `>=4`
    - Both files contain a top-of-file comment "PHASE 27 — Wave 0"
    - Running `npx vitest run src/features/field-mapping/__tests__/StaticMappingRow.test.tsx` exits non-zero (import resolution failure on `../StaticMappingRow` is expected and proves the test is actually wired up); same for StaticValueWidget.test.tsx
  </acceptance_criteria>
  <done>
    Two new test files committed. Both fail at import-resolution stage because `../StaticMappingRow` and `../StaticValueWidget` do not exist yet. Plan 04 creates those components and turns these tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-only code | New Wave 0 tests do not touch trust boundaries — they only construct in-memory DBs and mock the Tauri invoke surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | Wave 0 tests | accept | Tests are scaffolding only. Any malicious modification by a future plan would be caught by the round-trip assertion in `upsert_mapping_row_round_trips_static_value`. |
</threat_model>

<verification>
- `grep -rn "PHASE 27" src-tauri/src/field_mapping_db.rs src-tauri/src/field_transform/pipeline.rs | wc -l` returns 4
- `ls src/features/field-mapping/__tests__/StaticMappingRow.test.tsx src/features/field-mapping/__tests__/StaticValueWidget.test.tsx` lists both files
- `cargo test -p pmkar-lib --no-run` either compiles (after Plan 02 lands) or surfaces the expected `no field static_value on type FieldMappingRow` error pinpointing Plan 02's dependency
- `npx vitest run src/features/field-mapping/__tests__/StaticMappingRow.test.tsx` reports "Failed to resolve import '../StaticMappingRow'" — proves the test is in the runner's discovery scope
</verification>

<success_criteria>
Wave 0 tests committed and indexed by the test runner. Plans 02–04 can now flip them from RED to GREEN as each layer lands. No production code changed in this plan.
</success_criteria>

<output>
Create `.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-01-SUMMARY.md` when done.
</output>
