---
phase: 27-add-static-value-mapping-to-configurable-field-mapping
plan: 04
type: execute
wave: 3
depends_on:
  - 27-01
  - 27-02
  - 27-03
files_modified:
  - src/features/field-mapping/StaticMappingRow.tsx
  - src/features/field-mapping/StaticValueWidget.tsx
  - src/features/field-mapping/FieldMappingSection.tsx
autonomous: false
requirements:
  - STATIC-UI-01
  - STATIC-UI-02
  - STATIC-UI-03
  - STATIC-UI-04

must_haves:
  truths:
    - "D-04: Clicking 'Add static value' in Settings → Copying → Field Mapping inserts a new pending static row with a Static badge and an empty target combobox"
    - "D-02: Selecting a target field in a pending static row persists a row with sourceFieldId '__static__{targetFieldId}', transformerKind 'static', and the appropriate target schema"
    - "D-03: The source field column shows a 'Static' badge instead of a source field name for static rows"
    - "D-05: The value widget (col 3) is hidden until a target field is selected — target combobox first, then value widget appears inline"
    - "D-06: The smart value widget in col 3 adapts to the target field schema: text input for string/number/date/datetime, VirtualizedCombobox for option/option-with-child (single-select), comma-separated text input for arrays (both array-of-option and array-of-string), disabled input for user/priority"
    - "D-07: The value widget renders inline in the static row as a 4th column. No separate panel or popover."
    - "D-01: Static rows have no source field — sourceFieldId is a sentinel; no source_issue.pointer() call occurs for static transformer_kind"
    - "For single-select option / option-with-child target schemas, the StaticValueWidget stores the pre-serialized JSON write-shape (JSON.stringify({ id: opt.id })) — the pipeline pass-through emits the correct {\"id\":\"...\"} payload to Jira Cloud v3"
    - "For array target schemas (array-of-option and array-of-string), the StaticValueWidget stores the RAW comma-separated text exactly as the user typed it. The Rust pipeline (Plan 27-02 Task 3) splits + trims and produces the correct JSON array shape per target schema. The UI must NOT pre-serialize array values."
    - "Changing the value persists via invoke('set_field_mapping') and flashes a 'Saved' indicator for 1500ms (same pattern as MappingRow)"
    - "Clicking delete on a static row calls invoke('delete_field_mapping', { sourceFieldId: '__static__...' }) and removes the row from the list"
    - "All four Wave 0 React tests (STATIC-UI-01..04) pass"
  artifacts:
    - path: "src/features/field-mapping/StaticMappingRow.tsx"
      provides: "React component for static field-mapping rows — Static badge, target combobox, value widget, delete button"
      contains: "export function StaticMappingRow"
    - path: "src/features/field-mapping/StaticValueWidget.tsx"
      provides: "Smart value widget that dispatches by FieldSchemaType to the correct input control; single-option stores pre-serialized JSON write-shape, arrays store raw comma-separated text"
      contains: "export function StaticValueWidget"
    - path: "src/features/field-mapping/FieldMappingSection.tsx"
      provides: "Add static value button + static-row discrimination in the row map + pending static row state"
      contains: "addStaticRow"
  key_links:
    - from: "src/features/field-mapping/FieldMappingSection.tsx"
      to: "src/features/field-mapping/StaticMappingRow.tsx"
      via: "Component import and conditional render when row.sourceFieldId.startsWith('__static__')"
      pattern: "StaticMappingRow"
    - from: "src/features/field-mapping/StaticMappingRow.tsx"
      to: "src/features/field-mapping/StaticValueWidget.tsx"
      via: "Component import and inline render after target is selected"
      pattern: "StaticValueWidget"
    - from: "src/features/field-mapping/StaticMappingRow.tsx"
      to: "Tauri invoke('set_field_mapping', { row })"
      via: "Auto-save on target/value change"
      pattern: "invoke\\('set_field_mapping'"
    - from: "src/features/field-mapping/StaticMappingRow.tsx"
      to: "Tauri invoke('delete_field_mapping', { sourceFieldId })"
      via: "Delete button handler"
      pattern: "invoke\\('delete_field_mapping'"
---

<objective>
Ship the Phase 27 UI: a new `StaticMappingRow` component, a `StaticValueWidget` that adapts to the target field schema, and the FieldMappingSection wiring (`Add static value` button + static-row discrimination in the row map + pending static row state). This is the final layer that wires the backend from Plan 02 to a usable Settings UI.

Purpose: Turn the four Wave 0 React tests (STATIC-UI-01..04) from RED to GREEN and deliver the user-visible feature described in CONTEXT.md decisions D-01 through D-10 (including D-06 amended: array target schemas use comma-separated text storage; the pipeline splits). Includes a checkpoint at the end so the user can verify the smart widget per the UI-SPEC dispatch table — that scenario requires live Tauri IPC + field_schema_cache data and cannot be fully automated (per 27-VALIDATION.md Manual-Only Verifications).
Output: Two new React components, one modified orchestrator file, and a green test suite. The user can open Settings → Copying → Field Mapping → click "Add static value" → pick a target → type/select a value → see it saved.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-CONTEXT.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-RESEARCH.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md
@.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md
@src/features/field-mapping/MappingRow.tsx
@src/features/field-mapping/FieldMappingSection.tsx
@src/features/field-mapping/transformerOptions.ts
@src/features/field-mapping/types.ts
@src/types/fieldSchema.ts
@src/features/field-mapping/__tests__/StaticMappingRow.test.tsx
@src/features/field-mapping/__tests__/StaticValueWidget.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/features/field-mapping/types.ts (after Plan 03):
- `FieldMappingRow { sourceFieldId: string; targetFieldId: string; transformerKind: TransformerKind; sourceSchema: FieldSchemaType; targetSchema: FieldSchemaType; staticValue?: string; }`

From src/features/field-mapping/transformerOptions.ts (after Plan 03):
- `TransformerKind` union includes the `'static'` literal

From src/types/fieldSchema.ts:
- `FieldSchemaType` is a discriminated union with variants: `{ type: 'string' }`, `{ type: 'number' }`, `{ type: 'date' }`, `{ type: 'datetime' }`, `{ type: 'option' }`, `{ type: 'option-with-child' }`, `{ type: 'array', items: string }` (e.g. items: 'option' | 'string' | 'user' | 'version' | 'component'), `{ type: 'user' }`, `{ type: 'priority' }`, `{ type: 'any' }`, `{ type: 'unknown' }`
- `FieldSchema { fieldId: string; name: string; required?: boolean; schema: FieldSchemaType; allowedValues?: unknown[] }` — `allowedValues` typically contains objects with `{ id: string; value: string }` for option fields

From src/features/field-mapping/MappingRow.tsx (lines 1–176):
- Auto-save pattern with 1500ms feedback flash (line ~33 useState, lines 64–65 setFeedback/setTimeout)
- `availableTargetFields = targetFields.filter((f) => !usedTargetFieldIds.has(f.fieldId) || f.fieldId === row.targetFieldId)` (lines 43–45)
- VirtualizedCombobox usage for target field combobox (lines 118–133)
- Grid layout: `grid grid-cols-[35fr_35fr_20fr_10fr] gap-3 items-center min-h-[40px] py-2 border-b border-brand-border last:border-0`
- Delete button: `h-9 w-9 rounded hover:bg-brand-surface-hover text-brand-muted hover:text-destructive` + `<X className="h-3.5 w-3.5" />`
- Saved indicator: `<Check className="h-3.5 w-3.5" />` + `{t('settings.fieldMapping.saved')}` with `aria-live="polite"`

From src/features/field-mapping/FieldMappingSection.tsx:
- Line 182: `const [pendingAdd, setPendingAdd] = useState(false);`
- Lines 220–234: `driftedSourceFieldIds` memo — used only for MappingRow rendering; static rows skip this
- Lines 235–239: `usedTargetFieldIds` memo — `new Set(mappingRows.map((r) => r.targetFieldId).filter((id) => id !== ''))` — must remain unchanged and is passed to BOTH MappingRow and StaticMappingRow
- Lines 242–244: `handleAddRow()` sets `setPendingAdd(true)` — model the new `handleAddStaticRow` on this
- Lines 316–328: `mappingRows.map((row) => <MappingRow ... />)` — extend with a static-row branch
- Lines 331–363: `pendingAdd && (...)` block — model the new `pendingStaticAdd` block on this
- Lines 367–378: `Add field mapping` Button — model the new `Add static value` Button on this; place immediately after the existing Button

From the VirtualizedCombobox component:
- Props: `items, value, onChange, displayLabel, filterFn, placeholder, ariaLabel, align?, itemHeight?, renderItem?`
- Single-select only (per UI-SPEC note in Smart Value Widget section)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create StaticValueWidget — smart per-schema-type value input</name>
  <files>src/features/field-mapping/StaticValueWidget.tsx</files>
  <read_first>
    - src/features/field-mapping/MappingRow.tsx (lines 118–155 — VirtualizedCombobox usage pattern; lines 137–155 transformer combobox column)
    - src/features/field-mapping/__tests__/StaticValueWidget.test.tsx (Wave 0 test file from Plan 01 — defines the contract this widget must satisfy)
    - src/types/fieldSchema.ts (FieldSchemaType discriminated union variants)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md (Smart Value Widget table — authoritative dispatch contract)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-CONTEXT.md (D-06 amended — array target schemas use comma-separated text storage; the pipeline splits)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-RESEARCH.md (Pattern 2 + Open Questions (RESOLVED) — split storage→write-shape contract: single-option uses pre-serialized JSON pass-through; arrays use comma-separated text with pipeline splitting)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md (StaticValueWidget.tsx section — sketch and dispatch table)
  </read_first>
  <behavior>
    - schema.type === 'option' | 'option-with-child' (SINGLE-select) → renders VirtualizedCombobox over `field.allowedValues` (items shaped like `{ id, value }`); displayLabel reads `.value`; onChange of an item calls `onChange(JSON.stringify({ id: item.id }))` — the stored staticValue is the pre-serialized JSON write-shape `{"id":"..."}` that the pipeline pass-through emits verbatim to Jira Cloud v3.
    - schema.type === 'array' (items: 'option', 'string', or other non-user) → renders text input. The stored value is the RAW comma-separated string exactly as the user typed it (e.g. `"10001, 10002"` for array-of-option, `"bug, regression"` for array-of-string Labels). The Rust pipeline (Plan 27-02 Task 3) is responsible for splitting on `,`, trimming each part, dropping empty parts, and constructing the correct JSON array shape per target schema. The UI MUST NOT call `JSON.stringify(...)` or `split(...)` on array values. The placeholder differentiates array-of-option (e.g. "Enter option IDs, comma-separated") from array-of-string (e.g. "Enter values, comma-separated"). A visible hint below the input reads `t('settings.fieldMapping.staticMultiHint')` ("Separate values with commas").
    - schema.type === 'user' | 'priority' OR schema.type === 'array' with items === 'user' → renders a disabled `<input>` with placeholder `t('settings.fieldMapping.staticUnsupported')`.
    - schema.type === 'string' | 'number' | 'date' | 'datetime' | 'any' | 'unknown' → renders a text `<input>` with placeholder `t('settings.fieldMapping.staticValuePlaceholder')` ("Enter value") that calls `onChange` on `onChange` event. Stored value is the raw text the user typed; the pipeline's `serde_json::from_str` fallback emits it as `Value::String` to Jira (valid for text/number/date scalars).
    - Every input has an aria-label derived from `field.name` (e.g. `${field.name} static value`)
    - The Wave 0 test file `StaticValueWidget.test.tsx` passes
  </behavior>
  <action>
    Create src/features/field-mapping/StaticValueWidget.tsx. Export a single named export `StaticValueWidget`.

    Props interface:
    ```
    export interface StaticValueWidgetProps {
      field: FieldSchema;
      value: string | undefined;
      onChange: (newValue: string) => void;
    }
    ```

    Implementation outline (no fenced code in the action — narrative only):

    1. Read `field.schema` once into a local `schema` constant; read `t` from `useTranslation()`.
    2. Branch on `schema.type` in this order (FIRST MATCH wins):
       a. `'option'` or `'option-with-child'` (single-select) → render `VirtualizedCombobox<{ id: string; value: string }>` with `items = (field.allowedValues ?? []) as { id: string; value: string }[]`. Set `value` to the item whose `id === value` (or `null` if `value` is undefined). `displayLabel={(o) => o.value}`. `filterFn={(o, q) => o.value.toLowerCase().includes(q.toLowerCase())}`. `placeholder={t('settings.fieldMapping.staticOptionPlaceholder')}`. `ariaLabel={`${field.name} static value`}`. **onChange handler:** `onChange={(opt) => onChange(JSON.stringify({ id: opt.id }))}` — the stored value MUST be the pre-serialized JSON write-shape (e.g. `{"id":"10001"}`), NOT the bare id string `"10001"`. RESEARCH.md ## Open Questions (RESOLVED) is authoritative for single-option: the pipeline parses this JSON string and passes through. **Reading the existing value back:** when `value` is a JSON string like `{"id":"10001"}`, parse it via `JSON.parse(value)` (in a try/catch) and use the parsed `.id` to find the matching item in `field.allowedValues`. If parsing fails (legacy data) or `value` is `undefined`, fall back to `null`. Wrap the combobox in `<div className="[&_button]:min-h-9">` to match MappingRow's height-normalization wrapper.
       b. `'array'` → check `schema.items` to choose the placeholder, but ALWAYS render a controlled `<input type="text">` styled `min-h-9 w-full rounded-md border border-input bg-background px-3 py-1 text-sm` with `value={value ?? ''}`, `onChange={(e) => onChange(e.target.value)}`, `aria-label={`${field.name} static value`}`. Placeholder selection: when `schema.items === 'option'` use `t('settings.fieldMapping.staticOptionArrayPlaceholder')` (e.g. "Enter option IDs, comma-separated"); otherwise (including `'string'`) use `t('settings.fieldMapping.staticValuePlaceholder')`. Render the hint immediately below the input: `<span className="text-xs text-brand-muted block mt-1">{t('settings.fieldMapping.staticMultiHint')}</span>`. Additionally, if `schema.items === 'user'`, render the disabled-input branch (d) instead — user arrays are unsupported. **STORAGE FORMAT — CRITICAL:** the widget passes the RAW input string straight to `onChange(e.target.value)`. Do NOT call `JSON.stringify(...)`, do NOT `.split(',')`, do NOT wrap in `{id: ...}`. The pipeline (Plan 27-02 Task 3) reads `row.target_schema` and produces `[{"id":"val1"},...]` for array-of-option and `["val1",...]` for array-of-string. The UI's only responsibility is to capture and persist the comma-separated text exactly as the user entered it.
       c. `'user'` or `'priority'` → render disabled `<input>` with `placeholder={t('settings.fieldMapping.staticUnsupported')}` and `disabled` attribute, same border styling.
       d. default (string, number, date, datetime, any, unknown) → render a controlled `<input type="text">` with the same styling as (b) but no hint below; placeholder `t('settings.fieldMapping.staticValuePlaceholder')`.

    3. The widget does NOT call invoke directly — it only calls `onChange(string)`. The parent StaticMappingRow owns the persistence call. The string the widget emits is ALREADY the storage form:
       - Pre-serialized JSON (`{"id":"..."}`) for single-select option / option-with-child
       - Raw comma-separated text for array types (pipeline does the splitting)
       - Raw text for everything else (string/number/date/datetime/any)

    4. Imports:
       - `useTranslation` from `react-i18next`
       - `VirtualizedCombobox` from `@/features/field-renderers/components/VirtualizedCombobox`
       - `FieldSchema` type from `@/types/fieldSchema`

    5. Add a one-line file comment: `// Phase 27 — smart static value widget. Dispatch table in 27-UI-SPEC.md. Single-option stores pre-serialized JSON write-shape (pipeline passes through); array types store raw comma-separated text (pipeline splits per 27-CONTEXT.md D-06).`

    After implementation, run the StaticValueWidget tests. All `it(` cases from Plan 01 must pass — including any test that asserts the option-branch onChange emits the JSON write-shape rather than a bare id, AND any test that asserts the array-branch onChange emits the raw input string (NOT a pre-serialized JSON array). If the Wave 0 test file does not yet cover the array branch's raw-storage contract, treat the raw-storage assertion as part of acceptance_criteria below.
  </action>
  <verify>
    <automated>npx vitest run src/features/field-mapping/__tests__/StaticValueWidget.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/features/field-mapping/StaticValueWidget.tsx exists
    - File contains `export function StaticValueWidget`
    - File contains the literal `'option-with-child'`
    - File contains the literal `'array'`
    - File contains `t('settings.fieldMapping.staticValuePlaceholder')` AND `t('settings.fieldMapping.staticMultiHint')` AND `t('settings.fieldMapping.staticUnsupported')` AND `t('settings.fieldMapping.staticOptionPlaceholder')`
    - **Single-option write-shape contract:** The single-select `'option'` / `'option-with-child'` branch's onChange handler stores `JSON.stringify({ id: opt.id })`. Verify with `grep -E "JSON\.stringify\(\s*\{\s*id\s*:\s*opt\.id" src/features/field-mapping/StaticValueWidget.tsx` returns >= 1 match.
    - **Single-option negative gate (no bare-id storage):** `grep -nE "onChange\(\s*opt\.id\s*\)" src/features/field-mapping/StaticValueWidget.tsx | grep -v '^#' | wc -l` returns 0 — there must be NO call site in the single-option branch that stores `opt.id` as a bare string.
    - **Array branch raw-storage contract (BLOCKER fix from D-06 amendment):** The `'array'` branch stores the raw input string exactly as typed. Verify with `grep -E "onChange\(e\.target\.value\)" src/features/field-mapping/StaticValueWidget.tsx | grep -v '^#' | wc -l` returns >= 1 (the array branch input passes `e.target.value` straight to `onChange`).
    - **Array branch negative gate (no UI-side splitting or serialization):** Inspect the file to confirm the array branch does NOT contain `.split(',')` and does NOT pass `JSON.stringify(...)` for array values. Verify with: `awk '/case .array./,/break|return|}$/' src/features/field-mapping/StaticValueWidget.tsx | grep -E "split\(',\'\)|JSON\.stringify" | grep -v '^#' | wc -l` returns 0. (If the file uses `if` cascades instead of switch, hand-inspect the array branch.) The pipeline (Plan 27-02 Task 3) owns the split + serialization.
    - When `value` is a JSON-encoded write-shape like `{"id":"10001"}`, the single-option combobox renders with the matching item pre-selected (parse with try/catch; legacy bare-id values fall back to `null` selection without throwing).
    - When `value` is a comma-separated string like `"10001, 10002"` for an array-of-option target, the text input renders with that exact string in its `value` prop (no JSON parsing attempt).
    - `npx vitest run src/features/field-mapping/__tests__/StaticValueWidget.test.tsx` exits 0 with all tests passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    StaticValueWidget shipped; STATIC-UI-02 green. Single-option widgets store pre-serialized Jira write-shape (`{"id":"..."}`). Array widgets store raw comma-separated text — the Rust pipeline (Plan 27-02 Task 3) performs the split + JSON array construction. End-to-end the Jira Cloud v3 payloads are valid for both target shapes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create StaticMappingRow — row component with auto-save and delete</name>
  <files>src/features/field-mapping/StaticMappingRow.tsx</files>
  <read_first>
    - src/features/field-mapping/MappingRow.tsx (lines 1–176 — entire file; copy auto-save, delete, grid layout, saved indicator patterns)
    - src/features/field-mapping/__tests__/StaticMappingRow.test.tsx (Wave 0 test file — defines the prop contract and persistence behavior)
    - src/features/field-mapping/StaticValueWidget.tsx (from Task 1 — confirm the import path and prop shape)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md (Static Row Visual Structure and Interaction States sections)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md (StaticMappingRow.tsx section — props, imports, auto-save and delete patterns)
  </read_first>
  <behavior>
    - Renders a 4-column grid row matching the existing MappingRow layout (`grid-cols-[35fr_35fr_20fr_10fr]`)
    - Col 1: Static badge `<span>` styled per UI-SPEC (`inline-flex items-center px-1.5 py-0.5 rounded text-[11px] font-semibold text-brand bg-brand/10 border border-brand/30`) with text from `t('settings.fieldMapping.staticBadge')`, `aria-label="Static mapping — no source field"`. Wrap in Tooltip showing `t('settings.fieldMapping.staticBadgeTooltip')`.
    - Col 2: VirtualizedCombobox<FieldSchema> for target field. Items are `availableTargetFields = targetFields.filter((f) => !usedTargetFieldIds.has(f.fieldId) || f.fieldId === row.targetFieldId)`. Value is the matching FieldSchema or null. On change calls handleTargetChange.
    - Col 3: When `row.targetFieldId !== ''` (i.e. a target is selected), render `<StaticValueWidget field={targetField} value={row.staticValue} onChange={handleValueChange} />`. When the target is not selected (pending row), render `<span className="text-brand-muted">—</span>`.
    - Col 4: Saved indicator (Check icon + "Saved" text, aria-live polite, flashes for 1500ms) + delete button (X icon, h-9 w-9 button) with `aria-label={t('settings.fieldMapping.deleteStaticAriaLabel', { field: targetField?.name ?? row.targetFieldId })}`.
    - handleTargetChange: builds `sentinelId = '__static__' + newTarget.fieldId`, constructs `FieldMappingRow { sourceFieldId: sentinelId, targetFieldId: newTarget.fieldId, transformerKind: 'static', sourceSchema: { type: 'any' }, targetSchema: newTarget.schema, staticValue: row.staticValue }`. Calls `await invoke('set_field_mapping', { row: updated })` then `onRowUpdate(updated)`. No saved flash on target change (matches MappingRow's behavior for target change).
    - handleValueChange: spreads `...row`, sets `staticValue: newValue` (where `newValue` is already the storage form — pre-serialized JSON for single-option, raw comma-separated text for arrays, raw text otherwise — produced by StaticValueWidget). Calls `await invoke('set_field_mapping', { row: updated })` then `onRowUpdate(updated)` then `setFeedback('saved')` and `setTimeout(() => setFeedback(null), 1500)`.
    - handleDelete: calls `await invoke('delete_field_mapping', { sourceFieldId: row.sourceFieldId })` then `onRowDelete(row.sourceFieldId)`.
    - All three handlers wrap in try/catch and call `toast.error(t('settings.fieldMapping.saveError'))` (handleTargetChange + handleValueChange) or `toast.error(t('settings.fieldMapping.deleteError'))` (handleDelete) on failure.
    - All three Wave 0 tests for StaticMappingRow pass.
  </behavior>
  <action>
    Create src/features/field-mapping/StaticMappingRow.tsx. Export `StaticMappingRow` and `StaticMappingRowProps`.

    Props interface mirrors MappingRowProps minus `isDrifted` and `sourceName`:
    - `row: FieldMappingRow`
    - `targetFields: FieldSchema[]`
    - `usedTargetFieldIds: Set<string>`
    - `onRowUpdate: (row: FieldMappingRow) => void`
    - `onRowDelete: (sourceFieldId: string) => void`

    Imports (per PATTERNS.md analog section):
    - `invoke` from `@tauri-apps/api/core`
    - `Check, X` from `lucide-react`
    - `useState` from `react`
    - `useTranslation` from `react-i18next`
    - `toast` from `sonner`
    - `VirtualizedCombobox` from `@/features/field-renderers/components/VirtualizedCombobox`
    - `Tooltip, TooltipContent, TooltipProvider, TooltipTrigger` from `@/components/ui/tooltip` (existing shadcn components)
    - `FieldSchema` type from `@/types/fieldSchema`
    - `FieldMappingRow` type from `./types`
    - `StaticValueWidget` from `./StaticValueWidget`

    Component body:
    1. `const { t } = useTranslation();`
    2. `const [feedback, setFeedback] = useState<'saved' | null>(null);`
    3. `const targetField = targetFields.find((f) => f.fieldId === row.targetFieldId) ?? null;`
    4. `const availableTargetFields = targetFields.filter((f) => !usedTargetFieldIds.has(f.fieldId) || f.fieldId === row.targetFieldId);`
    5. Define `handleTargetChange`, `handleValueChange`, `handleDelete` per the behavior section.
    6. Return JSX:
       - Root: `<div className="grid grid-cols-[35fr_35fr_20fr_10fr] gap-3 items-center min-h-[40px] py-2 border-b border-brand-border last:border-0">`
       - Col 1: Static badge wrapped in `TooltipProvider` → `Tooltip` → `TooltipTrigger asChild` → `<span>...badge styling...</span>` and `<TooltipContent>{t('settings.fieldMapping.staticBadgeTooltip')}</TooltipContent>`.
       - Col 2: `<div className="[&_button]:min-h-9"><VirtualizedCombobox<FieldSchema> items={availableTargetFields} value={targetField} onChange={handleTargetChange} displayLabel={(f) => f.name} filterFn={(f, q) => f.name.toLowerCase().includes(q.toLowerCase())} placeholder={t('settings.fieldMapping.staticTargetPlaceholder')} ariaLabel="Static mapping target" renderItem={(f) => (<span className="flex items-center justify-between w-full"><span className="truncate">{f.name}</span><span className="text-xs text-muted-foreground ml-2">{f.schema.type}</span></span>)} /></div>`
       - Col 3: ternary — `{targetField ? <StaticValueWidget field={targetField} value={row.staticValue} onChange={handleValueChange} /> : <span className="text-brand-muted">—</span>}`
       - Col 4: flex container `<div className="flex items-center justify-end gap-1">` containing the saved indicator (rendered only when `feedback === 'saved'`) and the delete `<button>` (X icon, aria-label from `deleteStaticAriaLabel`).

    Add a one-line file header comment: `// Phase 27 — static mapping row. See 27-UI-SPEC.md for layout and 27-CONTEXT.md for the sentinel sourceFieldId pattern.`

    After implementation, run the StaticMappingRow tests. All three `it(` cases from Plan 01 must pass.

    Common pitfalls to AVOID (from 27-RESEARCH.md):
    - Do NOT render DriftWarning in StaticMappingRow (static rows cannot drift on source — Pitfall 4)
    - Do NOT call invoke before checking the target combobox value — `handleTargetChange` only fires when a target is actually selected (the combobox onChange already guards this)
    - Do NOT collapse the row into a single-column flex layout; the 4-column grid must match MappingRow so the columns visually align between row types
    - Do NOT post-process the `newValue` string in `handleValueChange` — StaticValueWidget is responsible for emitting the correct storage form (pre-serialized JSON for single-option, raw comma-separated text for arrays, raw text otherwise). The row component just persists what it receives.
  </action>
  <verify>
    <automated>npx vitest run src/features/field-mapping/__tests__/StaticMappingRow.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/features/field-mapping/StaticMappingRow.tsx exists
    - File contains `export function StaticMappingRow`
    - File contains the literal `'__static__'` (sentinel prefix construction)
    - File contains `await invoke('set_field_mapping'`
    - File contains `await invoke('delete_field_mapping'`
    - File contains `transformerKind: 'static'` (literal type assignment in handleTargetChange)
    - File contains `t('settings.fieldMapping.staticBadge')` and `t('settings.fieldMapping.staticBadgeTooltip')` and `t('settings.fieldMapping.staticTargetPlaceholder')` and `t('settings.fieldMapping.deleteStaticAriaLabel'`
    - File does NOT import or render `DriftWarning`
    - `npx vitest run src/features/field-mapping/__tests__/StaticMappingRow.test.tsx` exits 0 with all 3 tests passing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    StaticMappingRow shipped; STATIC-UI-03 and STATIC-UI-04 green; STATIC-UI-01 will go green after Task 3 wires the section.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire FieldMappingSection — Add static value button, pending row, static-row discrimination</name>
  <files>src/features/field-mapping/FieldMappingSection.tsx</files>
  <read_first>
    - src/features/field-mapping/FieldMappingSection.tsx (full file — confirm exact line numbers for pendingAdd state at 182, handleAddRow at 242, mappingRows.map at 316, pendingAdd render block at 331, Add field mapping Button at 367)
    - src/features/field-mapping/StaticMappingRow.tsx (from Task 2 — confirm prop signature)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-PATTERNS.md (FieldMappingSection.tsx section — pendingStaticAdd pattern and row discrimination)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md (Add static value Button placement and pending static row layout)
  </read_first>
  <behavior>
    - The component imports `StaticMappingRow` from `./StaticMappingRow`
    - A new `pendingStaticAdd` boolean useState exists alongside `pendingAdd`
    - A new `handleAddStaticRow` function sets `setPendingStaticAdd(true)`
    - The "Add static value" Button is rendered immediately below the existing "Add field mapping" Button, same variant (`ghost`), same size (`sm`), same `w-full justify-start text-brand-muted hover:text-brand-text gap-1.5` className. Its `disabled` prop is `pendingStaticAdd || pendingAdd`. `aria-label="Add static value mapping"`. Text from `t('settings.fieldMapping.addStaticRow')`.
    - The row map (`mappingRows.map((row) => ...)`) branches: if `row.sourceFieldId.startsWith('__static__')`, render `<StaticMappingRow ... />` (no `isDrifted` prop); else render `<MappingRow ... />` as today.
    - When `pendingStaticAdd === true`, an inline pending static row renders below the existing rows: a StaticMappingRow with an "empty" row `{ sourceFieldId: '__static__', targetFieldId: '', transformerKind: 'static', sourceSchema: { type: 'any' }, targetSchema: { type: 'any' }, staticValue: undefined }`. The row's `onRowUpdate` handler (provided by FieldMappingSection) inserts the new persisted row into `mappingRows` AND sets `setPendingStaticAdd(false)`. The row's `onRowDelete` handler removes the row (no-op since it was never persisted) AND sets `setPendingStaticAdd(false)`.
    - The empty-state branch (`mappingRows.length === 0 && !pendingAdd`) must also account for `!pendingStaticAdd` — i.e. `mappingRows.length === 0 && !pendingAdd && !pendingStaticAdd ? <empty> : <list>`
    - STATIC-UI-01 (render of Add static value button + initial pending state) test passes when the section is wrapped in a render test
  </behavior>
  <action>
    Modify src/features/field-mapping/FieldMappingSection.tsx with five surgical edits:

    (A) Add the import. Near the existing `import { MappingRow } from './MappingRow';` line, add `import { StaticMappingRow } from './StaticMappingRow';`.

    (B) Add the new state. Immediately after `const [pendingAdd, setPendingAdd] = useState(false);` (around line 182), add `const [pendingStaticAdd, setPendingStaticAdd] = useState(false);`.

    (C) Add the new handler. Near the existing `function handleAddRow()` (around line 242), add a sibling `function handleAddStaticRow() { setPendingStaticAdd(true); }`.

    (D) Discriminate the row map. Replace the existing `mappingRows.map((row) => <MappingRow ... />)` block (around lines 316–328) with a ternary that renders `<StaticMappingRow ... />` for rows where `row.sourceFieldId.startsWith('__static__')`, else `<MappingRow ... />`. The StaticMappingRow receives: `key={row.sourceFieldId}`, `row={row}`, `targetFields={targetFields}`, `usedTargetFieldIds={usedTargetFieldIds}`, `onRowUpdate={updateRow}`, `onRowDelete={deleteRow}` (the existing helpers from the component scope — confirm their names match what MappingRow currently receives).

    (E) Add the pending static row render block immediately AFTER the existing `pendingAdd && (...)` block (around line 363). The block renders a StaticMappingRow with `key="__pending_static__"`, an "empty" placeholder `row` whose `sourceFieldId` is `'__static__'`, `targetFieldId` is `''`, `transformerKind` is `'static'`, `sourceSchema` is `{ type: 'any' }`, `targetSchema` is `{ type: 'any' }`, `staticValue: undefined`. Pass `targetFields={targetFields}` and `usedTargetFieldIds={usedTargetFieldIds}`. Wire `onRowUpdate` to a callback that calls `updateRow(persisted)` then `setPendingStaticAdd(false)`. Wire `onRowDelete` to a callback that calls `setPendingStaticAdd(false)` (no `deleteRow` call needed because the row was never persisted). The exact JSX shape mirrors the existing `pendingAdd && (...)` block — use the same conditional-render pattern.

    (F) Add the Add static value Button immediately after the existing "Add field mapping" Button block (around line 367–378). The new button:
    - `type="button"`, `variant="ghost"`, `size="sm"`
    - `onClick={handleAddStaticRow}`
    - `disabled={pendingStaticAdd || pendingAdd}`
    - `className="w-full justify-start text-brand-muted hover:text-brand-text gap-1.5"` (no `mt-3` — sits directly below the existing button as part of a button group)
    - `aria-label="Add static value mapping"`
    - Children: `<Plus className="h-3.5 w-3.5" aria-hidden="true" />` and `<span>{t('settings.fieldMapping.addStaticRow')}</span>`

    (G) Update the empty-state condition. Find the existing `mappingRows.length === 0 && !pendingAdd ? <empty> : <list>` ternary (around line 306) and change it to `mappingRows.length === 0 && !pendingAdd && !pendingStaticAdd ? <empty> : <list>`.

    Do NOT modify the `driftedSourceFieldIds` memo, the `usedTargetFieldIds` memo, the source-field lookup, or any code paths that do not relate to static rows. The discriminator at the row map is the only place static rows diverge from normal rows in the render tree.

    Run the full field-mapping test suite. Also run the existing FieldMappingSection tests if any exist (if not, write none — the StaticMappingRow tests cover the row-level behavior; the section-level coverage is the human-verify checkpoint that follows).
  </action>
  <verify>
    <automated>npx vitest run src/features/field-mapping 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/features/field-mapping/FieldMappingSection.tsx contains `import { StaticMappingRow }`
    - File contains the literal `pendingStaticAdd`
    - File contains the literal `handleAddStaticRow`
    - File contains `aria-label="Add static value mapping"` (or equivalent in i18n form — at minimum, the literal `settings.fieldMapping.addStaticRow`)
    - File contains `row.sourceFieldId.startsWith('__static__')`
    - The empty-state ternary references `!pendingStaticAdd`
    - `npx vitest run src/features/field-mapping` exits 0 with all tests in the directory passing (StaticMappingRow.test.tsx 3 tests + StaticValueWidget.test.tsx 4 tests + MappingRow.test.tsx existing tests)
    - `npx tsc --noEmit` exits 0
    - `npm run lint -- src/features/field-mapping` (or equivalent — `npx eslint src/features/field-mapping`) exits 0
  </acceptance_criteria>
  <done>
    UI wiring complete. The Settings page now exposes the full static-value feature. All four Wave 0 React tests (STATIC-UI-01..04) are green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of smart widget per UI-SPEC dispatch table</name>
  <files>n/a — verification only; no files modified</files>
  <read_first>
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-UI-SPEC.md (Smart Value Widget dispatch table)
    - .planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-VALIDATION.md (Manual-Only Verifications section)
  </read_first>
  <what-built>
    A complete static-value mapping feature in Settings → Copying → Field Mapping:
    - "Add static value" button (ghost variant, sits immediately below "Add field mapping")
    - Clicking it inserts a pending static row with a red-tinted "Static" badge in col 1, an empty target combobox in col 2
    - Selecting a target field reveals a smart value widget in col 3 whose type depends on the target field's schema (text input, combobox, comma-separated text, or disabled placeholder)
    - For single-select option-type targets, the stored value is the pre-serialized Jira write-shape ({"id":"..."}), guaranteeing valid Jira Cloud v3 payloads on copy
    - For array target types (array-of-option, array-of-string Labels), the stored value is the RAW comma-separated text exactly as typed. The Rust pipeline splits + trims and produces the correct JSON array shape per target schema (`[{"id":"val"},...]` for array-of-option, `["val",...]` for array-of-string).
    - Value changes auto-save with a 1500ms "Saved" flash; delete works inline
    - All four Wave 0 React tests passing automatically; full Rust test suite still green from Plan 02 (including the two new array-splitting tests)
  </what-built>
  <action>
    Pause execution and await human verification. The executor MUST NOT proceed past this checkpoint until the user types "approved" (or an equivalent confirmation). During the pause, surface the `<how-to-verify>` steps verbatim to the user and remain idle until the resume-signal arrives. If the user reports a widget rendering the wrong control for any schema type, capture the specific mismatch (which schema type → which widget actually rendered) and stop — that triggers a plan revision rather than a fix attempt inside this checkpoint.
  </action>
  <how-to-verify>
    1. Launch the app in dev mode: `npm run tauri dev` (or your usual dev launch command — confirm the mock server is registered)
    2. Open Settings → Copying → Field Mapping
    3. Click the "Add static value" button (should be the second of two ghost buttons at the bottom of the row list)
    4. Verify a new row appears with the red-tinted "Static" badge in col 1 and an empty "Pick a target field" combobox in col 2
    5. Open the target combobox and pick a SINGLE-select OPTION-type field (e.g. a SingleSelect custom field like "Severity" if present in the mock — verify by looking at the schema type indicator in the combobox item; should show `option`, NOT `array`)
    6. Verify col 3 shows a VirtualizedCombobox listing the option's allowed values (e.g. "Blocker", "High", "Medium", "Low")
    7. Pick a value; verify the "Saved" indicator flashes briefly in col 4. **Power-user check:** inspect the SQLite `field_mapping.static_value` column for the persisted row — the stored string MUST be JSON-shaped like `{"id":"10001"}`, NOT a bare id like `10001`. This confirms the single-option write-shape contract.
    8. Delete the row using the X button; verify it disappears
    9. Repeat steps 3–7 with a STRING-type field — verify col 3 is a plain text input; type a value, blur, verify the saved indicator. Stored value in the DB should be the raw text (no JSON wrapping).
    10. Repeat with an ARRAY-OF-OPTION-type field (a MultiSelect custom field if present — schema indicator shows `array`, items `option`). Verify col 3 is a text input (NOT a combobox) with the placeholder indicating "Enter option IDs, comma-separated" and the hint "Separate values with commas" below. Type two option IDs separated by a comma (e.g. `10001, 10002`). **Power-user check:** inspect `field_mapping.static_value` — the stored string MUST be the RAW comma-separated text `"10001, 10002"`, NOT a pre-serialized JSON array. The pipeline does the splitting at copy time.
    11. Repeat with an ARRAY-OF-STRING-type field (Labels — schema indicator shows `array`, items `string`). Verify col 3 is a text input with the same comma-separated hint. Type `"bug, regression"`. Inspect the DB — stored value is the raw `"bug, regression"` text, NOT `["bug","regression"]`.
    12. (Optional) If a USER-type target is in the schema, verify the widget renders as disabled with the "Not supported in this phase" placeholder
    13. Reload the app — verify all persisted static rows reappear with their values intact: the single-option combobox displays the previously-selected value (confirms JSON write-shape round-trips through the read path), and the array text inputs display the original raw comma-separated text exactly as typed.
    14. **End-to-end copy verification:** perform an actual copy of a ticket whose mapping includes (a) a single-option static row, and (b) an array-of-option static row. Verify in Settings → Copying → Audit Log → Field Transformations that the static_value writes appear with the correct JSON shape: `{"id":"10001"}` for the single-option row and `[{"id":"10001"},{"id":"10002"}]` for the array-of-option row. The pipeline is responsible for the array shape — this confirms Plan 27-02 Task 3 dispatch works end-to-end.
  </how-to-verify>
  <verify>
    <human-check>User confirms each numbered step in `<how-to-verify>` works as described. If any step fails, the user describes the failure mode (which schema → which widget mismatch, or which storage/payload shape was wrong) so the plan can be revised.</human-check>
  </verify>
  <acceptance_criteria>
    - User confirms steps 1–8 (single-option widget + write-shape) work end-to-end, including DB inspection in step 7
    - User confirms step 9 (string-type widget + raw text storage) works
    - User confirms step 10 (array-of-option widget + raw comma-separated storage) works
    - User confirms step 11 (array-of-string widget + raw comma-separated storage) works
    - User confirms step 13 (persistence + round-trip of single-option JSON write-shape AND array raw text across reload) works
    - User confirms step 14 (end-to-end copy emits correct Jira payload shapes for single-option and array-of-option) works
    - User types "approved" or equivalent affirmative resume signal
  </acceptance_criteria>
  <done>
    Smart widget dispatch verified by the user. The Phase 27 feature is confirmed shipped end-to-end across all relevant target field schema types. Single-option values store the pre-serialized Jira Cloud v3 write-shape; array values store raw comma-separated text that the pipeline splits at copy time.
  </done>
  <resume-signal>Type "approved" when the smart widget dispatches correctly for each tested schema type and persistence + end-to-end copy work across reload. If a widget renders the wrong control for a schema type, the stored value is not in the expected form (JSON for single-option, raw text for arrays), or the Jira payload shape is wrong for either case, describe the mismatch and the plan will be revised.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input in static value widget | Free-text input that flows to Tauri command, SQLite, and ultimately the Jira Cloud API body |
| Combobox selection from allowed_values | Bounded set sourced from the cached field schema — lower trust risk than free text |
| Static badge tooltip content | Static i18n string — no user influence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Injection | static_value input | mitigate | Plan 02 already binds static_value via rusqlite params![] and serializes via serde_json — no string interpolation. UI just calls invoke('set_field_mapping', { row }) with the value as a struct field. For single-option fields, the stored value is `JSON.stringify({ id: opt.id })` — the `id` comes from the trusted allowed_values cache, not free-text input. For array fields, the stored value is raw user text; the pipeline's split-and-wrap (Plan 02 Task 3) constructs each array element via `json!({"id": s})` or `Value::String(s)`, both of which escape embedded JSON syntax. |
| T-27-04-02 | Injection | Jira API payload | accept | Jira Cloud validates field shapes server-side. The existing audit pipeline (mapping_audit_log) already logs arbitrary user-provided field values without filtering — same risk profile here. |
| T-27-04-03 | Information disclosure | Static badge tooltip | accept | Tooltip text is a static i18n string. No PII. |
| T-27-04-04 | Tampering | Sentinel sourceFieldId collision | mitigate | The `__static__` prefix is namespaced to avoid collision with real Jira field IDs (which never contain underscores at the start of the customfield_X pattern). Verified in 27-RESEARCH.md Pitfall 3. The UI's discriminator `row.sourceFieldId.startsWith('__static__')` is the single source of truth for routing rendering. |
| T-27-04-05 | Denial of service | Allowed_values dropdown size | accept | VirtualizedCombobox already handles 500+ items per phase-22 testing (referenced in STATE.md deferred items). No new performance risk. |
| T-27-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan. RESEARCH.md "Package Legitimacy Audit" confirms zero new deps. |
</threat_model>

<verification>
- `npx vitest run src/features/field-mapping` exits 0 with the four Wave 0 React tests passing alongside the existing MappingRow tests
- `npx tsc --noEmit` exits 0
- `npx eslint src/features/field-mapping` exits 0 (or the project's lint command for this directory)
- Human verification checkpoint approved with description of which schema types were verified end-to-end, including single-option write-shape round-trip AND array raw-text storage AND end-to-end copy producing the correct Jira array shape
- Full project test suite: `npx vitest run && cargo test -p pmkar-lib` both exit 0
</verification>

<success_criteria>
The user-visible Phase 27 feature is shipped: Settings → Copying → Field Mapping exposes "Add static value", the smart widget adapts per UI-SPEC, single-option targets store the pre-serialized Jira Cloud v3 write-shape (`{"id":"..."}`) so the pipeline pass-through emits valid payloads, array targets store raw comma-separated text and the Rust pipeline (Plan 02 Task 3) splits + trims to produce the correct JSON array shape (`[{"id":"val"},...]` for array-of-option, `["val",...]` for array-of-string), persistence and delete work, audit log captures static writes through the existing pipeline, all Wave 0 tests are green, and no regressions in the rest of the project test suite. Human checkpoint confirms each schema-type widget renders the correct control AND the storage shape AND the end-to-end Jira payload shape across reload and during a real copy.
</success_criteria>

<output>
Create `.planning/phases/27-add-static-value-mapping-to-configurable-field-mapping/27-04-SUMMARY.md` when done.
</output>
