---
phase: 10-improve-codebase-quality
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-03]
files_modified:
  - vitest.config.ts
  - package.json
  - src/features/tickets/__tests__/ticketStore.test.ts
  - src/features/tickets/__tests__/copyStore.test.ts
  - src/features/connections/__tests__/connectionStore.test.ts
  - src/features/theme/__tests__/themeStore.test.ts
  - src/lib/__tests__/format.test.ts
  - src/lib/__tests__/utils.test.ts
autonomous: true
requirements: [D-04, D-05, D-06]

must_haves:
  truths:
    - "npm run test:coverage exits 0 with line coverage >= 80%"
    - "All 4 Zustand stores have dedicated test files"
    - "Utility functions in lib/ have test files"
    - "Coverage thresholds are enforced — dropping below 80% fails the command"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Coverage provider and threshold configuration"
      contains: "thresholds"
    - path: "src/features/tickets/__tests__/ticketStore.test.ts"
      provides: "Tests for ticket store actions and state"
      contains: "useTicketStore"
    - path: "src/features/tickets/__tests__/copyStore.test.ts"
      provides: "Tests for copy store actions and state"
      contains: "useCopyStore"
    - path: "src/features/connections/__tests__/connectionStore.test.ts"
      provides: "Tests for connection store actions and state"
      contains: "useConnectionStore"
    - path: "src/lib/__tests__/format.test.ts"
      provides: "Tests for format utility functions"
      contains: "describe"
  key_links:
    - from: "vitest.config.ts"
      to: "package.json"
      via: "test:coverage script"
      pattern: "vitest run --coverage"
---

<objective>
Add vitest coverage tooling with enforced 80% threshold and write tests for all untested Zustand stores and utility functions.

Purpose: Raise frontend test coverage from current baseline to 80% line coverage per D-04, with threshold enforcement per D-05, covering the priority areas identified in D-06.
Output: Coverage config in vitest.config.ts, test:coverage script, test files for ticketStore, copyStore, connectionStore, themeStore, format.ts, and utils.ts.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-CONTEXT.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install coverage provider, configure thresholds, add test:coverage script</name>
  <files>vitest.config.ts, package.json</files>
  <read_first>
    - vitest.config.ts (current test config — add coverage block)
    - package.json (current scripts — add test:coverage)
    - .planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md (coverage config pattern)
  </read_first>
  <action>
1. Install vitest coverage provider:
   ```bash
   npm install -D @vitest/coverage-v8
   ```

2. Update `vitest.config.ts` to add the coverage block inside the existing `test` section:
   ```typescript
   test: {
     environment: 'jsdom',
     setupFiles: ['./src/test-setup.ts'],
     globals: true,
     include: ['src/**/*.test.{ts,tsx}'],
     coverage: {
       provider: 'v8',
       include: ['src/**/*.{ts,tsx}'],
       exclude: [
         'src/test-setup.ts',
         'src/test-utils/**',
         'src/main.tsx',
         '**/*.test.{ts,tsx}',
         '**/__tests__/**',
       ],
       thresholds: {
         lines: 80,
         functions: 80,
         branches: 75,
         statements: 80,
       },
       reporter: ['text', 'lcov'],
     },
   },
   ```
   NOTE: Do NOT set thresholds yet — set them to 0 initially. We will ratchet up AFTER writing tests in Task 2. Use:
   ```typescript
   thresholds: {
     lines: 0,
     functions: 0,
     branches: 0,
     statements: 0,
   },
   ```

3. Add `test:coverage` script to package.json:
   ```json
   "test:coverage": "vitest run --coverage"
   ```

4. Run `npm run test:coverage` to establish baseline coverage numbers. Record the output — this tells us how much test-writing is needed in Task 2.

5. Verify all 114+ existing tests still pass with the coverage provider installed.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run test:coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"test:coverage": "vitest run --coverage"`
    - package.json devDependencies contains `"@vitest/coverage-v8"`
    - vitest.config.ts contains `coverage:` block with `provider: 'v8'`
    - vitest.config.ts contains `thresholds:` block (initially at 0)
    - `npm run test:coverage` exits 0 and prints coverage table
    - All existing tests still pass
  </acceptance_criteria>
  <done>Coverage tooling installed with v8 provider per D-05. Baseline coverage measured. Thresholds temporarily at 0, will be set to 80% after tests are written.</done>
</task>

<task type="auto">
  <name>Task 2: Write tests for all untested Zustand stores and utility functions, enforce 80% threshold</name>
  <files>
    src/features/tickets/__tests__/ticketStore.test.ts,
    src/features/tickets/__tests__/copyStore.test.ts,
    src/features/connections/__tests__/connectionStore.test.ts,
    src/features/theme/__tests__/themeStore.test.ts,
    src/lib/__tests__/format.test.ts,
    src/lib/__tests__/utils.test.ts,
    vitest.config.ts
  </files>
  <read_first>
    - src/features/tickets/ticketStore.ts (full file — understand state shape, actions, invoke calls)
    - src/features/tickets/copyStore.ts (full file — understand state shape, actions, invoke calls)
    - src/features/connections/connectionStore.ts (full file — understand state shape, actions, invoke calls)
    - src/features/theme/themeStore.ts (full file — understand state shape, actions)
    - src/lib/format.ts (full file — understand exported functions)
    - src/lib/utils.ts (full file — understand exported functions)
    - src/i18n/__tests__/languageStore.test.ts (reference pattern for Zustand store testing)
    - src/i18n/languageStore.ts (reference for how existing store test was written)
    - vitest.config.ts (to update thresholds at the end)
  </read_first>
  <action>
1. **Follow the established Zustand test pattern** from `languageStore.test.ts`:
   - `vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn() }))` at top
   - Import store and `invoke`, cast with `vi.mocked(invoke)`
   - `beforeEach` resets mock and store state via `store.setState(initialState)`

2. **Create `src/features/tickets/__tests__/ticketStore.test.ts`:**
   Test all exported actions and state transitions:
   - Initial state is correct (empty tickets, null selectedKey, loading false, error null)
   - `fetchTickets` action: mock invoke to return ticket data, verify tickets are set in state
   - `fetchTickets` error path: mock invoke to reject, verify error is set and loading is false
   - `setSelectedKey` action: verify selectedKey updates
   - `setTriageState` action: mock invoke, verify triage map updates
   - `hydrateFetchConfig` action: mock invoke, verify fetchConfig state updates
   - `hydrateTriageMap` action: mock invoke, verify triageMap state updates
   - Loading states: verify loading is true during fetch, false after

3. **Create `src/features/tickets/__tests__/copyStore.test.ts`:**
   - Initial state is correct (null preview, idle phase, no error)
   - `fetchPreview` action: mock invoke, verify preview data is set
   - `executeCopy` action: mock invoke, verify result data and phase transitions
   - `resetCopy` action: verify state returns to initial
   - Error handling: mock invoke rejection, verify error state
   - Phase transitions: idle -> previewing -> copying -> done

4. **Create `src/features/connections/__tests__/connectionStore.test.ts`:**
   - Initial state is correct
   - `setServerConnection` and `setCloudConnection` actions update state
   - `hasCompletedSetup()` returns false initially, true after both connections configured
   - `testConnection` action: mock invoke for success and failure paths
   - `storeCredential` action: mock invoke, verify credential stored

5. **Create `src/features/theme/__tests__/themeStore.test.ts`:**
   - Initial state has correct default theme
   - `setTheme` action updates theme
   - Theme values are one of expected enum values

6. **Create `src/lib/__tests__/format.test.ts`:**
   Read `src/lib/format.ts` and test every exported function:
   - Test each function with normal inputs, edge cases (empty string, null, undefined if accepted)
   - Test date formatting, relative time, number formatting, etc. (whatever the module exports)

7. **Create `src/lib/__tests__/utils.test.ts`:**
   Read `src/lib/utils.ts` and test every exported function:
   - `cn()` utility: test with single class, multiple classes, conditional classes, undefined values

8. **Run `npm run test:coverage` and check if 80% line coverage is met.**
   - If coverage is below 80%, identify the largest uncovered files from the coverage report
   - Write additional tests for those files (component tests if needed, or additional store/utility tests)
   - Focus on the files with most uncovered lines first for maximum impact

9. **Once coverage meets 80%, update `vitest.config.ts` thresholds to enforce it:**
   ```typescript
   thresholds: {
     lines: 80,
     functions: 80,
     branches: 75,
     statements: 80,
   },
   ```

10. Run `npm run test:coverage` one final time — it must exit 0 with thresholds enforced.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run test:coverage 2>&1 | grep -E "^(All files|Statements|Branches|Functions|Lines)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/tickets/__tests__/ticketStore.test.ts` exists and contains at least 6 test cases
    - `src/features/tickets/__tests__/copyStore.test.ts` exists and contains at least 5 test cases
    - `src/features/connections/__tests__/connectionStore.test.ts` exists and contains at least 4 test cases
    - `src/features/theme/__tests__/themeStore.test.ts` exists and contains at least 3 test cases
    - `src/lib/__tests__/format.test.ts` exists with tests for every exported function
    - `src/lib/__tests__/utils.test.ts` exists with tests for `cn` utility
    - `npm run test:coverage` exits 0
    - vitest.config.ts `thresholds.lines` is set to 80
    - Coverage output shows Lines >= 80%
  </acceptance_criteria>
  <done>All 4 Zustand stores and lib utilities have test files per D-06. Line coverage >= 80% per D-04. Thresholds enforced in vitest config per D-05.</done>
</task>

</tasks>

<verification>
- `npm run test:coverage` exits 0 with enforced 80% line threshold
- `npm test` exits 0 (all tests pass without coverage overhead)
- 6 new test files exist in __tests__/ directories
- vitest.config.ts has thresholds.lines = 80
</verification>

<success_criteria>
Coverage tooling is configured with v8 provider. All untested Zustand stores and utility functions have dedicated test suites. Line coverage meets 80% threshold. Threshold enforcement is active — dropping below 80% fails `npm run test:coverage`.
</success_criteria>

<output>
After completion, create `.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-04-SUMMARY.md`
</output>
