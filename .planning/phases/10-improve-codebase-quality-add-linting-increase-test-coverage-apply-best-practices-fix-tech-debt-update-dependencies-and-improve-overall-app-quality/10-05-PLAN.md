---
phase: 10-improve-codebase-quality
plan: 05
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03, 10-04]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [D-09]

must_haves:
  truths:
    - "CI workflow file exists at .github/workflows/ci.yml"
    - "CI runs lint, type-check, and test:coverage for frontend"
    - "CI runs cargo fmt --check, cargo clippy, and cargo test for Rust"
    - "CI triggers on push to main and pull requests to main"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI pipeline"
      contains: "npm run lint"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "npm scripts"
      pattern: "npm run"
    - from: ".github/workflows/ci.yml"
      to: "src-tauri/Cargo.toml"
      via: "cargo commands"
      pattern: "cargo"
---

<objective>
Create a GitHub Actions CI workflow that runs lint, type-check, test with coverage, and Rust quality checks on every push/PR.

Purpose: Automate quality enforcement so that the linting, coverage, and formatting standards from Plans 01-04 are enforced on every code change. No manual verification needed.
Output: .github/workflows/ci.yml with frontend and Rust jobs.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-CONTEXT.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow with frontend and Rust jobs</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - package.json (verify script names: lint, test:coverage, build)
    - src-tauri/Cargo.toml (verify workspace structure)
    - .planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md (CI workflow pattern and Linux deps)
  </read_first>
  <action>
1. Create directory `.github/workflows/` if it doesn't exist.

2. Create `.github/workflows/ci.yml` with this exact content:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm
      - run: npm ci
      - name: Lint
        run: npm run lint
      - name: Type check
        run: npx tsc --noEmit
      - name: Test with coverage
        run: npm run test:coverage

  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: '. -> target'
      - name: Install Linux deps
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - name: Format check
        run: cargo fmt --check
        working-directory: src-tauri
      - name: Clippy
        run: cargo clippy -- -D warnings
        working-directory: src-tauri
      - name: Test
        run: cargo test
        working-directory: src-tauri
```

3. Verify the YAML is valid by checking syntax (correct indentation, no tabs).

4. Verify the workflow references match actual package.json scripts:
   - `npm run lint` exists (added in Plan 01)
   - `npm run test:coverage` exists (added in Plan 04)
   - `npx tsc --noEmit` works (TypeScript installed)
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && cat .github/workflows/ci.yml | head -5 && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" 2>&1 && echo "YAML valid"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists
    - File contains `on:` with `push:` and `pull_request:` triggers for `main` branch
    - File contains `frontend:` job with steps: checkout, setup-node, npm ci, npm run lint, tsc --noEmit, npm run test:coverage
    - File contains `rust:` job with steps: checkout, rust-toolchain, rust-cache, apt-get install, cargo fmt --check, cargo clippy -- -D warnings, cargo test
    - `rust:` job has `working-directory: src-tauri` on cargo commands
    - YAML parses without errors
  </acceptance_criteria>
  <done>GitHub Actions CI workflow created per D-09. Frontend job runs lint + type-check + test:coverage. Rust job runs fmt + clippy + test. Triggers on push/PR to main.</done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` exists and is valid YAML
- File contains both `frontend` and `rust` jobs
- Frontend job runs: lint, tsc --noEmit, test:coverage
- Rust job runs: fmt --check, clippy, test
- Triggers on push and PR to main
</verification>

<success_criteria>
CI workflow is committed and ready to run. On next push to main or PR creation, GitHub Actions will automatically enforce all quality gates: Biome lint, TypeScript type-check, 80% test coverage, Rust formatting, clippy pedantic, and Rust tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-05-SUMMARY.md`
</output>
