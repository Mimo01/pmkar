---
phase: 10-improve-codebase-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - biome.json
  - package.json
  - src/test-setup.ts
  - src/features/tickets/TicketListPage.test.tsx
  - src/features/tickets/CopyPreviewModal.test.tsx
  - src/features/tickets/IgnoredTicketsPage.test.tsx
  - src/features/tickets/TicketTable.tsx
autonomous: true
requirements: [D-01, D-02, D-07, D-08]

must_haves:
  truths:
    - "biome check src/ exits 0 with zero warnings and zero errors"
    - "biome format --check src/ exits 0 (all files already formatted)"
    - "No eslint-disable comments remain in codebase"
    - "No `as any` or `: any` remains in source or test files"
    - "TicketTable.tsx is deleted and no file imports it"
    - "npm run lint script exists and passes"
  artifacts:
    - path: "biome.json"
      provides: "Biome linter and formatter configuration"
      contains: "recommended"
    - path: "package.json"
      provides: "lint, lint:fix, format scripts"
      contains: "biome check"
  key_links:
    - from: "package.json"
      to: "biome.json"
      via: "npm run lint invokes biome check"
      pattern: "biome check"
---

<objective>
Set up Biome linter/formatter, fix all violations, eliminate `any` types, and remove dead code.

Purpose: Establish the frontend quality baseline that all subsequent plans build on. Lint must be clean before dependency upgrades or new test code.
Output: biome.json config, clean lint pass, no dead code, no `any` usage.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-CONTEXT.md
@.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Biome, create config, add npm scripts, and auto-format codebase</name>
  <files>biome.json, package.json</files>
  <read_first>
    - package.json (current scripts and dependencies)
    - vite.config.ts (understand build pipeline)
    - vitest.config.ts (understand test pipeline)
    - .planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-RESEARCH.md (Biome config pattern)
  </read_first>
  <action>
1. Install Biome with exact version pin:
   ```
   npm install -D -E @biomejs/biome
   ```

2. Create `biome.json` at project root with this exact content:
   ```json
   {
     "$schema": "https://biomejs.dev/schemas/2.4.8/schema.json",
     "files": {
       "includes": ["src/**", "*.ts", "*.tsx", "*.js", "*.json"],
       "ignore": ["node_modules", "dist", "target", "src-tauri"]
     },
     "linter": {
       "enabled": true,
       "rules": {
         "recommended": true,
         "correctness": { "recommended": true, "all": false },
         "suspicious": { "recommended": true, "all": false },
         "style": { "recommended": true, "all": false },
         "performance": { "recommended": true, "all": false },
         "security": { "recommended": true, "all": false },
         "a11y": { "recommended": true, "all": false },
         "complexity": { "recommended": true, "all": false }
       }
     },
     "formatter": {
       "enabled": true,
       "indentStyle": "space",
       "indentWidth": 2,
       "lineWidth": 100,
       "trailingNewline": true
     },
     "javascript": {
       "formatter": {
         "quoteStyle": "single",
         "jsxQuoteStyle": "double",
         "semicolons": "always",
         "arrowParentheses": "always",
         "trailingCommas": "all"
       },
       "jsxRuntime": "transparent"
     },
     "json": {
       "formatter": { "indentWidth": 2 }
     }
   }
   ```

3. Add these scripts to package.json (preserve all existing scripts):
   ```json
   "lint": "biome check src/",
   "lint:fix": "biome check --write src/",
   "format": "biome format --write src/"
   ```

4. Run `npx biome format --write src/` to auto-format all files first (per D-02, format before lint).

5. Run `npx biome check src/` to see remaining lint violations. Note: do NOT fix violations yet — Task 2 handles that.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npx biome format --check src/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - biome.json exists at project root and contains `"recommended": true`
    - package.json contains script `"lint": "biome check src/"`
    - package.json contains script `"lint:fix": "biome check --write src/"`
    - package.json contains script `"format": "biome format --write src/"`
    - package.json devDependencies contains `"@biomejs/biome"`
    - `npx biome format --check src/` exits 0 (all files formatted)
  </acceptance_criteria>
  <done>Biome installed, configured with recommended+strict rules per D-01/D-02, npm scripts added, all files auto-formatted.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all Biome lint violations, eliminate `any` types, remove dead code</name>
  <files>
    src/test-setup.ts,
    src/features/tickets/TicketListPage.test.tsx,
    src/features/tickets/CopyPreviewModal.test.tsx,
    src/features/tickets/IgnoredTicketsPage.test.tsx,
    src/features/tickets/TicketTable.tsx
  </files>
  <read_first>
    - src/test-setup.ts (has `window as any`)
    - src/features/tickets/TicketListPage.test.tsx (has `selector: any`)
    - src/features/tickets/CopyPreviewModal.test.tsx (has `selector: (s: any) => any`)
    - src/features/tickets/IgnoredTicketsPage.test.tsx (has `tickets as any`, `triageMap as any`)
    - src/features/tickets/TicketTable.tsx (dead code — check for any remaining imports)
    - src/features/tickets/types.ts (ticket type definitions needed for fixing `any`)
    - src/features/tickets/ticketStore.ts (store type for selector fixes)
    - src/features/tickets/copyStore.ts (store type for selector fixes)
  </read_first>
  <action>
1. **Fix `any` types in test files (per D-07):**

   In `src/test-setup.ts` line 6:
   - Change `(window as any).crypto` to `(window as unknown as { crypto: Crypto }).crypto` or use a proper type assertion. If the crypto polyfill is no longer needed (jsdom 29 includes crypto), remove the block entirely.

   In `src/features/tickets/TicketListPage.test.tsx` line 14:
   - Change `(selector: any) =>` to the proper store selector type. Read the ticketStore type and use `(selector: (state: TicketStoreState) => unknown) =>` or the actual inferred type from Zustand's `UseBoundStore`.

   In `src/features/tickets/CopyPreviewModal.test.tsx` lines 12 and 102:
   - Change `(selector: (s: any) => any) =>` to use the actual copyStore state type. Import the store state type and use `(selector: (s: CopyStoreState) => unknown) =>`.

   In `src/features/tickets/IgnoredTicketsPage.test.tsx` lines 61 and 63:
   - Change `tickets as any` and `triageMap as any` to proper type casts. Read the store's expected shape and construct test data matching the real types instead of casting to `any`.

2. **Remove all `eslint-disable` comments:** Search for `eslint-disable` across all `src/**/*.{ts,tsx}` files and delete those comment lines — Biome does not recognize them.

3. **Delete dead code (per D-08):**
   - Delete `src/features/tickets/TicketTable.tsx` entirely (marked DEPRECATED, not imported anywhere).
   - Search all `src/**/*.{ts,tsx}` for any remaining `TicketTable` imports — remove if found.
   - Run `npx biome check src/` to catch any unused imports flagged by Biome. Fix with `npx biome check --write src/` for auto-fixable issues, manually fix the rest.

4. **Fix any remaining Biome lint violations** that appear after the above changes. Run `npx biome check src/` iteratively until it exits 0.

5. **Verify all 114 existing tests still pass:** Run `npm test`.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run lint && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` (biome check src/) exits 0 with no errors
    - `grep -r "as any" src/ --include="*.ts" --include="*.tsx"` returns no matches
    - `grep -r ": any" src/ --include="*.ts" --include="*.tsx"` returns no matches
    - `grep -r "eslint-disable" src/ --include="*.ts" --include="*.tsx"` returns no matches
    - `src/features/tickets/TicketTable.tsx` does not exist (deleted)
    - `npm test` exits 0 with 114 tests passing
  </acceptance_criteria>
  <done>Zero Biome violations, zero `any` usage, zero eslint-disable comments, TicketTable deleted, all existing tests pass.</done>
</task>

</tasks>

<verification>
- `npm run lint` exits 0 (Biome clean)
- `npx biome format --check src/` exits 0 (all formatted)
- `npm test` exits 0 (all 114 tests pass)
- `grep -r "as any\|: any" src/ --include="*.ts" --include="*.tsx"` returns nothing
- `ls src/features/tickets/TicketTable.tsx` returns "No such file"
</verification>

<success_criteria>
Biome is installed and configured with recommended+strict rules. All frontend code passes lint and format checks. No `any` types remain. TicketTable.tsx and all dead code are removed. All 114 existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/10-improve-codebase-quality-add-linting-increase-test-coverage-apply-best-practices-fix-tech-debt-update-dependencies-and-improve-overall-app-quality/10-01-SUMMARY.md`
</output>
