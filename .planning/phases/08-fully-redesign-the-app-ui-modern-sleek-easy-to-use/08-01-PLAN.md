---
phase: 08-fully-redesign-the-app-ui-modern-sleek-easy-to-use
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vite.config.ts
  - src/index.css
  - components.json
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/tooltip.tsx
  - src/components/ui/progress.tsx
  - src/components/ui/AppShell.tsx
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: true
requirements:
  - UI-01
  - UI-04
  - UI-07

must_haves:
  truths:
    - "shadcn/ui is initialized with Tailwind v4 mode and all 10 components are installed"
    - "Lucide React icons replace all hand-coded SVG icon components"
    - "AppShell header and nav tabs use shadcn Tabs and Lucide icons with the Linear-inspired styling from UI-SPEC"
    - "Existing brand CSS tokens are preserved alongside shadcn semantic tokens"
    - "Dark and light themes both render correctly with the merged token system"
    - "All new i18n keys for the redesign exist in both en.json and sk.json"
  artifacts:
    - path: "components.json"
      provides: "shadcn configuration"
    - path: "src/lib/utils.ts"
      provides: "cn() utility for shadcn class merging"
    - path: "src/components/ui/AppShell.tsx"
      provides: "Redesigned shell with Lucide icons and shadcn Tooltip"
    - path: "src/i18n/locales/en.json"
      provides: "Complete English translations including all new UI strings"
    - path: "src/i18n/locales/sk.json"
      provides: "Complete Slovak translations including all new UI strings"
  key_links:
    - from: "src/index.css"
      to: "shadcn components"
      via: "CSS variable aliases"
      pattern: "--background.*var\\(--color-brand"
    - from: "src/components/ui/AppShell.tsx"
      to: "lucide-react"
      via: "import"
      pattern: "import.*from 'lucide-react'"
    - from: "src/i18n/locales/en.json"
      to: "src/features/tickets/TicketListPage.tsx"
      via: "i18n keys"
      pattern: "tickets.empty.heading"
---

<objective>
Initialize shadcn/ui with Tailwind v4, install all required components and Lucide React, merge CSS tokens, add all new i18n keys for the redesign, and redesign the AppShell (header + nav tabs) to the Linear-inspired spec.

Purpose: Every subsequent plan depends on shadcn components, Lucide icons, i18n keys, and the redesigned shell. This is the foundation for the entire UI overhaul. Adding i18n keys here (Wave 1) ensures all downstream plans can reference them without missing key errors.
Output: Working shadcn/ui setup, Lucide icons, merged CSS tokens, complete i18n keys, redesigned AppShell component.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-CONTEXT.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-UI-SPEC.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-RESEARCH.md

<interfaces>
<!-- Current AppShell interface (being redesigned) -->
From src/components/ui/AppShell.tsx:
```typescript
interface AppShellProps {
  children: ReactNode;
  onGearClick?: () => void;
  activeTab?: 'new' | 'not-mine' | 'linked';
  onTabChange?: (tab: 'new' | 'not-mine' | 'linked') => void;
  auditCount?: number;
  onAuditClick?: () => void;
}
```

From src/index.css (current tokens):
```css
@theme {
  --color-brand: #c02232;
  --color-brand-light: #d4354a;
  --color-brand-dark: #a01c2a;
  --color-brand-bg: #f7f7f8;
  --color-brand-surface: #ffffff;
  --color-brand-surface-hover: #f0f0f2;
  --color-brand-surface-raised: #eaeaec;
  --color-brand-border: #e0e0e3;
  --color-brand-border-subtle: #ebebed;
  --color-brand-muted: #8c8c92;
  --color-brand-text: #231f20;
  --color-brand-text-secondary: #5c585a;
}
```

From tsconfig.json (no path alias yet):
```json
{
  "compilerOptions": { /* no paths configured */ }
}
```

From vite.config.ts (no resolve alias):
```typescript
export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui, install components, install Lucide, configure path alias, merge CSS tokens, add all new i18n keys</name>
  <files>
    package.json, tsconfig.json, vite.config.ts, src/index.css, components.json, src/lib/utils.ts,
    src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx,
    src/components/ui/tabs.tsx, src/components/ui/badge.tsx, src/components/ui/separator.tsx,
    src/components/ui/skeleton.tsx, src/components/ui/scroll-area.tsx, src/components/ui/tooltip.tsx,
    src/components/ui/progress.tsx,
    src/i18n/locales/en.json, src/i18n/locales/sk.json
  </files>
  <read_first>
    src/index.css, tsconfig.json, vite.config.ts, package.json, src/i18n/locales/en.json, src/i18n/locales/sk.json
  </read_first>
  <action>
    **Step 1: Configure path alias for `@/` imports (required by shadcn).**

    In `tsconfig.json`, add inside `compilerOptions`:
    ```json
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
    ```

    In `vite.config.ts`, add resolve alias:
    ```typescript
    import path from "path";
    // inside defineConfig:
    resolve: {
      alias: {
        "@": path.resolve(__dirname, "./src"),
      },
    },
    ```

    **Step 2: Install Lucide React.**
    ```bash
    npm install lucide-react
    ```

    **Step 3: Initialize shadcn/ui.**
    ```bash
    npx shadcn@latest init
    ```
    During init, select:
    - Style: Default
    - Base color: Neutral
    - CSS variables: Yes
    - Tailwind v4: Yes (or auto-detected)
    - Components path: `src/components/ui`
    - Utils path: `src/lib/utils`

    If the CLI is interactive and cannot be automated, create the files manually:

    Create `components.json`:
    ```json
    {
      "$schema": "https://ui.shadcn.com/schema.json",
      "style": "default",
      "rsc": false,
      "tsx": true,
      "tailwind": {
        "config": "",
        "css": "src/index.css",
        "baseColor": "neutral",
        "cssVariables": true
      },
      "aliases": {
        "components": "@/components",
        "utils": "@/lib/utils",
        "ui": "@/components/ui",
        "lib": "@/lib",
        "hooks": "@/hooks"
      },
      "iconLibrary": "lucide"
    }
    ```

    Create `src/lib/utils.ts`:
    ```typescript
    import { type ClassValue, clsx } from "clsx";
    import { twMerge } from "tailwind-merge";

    export function cn(...inputs: ClassValue[]) {
      return twMerge(clsx(inputs));
    }
    ```
    Install clsx and tailwind-merge: `npm install clsx tailwind-merge`

    **Step 4: Install all 10 shadcn components.**
    ```bash
    npx shadcn@latest add button card dialog tabs badge separator skeleton scroll-area tooltip progress
    ```
    If CLI fails, install manually by copying from shadcn registry.

    **Step 5: Merge CSS tokens in `src/index.css`.**

    After shadcn init modifies `src/index.css`, ensure the following state. Inside the `@theme {}` block, ADD shadcn semantic token aliases that point to existing brand tokens. Do NOT remove existing brand tokens. The merged `@theme {}` block must contain:

    ```css
    @theme {
      /* Existing brand tokens — preserved */
      --color-brand: #c02232;
      --color-brand-light: #d4354a;
      --color-brand-dark: #a01c2a;
      --color-brand-bg: #f7f7f8;
      --color-brand-surface: #ffffff;
      --color-brand-surface-hover: #f0f0f2;
      --color-brand-surface-raised: #eaeaec;
      --color-brand-border: #e0e0e3;
      --color-brand-border-subtle: #ebebed;
      --color-brand-muted: #8c8c92;
      --color-brand-text: #231f20;
      --color-brand-text-secondary: #5c585a;

      /* shadcn semantic aliases — pointing to brand tokens */
      --background: var(--color-brand-bg);
      --foreground: var(--color-brand-text);
      --card: var(--color-brand-surface);
      --card-foreground: var(--color-brand-text);
      --popover: var(--color-brand-surface);
      --popover-foreground: var(--color-brand-text);
      --primary: var(--color-brand);
      --primary-foreground: #ffffff;
      --secondary: var(--color-brand-surface-hover);
      --secondary-foreground: var(--color-brand-text);
      --muted: var(--color-brand-surface-hover);
      --muted-foreground: var(--color-brand-muted);
      --accent: var(--color-brand-surface-hover);
      --accent-foreground: var(--color-brand-text);
      --destructive: #dc2626;
      --destructive-foreground: #ffffff;
      --border: var(--color-brand-border);
      --input: var(--color-brand-border);
      --ring: var(--color-brand);
      --radius: 0.5rem;
    }
    ```

    In `body.dark {}`, add the same shadcn semantic aliases pointing to the dark brand values:
    ```css
    body.dark {
      /* existing dark brand overrides */
      --color-brand-bg: #161617;
      --color-brand-surface: #1e1e20;
      --color-brand-surface-hover: #28282a;
      --color-brand-surface-raised: #1a1a1c;
      --color-brand-border: #2e2e30;
      --color-brand-border-subtle: #252527;
      --color-brand-muted: #6b6b6f;
      --color-brand-text: #e4e4e6;
      --color-brand-text-secondary: #95959a;
      background-color: #161617;
      color: #e4e4e6;

      /* shadcn semantic dark overrides */
      --background: #161617;
      --foreground: #e4e4e6;
      --card: #1e1e20;
      --card-foreground: #e4e4e6;
      --popover: #1e1e20;
      --popover-foreground: #e4e4e6;
      --primary: #c02232;
      --primary-foreground: #ffffff;
      --secondary: #28282a;
      --secondary-foreground: #e4e4e6;
      --muted: #28282a;
      --muted-foreground: #6b6b6f;
      --accent: #28282a;
      --accent-foreground: #e4e4e6;
      --destructive: #dc2626;
      --destructive-foreground: #ffffff;
      --border: #2e2e30;
      --input: #2e2e30;
      --ring: #c02232;
    }
    ```

    Preserve the existing `@import "tailwindcss"` at the top. Preserve existing scrollbar styles.

    **Step 6: Add all new i18n keys to en.json and sk.json.**

    This step front-loads ALL new i18n keys needed by Plans 02-05 so that no downstream plan references missing keys.

    Add the following keys to `en.json` (per UI-SPEC Copywriting Contract):
    ```json
    "tickets.empty.heading": "No new tickets",
    "tickets.empty.body": "Tickets assigned to you or your watched users will appear here. Configure your JQL in Settings to adjust the query.",
    "ignored.empty.heading": "No ignored tickets",
    "ignored.empty.body": "Tickets you mark as 'Not for me' will appear here.",
    "linked.empty.heading": "No linked tickets",
    "linked.empty.body": "Tickets already copied to your company Jira appear here.",
    "copy.progress.fetchingSource": "Reading source ticket",
    "copy.progress.copyingFields": "Creating ticket in Company Jira",
    "copy.progress.attachments": "Uploading attachments",
    "copy.progress.comments": "Copying comments",
    "copy.progress.done": "Ticket copied successfully",
    "detail.back": "Back to tickets",
    "detail.copy": "Copy to Company Jira",
    "detail.copied": "Copied",
    "detail.ignore": "Not for me",
    "detail.ignored": "Ignored",
    "ignored.restore": "Restore",
    "ignored.confirm.title": "Ignore ticket",
    "ignored.confirm.body": "Mark this ticket as 'not for me'? You can restore it from the Ignored tab at any time.",
    "ignored.confirm.action": "Ignore Ticket",
    "audit.back": "Back to App",
    "audit.empty.heading": "No API calls recorded yet",
    "audit.empty.body": "Fetch or copy a ticket to see activity.",
    "error.fetchFailed": "Could not fetch tickets. Check your connection in Settings and try again.",
    "error.copyFailed": "Copy did not complete. Check the audit log for details.",
    "settings.back": "Back to App"
    ```

    Update existing `"settings.back"` if it exists with value `"Back"` to `"Back to App"`.

    Add the corresponding Slovak translations to `sk.json`:
    ```json
    "tickets.empty.heading": "Ziadne nove tikety",
    "tickets.empty.body": "Tu sa zobrazia tikety priradene vam alebo vasim sledovanym pouzivatelom. Upravte JQL v Nastaveniach.",
    "ignored.empty.heading": "Ziadne ignorovane tikety",
    "ignored.empty.body": "Tu sa zobrazia tikety, ktore oznacite ako 'Nie moje'.",
    "linked.empty.heading": "Ziadne prepojene tikety",
    "linked.empty.body": "Tu sa zobrazia tikety skopirovane do firemnej Jiry.",
    "copy.progress.fetchingSource": "Citanie zdrojoveho tiketu",
    "copy.progress.copyingFields": "Vytvaranie tiketu vo firemnej Jire",
    "copy.progress.attachments": "Nahravanie priloh",
    "copy.progress.comments": "Kopirovanie komentarov",
    "copy.progress.done": "Tiket uspesne skopirovany",
    "detail.back": "Spat na tikety",
    "detail.copy": "Skopirovat do firemnej Jiry",
    "detail.copied": "Skopirovane",
    "detail.ignore": "Nie moj",
    "detail.ignored": "Ignorovane",
    "ignored.restore": "Obnovit",
    "ignored.confirm.title": "Ignorovat tiket",
    "ignored.confirm.body": "Oznacit tento tiket ako 'nie moj'? Mozete ho obnovit z karty Ignorovane kedykolvek.",
    "ignored.confirm.action": "Ignorovat tiket",
    "audit.back": "Spat do aplikacie",
    "audit.empty.heading": "Zatial ziadne API volania",
    "audit.empty.body": "Nacitajte alebo skopirujte tiket pre zobrazenie aktivity.",
    "error.fetchFailed": "Nepodarilo sa nacitat tikety. Skontrolujte pripojenie v Nastaveniach a skuste znova.",
    "error.copyFailed": "Kopirovanie sa nedokoncilo. Skontrolujte audit log pre detaily.",
    "settings.back": "Spat do aplikacie"
    ```

    Match the existing pattern in sk.json for diacritics usage.

    **Critical:** After all changes, verify `npm run build` succeeds and shadcn components render with correct brand colors, not default gray.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run build 2>&1 | tail -5 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - components.json exists at project root and contains `"style": "default"`
    - src/lib/utils.ts exists and contains `export function cn(`
    - src/components/ui/button.tsx exists
    - src/components/ui/card.tsx exists
    - src/components/ui/dialog.tsx exists
    - src/components/ui/skeleton.tsx exists
    - src/components/ui/progress.tsx exists
    - tsconfig.json contains `"@/*": ["./src/*"]`
    - vite.config.ts contains `"@": path.resolve`
    - src/index.css contains `--background: var(--color-brand-bg)` (or equivalent alias)
    - src/index.css still contains `--color-brand: #c02232`
    - body.dark section in src/index.css contains `--background: #161617`
    - package.json contains `lucide-react`
    - package.json contains `clsx` and `tailwind-merge`
    - src/i18n/locales/en.json contains `"tickets.empty.heading"`
    - src/i18n/locales/en.json contains `"detail.back": "Back to tickets"`
    - src/i18n/locales/en.json contains `"settings.back": "Back to App"`
    - src/i18n/locales/en.json contains `"audit.empty.heading"`
    - src/i18n/locales/sk.json contains `"tickets.empty.heading"`
    - src/i18n/locales/sk.json contains `"detail.back"`
    - `npm run build` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>shadcn/ui initialized with Tailwind v4, all 10 components installed, Lucide React installed, path alias configured, CSS tokens merged with brand values preserved, all new i18n keys added to both en.json and sk.json, build and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign AppShell with Lucide icons, shadcn Tooltip, and Linear-inspired styling</name>
  <files>src/components/ui/AppShell.tsx</files>
  <read_first>
    src/components/ui/AppShell.tsx, src/components/ui/tooltip.tsx, src/index.css,
    .planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-UI-SPEC.md
  </read_first>
  <action>
    Rewrite `src/components/ui/AppShell.tsx` to replace hand-coded SVGs with Lucide icons and use shadcn Tooltip for icon buttons. Preserve the existing `AppShellProps` interface exactly (all consumers unchanged).

    **Changes per UI-SPEC and D-04, D-06, D-15:**

    1. **Remove** inline `GearIcon` and `TerminalIcon` function components entirely.

    2. **Add imports:**
    ```typescript
    import { Settings, Terminal, ArrowLeft } from 'lucide-react';
    import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip';
    ```

    3. **Header** (per UI-SPEC Typography and D-06):
    - Brand wordmark: `text-xs font-semibold tracking-tight` (12px per UI-SPEC Typography — Label tier)
    - `<span className="text-brand">pm</span>kar` — preserved
    - Header padding: `px-5 py-2.5` (retained — 44px height per UI-SPEC Spacing exceptions)
    - Background: `bg-brand-surface border-b border-brand-border`
    - Brand accent gradient underline: `<div className="absolute bottom-0 left-0 right-0 h-[2px] bg-gradient-to-r from-brand via-brand/60 to-transparent" />` (already exists — retain)

    4. **Icon buttons** wrapped in `<Tooltip>`:
    ```tsx
    <TooltipProvider delayDuration={300}>
      {onAuditClick && (
        <Tooltip>
          <TooltipTrigger asChild>
            <button
              type="button"
              onClick={onAuditClick}
              aria-label={t('audit.open')}
              className="relative flex items-center justify-center w-7 h-7 text-brand-muted hover:text-brand-text rounded transition-colors duration-150 focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-offset-2"
            >
              <Terminal className="w-[15px] h-[15px]" aria-hidden="true" />
              {(auditCount ?? 0) > 0 && (
                <span className="absolute -top-0.5 -right-0.5 text-[9px] bg-brand text-white rounded-full min-w-[14px] h-[14px] flex items-center justify-center pointer-events-none">
                  {auditCount}
                </span>
              )}
            </button>
          </TooltipTrigger>
          <TooltipContent>{t('audit.open')}</TooltipContent>
        </Tooltip>
      )}
      {onGearClick && (
        <Tooltip>
          <TooltipTrigger asChild>
            <button
              type="button"
              onClick={onGearClick}
              aria-label={t('common.settings')}
              className="flex items-center justify-center w-7 h-7 text-brand-muted hover:text-brand-text rounded transition-colors duration-150 focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-offset-2"
            >
              <Settings className="w-[15px] h-[15px]" aria-hidden="true" />
            </button>
          </TooltipTrigger>
          <TooltipContent>{t('common.settings')}</TooltipContent>
        </Tooltip>
      )}
    </TooltipProvider>
    ```

    5. **Nav tabs** (per UI-SPEC Interaction Contracts — Navigation tabs):
    - Active tab: `text-brand-text font-semibold border-brand` (2px bottom border with brand color)
    - Inactive tab: `text-brand-muted font-normal border-transparent`
    - Transition: `transition-colors duration-150`
    - Tab nav height: `py-2` with `text-sm` (36px per UI-SPEC Spacing)
    - Add `focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-offset-2` to each tab button
    - Keep `aria-current="page"` on active tab

    6. **Content wrapper** — keep `flex-1 flex flex-col overflow-hidden` on the children container. This is critical for downstream list pages.

    7. **Retain** `useTranslation()` hook. All existing i18n keys (`nav.new`, `nav.notMine`, `nav.linked`, `audit.open`, `common.settings`) are preserved.

    8. **Do NOT change** the AppShellProps interface — it must remain backward compatible.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run build 2>&1 | tail -5 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/AppShell.tsx does NOT contain `function GearIcon`
    - src/components/ui/AppShell.tsx does NOT contain `function TerminalIcon`
    - src/components/ui/AppShell.tsx contains `import { Settings, Terminal` from `lucide-react`
    - src/components/ui/AppShell.tsx contains `TooltipProvider`
    - src/components/ui/AppShell.tsx contains `focus-visible:ring-2 focus-visible:ring-brand`
    - src/components/ui/AppShell.tsx contains `text-xs font-semibold tracking-tight` (brand wordmark)
    - src/components/ui/AppShell.tsx contains `transition-colors duration-150` (tab transitions)
    - src/components/ui/AppShell.tsx contains `useTranslation`
    - src/components/ui/AppShell.tsx exports `AppShell` function
    - `npm run build` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>AppShell redesigned with Lucide icons replacing all hand-coded SVGs, shadcn Tooltip wrapping icon buttons, Linear-inspired typography and spacing, focus-visible rings on interactive elements. Props interface preserved for backward compatibility.</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no TypeScript errors
- `npm run test` passes (existing tests should not break — AppShell has no dedicated test file)
- Brand tokens are present in index.css alongside shadcn semantic aliases
- All 10 shadcn components exist in src/components/ui/
- AppShell renders without hand-coded SVGs
- All new i18n keys present in both en.json and sk.json
</verification>

<success_criteria>
- shadcn/ui fully initialized with Tailwind v4 mode
- All 10 required shadcn components installed and importable
- Lucide React installed and used in AppShell
- CSS token merge preserves brand tokens and maps shadcn tokens to them
- Dark/light theme toggle still works
- AppShell header and nav tabs match UI-SPEC Linear-inspired spec
- All new i18n keys added to en.json and sk.json per UI-SPEC copywriting contract
- Build and tests pass clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-01-SUMMARY.md`
</output>
