---
phase: 08-fully-redesign-the-app-ui-modern-sleek-easy-to-use
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: [src/features/connections/SettingsPage.tsx]
autonomous: true
requirements: [UI-01]
gap_closure: true

must_haves:
  truths:
    - "SettingsPage.tsx contains zero hand-coded <svg> elements"
    - "Search, X, Sun, Moon, Monitor icons render from lucide-react"
  artifacts:
    - path: "src/features/connections/SettingsPage.tsx"
      provides: "Settings page with all Lucide icons"
      contains: "import { ArrowLeft, Search, X, Sun, Moon, Monitor } from 'lucide-react'"
  key_links:
    - from: "src/features/connections/SettingsPage.tsx"
      to: "lucide-react"
      via: "named imports"
      pattern: "import.*Search.*X.*Sun.*Moon.*Monitor.*from 'lucide-react'"
---

<objective>
Replace the 5 remaining hand-coded SVG icons in SettingsPage.tsx with their Lucide React equivalents.

Purpose: Close the verification gap on Success Criterion #1 ("All hand-coded SVG icons are replaced with Lucide React icons"). SettingsPage has 5 inline SVGs that should be Search, X, Sun, Moon, and Monitor from lucide-react.
Output: SettingsPage.tsx with zero hand-coded SVGs
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 5 hand-coded SVGs with Lucide icons in SettingsPage.tsx</name>
  <files>src/features/connections/SettingsPage.tsx</files>

  <read_first>
    src/features/connections/SettingsPage.tsx
  </read_first>

  <action>
    Update the lucide-react import on line 4 from:
    `import { ArrowLeft } from 'lucide-react';`
    to:
    `import { ArrowLeft, Search, X, Sun, Moon, Monitor } from 'lucide-react';`

    Then make these 5 replacements:

    1. SEARCH ICON (line ~379): Replace the entire `<svg width="13" height="13" viewBox="0 0 24 24" ...>` element (the search magnifier with circle+line) with:
       `<Search className="w-3.5 h-3.5 text-brand-muted flex-shrink-0" aria-hidden="true" />`

    2. X/CLOSE ICON (line ~452): Replace the entire `<svg width="13" height="13" viewBox="0 0 24 24" ...>` element (the X with two crossing lines) with:
       `<X className="w-3.5 h-3.5" />`

    3. SUN ICON (line ~558): In the ThemeSection THEME_OPTIONS array, replace the light theme `icon:` value (the SVG with circle+lines sun pattern) with:
       `icon: <Sun className="w-4 h-4" />`

    4. MOON ICON (line ~567): Replace the dark theme `icon:` value (the SVG with moon path) with:
       `icon: <Moon className="w-4 h-4" />`

    5. MONITOR ICON (line ~576): Replace the system theme `icon:` value (the SVG with rect+lines monitor) with:
       `icon: <Monitor className="w-4 h-4" />`

    Preserve all surrounding JSX, className props on parent elements, conditional styling, and existing behavior. Only the SVG elements themselves change to Lucide components.
  </action>

  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && grep -c '&lt;svg' src/features/connections/SettingsPage.tsx | grep -q '^0$' && grep -q "import { ArrowLeft, Search, X, Sun, Moon, Monitor } from 'lucide-react'" src/features/connections/SettingsPage.tsx && npm run build 2>&1 | tail -1</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '<svg' src/features/connections/SettingsPage.tsx` returns `0`
    - `grep 'lucide-react' src/features/connections/SettingsPage.tsx` contains `ArrowLeft, Search, X, Sun, Moon, Monitor`
    - `grep '<Search' src/features/connections/SettingsPage.tsx` returns a match
    - `grep '<X ' src/features/connections/SettingsPage.tsx` returns a match
    - `grep '<Sun' src/features/connections/SettingsPage.tsx` returns a match
    - `grep '<Moon' src/features/connections/SettingsPage.tsx` returns a match
    - `grep '<Monitor' src/features/connections/SettingsPage.tsx` returns a match
    - `npm run build` exits 0 with no errors
    - `npm run test` exits 0 (all 114 tests pass)
  </acceptance_criteria>

  <done>SettingsPage.tsx has zero hand-coded SVG elements. All 5 icons (Search, X, Sun, Moon, Monitor) render via lucide-react imports. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `grep -c '<svg' src/features/connections/SettingsPage.tsx` returns 0
- `grep -rn '<svg' src/features/ src/components/` returns no results in any phase-08-scoped files
- `npm run build` succeeds
- `npm run test` passes all tests
</verification>

<success_criteria>
Success Criterion #1 from ROADMAP ("All hand-coded SVG icons are replaced with Lucide React icons") is fully satisfied with zero remaining SVGs in any in-scope file.
</success_criteria>

<output>
After completion, create `.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-06-SUMMARY.md`
</output>
