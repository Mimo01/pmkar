---
phase: 08-fully-redesign-the-app-ui-modern-sleek-easy-to-use
plan: 05
type: execute
wave: 4
depends_on: ["08-02", "08-03", "08-04"]
files_modified:
  - src/features/tickets/AuditLogPage.tsx
  - src/features/connections/SetupWizard.tsx
  - src/i18n/locales/en.json
  - src/i18n/locales/sk.json
autonomous: false
requirements:
  - UI-07

must_haves:
  truths:
    - "Audit log page uses shadcn ScrollArea and has polished empty state"
    - "Setup wizard uses Lucide icons and updated button styling"
    - "All new UI strings have i18n keys in both en.json and sk.json"
    - "The full redesign passes visual inspection on both light and dark themes"
  artifacts:
    - path: "src/features/tickets/AuditLogPage.tsx"
      provides: "Polished audit log with shadcn components"
    - path: "src/i18n/locales/en.json"
      provides: "Complete English translations including new UI strings"
    - path: "src/i18n/locales/sk.json"
      provides: "Complete Slovak translations including new UI strings"
  key_links:
    - from: "src/features/tickets/AuditLogPage.tsx"
      to: "@/components/ui/scroll-area"
      via: "import ScrollArea"
      pattern: "import.*ScrollArea.*from.*scroll-area"
    - from: "src/i18n/locales/en.json"
      to: "src/features/tickets/TicketListPage.tsx"
      via: "i18n keys"
      pattern: "tickets.empty.heading"
---

<objective>
Polish remaining surfaces (AuditLogPage, SetupWizard), add all new i18n keys to both language packs, and verify the complete redesign visually.

Purpose: Final polish pass ensuring every surface matches the Linear-inspired spec and all i18n strings are complete. Visual checkpoint confirms the redesign before shipping.
Output: Polished AuditLogPage, SetupWizard, complete i18n, visual approval.
</objective>

<execution_context>
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Desktop/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-CONTEXT.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-UI-SPEC.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-RESEARCH.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-01-SUMMARY.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-02-SUMMARY.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-03-SUMMARY.md
@.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish AuditLogPage, SetupWizard, and add all new i18n keys</name>
  <files>
    src/features/tickets/AuditLogPage.tsx,
    src/features/connections/SetupWizard.tsx,
    src/i18n/locales/en.json,
    src/i18n/locales/sk.json
  </files>
  <read_first>
    src/features/tickets/AuditLogPage.tsx, src/features/connections/SetupWizard.tsx,
    src/features/connections/ConnectionCard.tsx,
    src/i18n/locales/en.json, src/i18n/locales/sk.json,
    .planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-UI-SPEC.md
  </read_first>
  <action>
    **AuditLogPage.tsx — Polish with shadcn components (per D-05):**

    1. **Add imports:**
    ```typescript
    import { ArrowLeft } from 'lucide-react';
    import { ScrollArea } from '@/components/ui/scroll-area';
    import { Badge } from '@/components/ui/badge';
    ```

    2. **Header** — use same back button pattern as SettingsPage and TicketDetailPage:
    ```tsx
    <div className="flex items-center gap-3 px-4 py-3 border-b border-brand-border bg-brand-surface">
      <button
        type="button"
        onClick={onClose}
        className="flex items-center gap-1 text-sm text-brand-muted hover:text-brand-text transition-colors duration-150 focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-offset-2 rounded"
      >
        <ArrowLeft className="w-4 h-4" aria-hidden="true" />
        {t('audit.back')}
      </button>
      <h1 className="text-base font-semibold text-brand-text">{t('audit.heading')}</h1>
    </div>
    ```

    3. **Wrap the audit entries list** in `<ScrollArea className="flex-1">` for consistent scrollbar styling.

    4. **Method badges** — use shadcn Badge for HTTP method:
    ```tsx
    <Badge variant="outline" className={cn("text-xs font-mono", methodColor(entry.method))}>
      {entry.method}
    </Badge>
    ```
    Where `methodColor` returns: GET = `text-green-600`, POST = `text-blue-600`, PUT = `text-yellow-600`, DELETE = `text-red-600`.

    5. **Status code** color: 2xx = green, 4xx = yellow, 5xx = red.

    6. **Empty state** (per UI-SPEC Copywriting):
    ```tsx
    <div className="flex flex-col items-center justify-center flex-1 py-16">
      <p className="text-sm font-semibold text-brand-text mb-1">{t('audit.empty.heading')}</p>
      <p className="text-xs text-brand-muted text-center max-w-sm">{t('audit.empty.body')}</p>
    </div>
    ```

    7. **Expandable rows** — keep existing expand/collapse logic but add `transition-colors duration-150` to row hover.

    8. **Replace** any inline SVG icons with Lucide equivalents (ChevronDown/ChevronUp for expand toggle).

    9. Add `focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-offset-2` to interactive elements.

    **SetupWizard.tsx — Light styling polish:**

    1. Replace any hand-coded SVG icons with Lucide equivalents.
    2. Add `focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-offset-2` to buttons and inputs.
    3. Update button styling to use consistent `bg-brand hover:bg-brand-light text-white font-semibold rounded-md px-4 py-2 text-sm transition-colors duration-150` pattern.
    4. Keep existing step logic, validation, and i18n usage unchanged.
    5. This is a light pass — the wizard structure stays the same.

    **i18n — Add all new keys to en.json and sk.json:**

    Add the following keys to `en.json` (per UI-SPEC Copywriting Contract):
    ```json
    "tickets.empty.heading": "No new tickets",
    "tickets.empty.body": "Tickets assigned to you or your watched users will appear here. Configure your JQL in Settings to adjust the query.",
    "ignored.empty.heading": "No ignored tickets",
    "ignored.empty.body": "Tickets you mark as 'Not for me' will appear here.",
    "linked.empty.heading": "No linked tickets",
    "linked.empty.body": "Tickets already copied to your company Jira appear here.",
    "copy.progress.fetchingSource": "Reading source ticket",
    "copy.progress.copyingFields": "Creating ticket in Company Jira",
    "copy.progress.attachments": "Uploading attachments",
    "copy.progress.comments": "Copying comments",
    "copy.progress.done": "Ticket copied successfully",
    "detail.back": "Back to tickets",
    "ignored.confirm.title": "Ignore ticket",
    "ignored.confirm.body": "Mark this ticket as 'not for me'? You can restore it from the Ignored tab at any time.",
    "ignored.confirm.action": "Ignore Ticket",
    "audit.empty.heading": "No API calls recorded yet",
    "audit.empty.body": "Fetch or copy a ticket to see activity.",
    "error.fetchFailed": "Could not fetch tickets. Check your connection in Settings and try again.",
    "error.copyFailed": "Copy did not complete. Check the audit log for details."
    ```

    Update existing key:
    - `"settings.back": "Back to App"` (was `"Back"` — per UI-SPEC Copywriting)

    Add the corresponding Slovak translations to `sk.json`:
    ```json
    "tickets.empty.heading": "Ziadne nove tikety",
    "tickets.empty.body": "Tu sa zobrazia tikety priradene vam alebo vasim sledovanym pouzivatelom. Upravte JQL v Nastaveniach.",
    "ignored.empty.heading": "Ziadne ignorovane tikety",
    "ignored.empty.body": "Tu sa zobrazia tikety, ktore oznacite ako 'Nie moje'.",
    "linked.empty.heading": "Ziadne prepojene tikety",
    "linked.empty.body": "Tu sa zobrazia tikety skopirovane do firemnej Jiry.",
    "copy.progress.fetchingSource": "Citanie zdrojoveho tiketu",
    "copy.progress.copyingFields": "Vytvaranie tiketu vo firemnej Jire",
    "copy.progress.attachments": "Nahravanie priloh",
    "copy.progress.comments": "Kopirovanie komentarov",
    "copy.progress.done": "Tiket uspesne skopirovany",
    "detail.back": "Spat na tikety",
    "ignored.confirm.title": "Ignorovat tiket",
    "ignored.confirm.body": "Oznacit tento tiket ako 'nie moj'? Mozete ho obnovit z karty Ignorovane kedykolvek.",
    "ignored.confirm.action": "Ignorovat tiket",
    "audit.empty.heading": "Zatial ziadne API volania",
    "audit.empty.body": "Nacitajte alebo skopirujte tiket pre zobrazenie aktivity.",
    "error.fetchFailed": "Nepodarilo sa nacitat tikety. Skontrolujte pripojenie v Nastaveniach a skuste znova.",
    "error.copyFailed": "Kopirovanie sa nedokoncilo. Skontrolujte audit log pre detaily."
    ```

    Update existing Slovak key:
    - `"settings.back": "Spat do aplikacie"` (was `"Spat"`)

    **Note:** Slovak translations use ASCII characters only (no diacritics in JSON keys). The translator should use proper Slovak diacritics (e.g., "Ziadne" should be "Ziadne" with proper caron/acute marks). However, match the existing pattern in sk.json — if existing Slovak strings use diacritics, use them here too. If they use ASCII approximations, match that.
  </action>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run build 2>&1 | tail -5 && npm run test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/features/tickets/AuditLogPage.tsx contains `import { ArrowLeft }` from `lucide-react`
    - src/features/tickets/AuditLogPage.tsx contains `import { ScrollArea }`
    - src/features/tickets/AuditLogPage.tsx contains `import { Badge }`
    - src/features/tickets/AuditLogPage.tsx contains `focus-visible:ring-2`
    - src/features/tickets/AuditLogPage.tsx contains `t('audit.empty.heading')`
    - src/i18n/locales/en.json contains `"tickets.empty.heading"`
    - src/i18n/locales/en.json contains `"detail.back": "Back to tickets"`
    - src/i18n/locales/en.json contains `"settings.back": "Back to App"`
    - src/i18n/locales/en.json contains `"ignored.confirm.title"`
    - src/i18n/locales/en.json contains `"audit.empty.heading"`
    - src/i18n/locales/sk.json contains `"tickets.empty.heading"`
    - src/i18n/locales/sk.json contains `"detail.back"`
    - src/i18n/locales/sk.json contains `"audit.empty.heading"`
    - `npm run build` exits 0
    - `npm run test` exits 0
  </acceptance_criteria>
  <done>AuditLogPage polished with shadcn ScrollArea, Badge, and Lucide icons. SetupWizard lightly polished. All new i18n keys added to both en.json and sk.json per UI-SPEC copywriting contract. Build and tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete UI redesign</name>
  <files>src/App.tsx</files>
  <action>
    Human visual verification of the complete UI redesign. Start the app with `npm run tauri dev` or `npm run dev` (for web preview) and verify all surfaces match the Linear-inspired minimal aesthetic from 08-UI-SPEC.md. Check light theme, dark theme, card layout, full-page detail, copy modals, settings sidebar, audit log, and empty states.
  </action>
  <what-built>
    Complete UI redesign across all app surfaces:
    1. AppShell header + nav tabs with Lucide icons and shadcn Tooltip
    2. Card-based ticket list replacing sortable table (all 3 tabs)
    3. Full-page ticket detail view replacing side panel
    4. Copy modals using shadcn Dialog with progress bar
    5. Settings page with sidebar nav layout
    6. Polished AuditLogPage and SetupWizard
    7. Skeleton loading states and polished empty states
  </what-built>
  <how-to-verify>
    Start the app with `npm run tauri dev` or `npm run dev` (for web preview).

    **1. Light theme verification:**
    - Header shows "pmkar" with red "pm" prefix, gear and terminal Lucide icons with tooltips on hover
    - Tab nav shows New / Not Mine / Already Linked with red underline on active tab
    - Click "Fetch Tickets" -- see 3 skeleton cards during loading
    - Ticket list shows compact 3-line cards (key+time, summary, status+priority+assignee)
    - Cards have subtle hover background, no shadows, flush with border dividers only
    - Click a ticket card -- full-page detail appears with back button "Back to tickets"
    - Detail page shows ticket summary, metadata, 5 tabs, copy/ignore buttons
    - Click back -- returns to same tab
    - Open Settings (gear icon) -- sidebar nav on left (160px), section content on right
    - Active section has red left border
    - Open Audit Log (terminal icon) -- polished with badges and scroll area
    - Empty states show heading + descriptive body text

    **2. Dark theme verification (toggle in Settings > Theme):**
    - All surfaces use dark brand tokens
    - Cards, header, settings sidebar all have correct dark backgrounds
    - Text is light on dark, borders are subtle
    - Accent red remains consistent

    **3. Copy workflow:**
    - Click a ticket > "Copy to Company Jira" > Preview modal appears as centered dialog
    - Confirm copy > Progress bar visible during copy
    - Result modal shows step results with check/X icons

    **4. Not Mine / Already Linked tabs:**
    - Not Mine tab shows ignored tickets with Restore action on each card
    - Already Linked tab shows copied tickets with linked key badge

    Expected: All surfaces match the Linear-inspired minimal aesthetic. No old table layouts visible. No old side panels. No hand-coded SVG icons.
  </how-to-verify>
  <verify>
    <automated>cd /Users/mimo/Desktop/pmkar && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually approved the complete UI redesign on both light and dark themes.</done>
  <resume-signal>Type "approved" or describe any issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npm run test` passes
- All surfaces visually match UI-SPEC
- Light and dark themes both correct
- i18n keys complete in both languages
</verification>

<success_criteria>
- All app surfaces match the Linear-inspired minimal aesthetic from UI-SPEC
- No old table layouts remain visible
- No old side panels remain visible
- No hand-coded SVG icons remain
- Dark and light themes both correct
- All i18n keys present in both en.json and sk.json
- User visually approves the redesign
</success_criteria>

<output>
After completion, create `.planning/phases/08-fully-redesign-the-app-ui-modern-sleek-easy-to-use/08-05-SUMMARY.md`
</output>
