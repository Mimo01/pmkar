---
phase: 20-renderer-registry-field-type-aware-controls
plan: 03
type: execute
wave: 1
depends_on: [20-01]
files_modified:
  - src/features/field-renderers/renderers/StringRenderer.tsx
  - src/features/field-renderers/renderers/TextAreaRenderer.tsx
  - src/features/field-renderers/renderers/UrlRenderer.tsx
  - src/features/field-renderers/renderers/DateRenderer.tsx
  - src/features/field-renderers/renderers/DateTimeRenderer.tsx
  - src/features/field-renderers/renderers/NumberRenderer.tsx
  - src/features/field-renderers/renderers/CheckboxRenderer.tsx
  - src/features/field-renderers/renderers/RadioRenderer.tsx
  - src/features/field-renderers/renderers/UnsupportedTypeRenderer.tsx
  - src/features/field-renderers/__tests__/StringRenderer.test.tsx
  - src/features/field-renderers/__tests__/TextAreaRenderer.test.tsx
  - src/features/field-renderers/__tests__/UrlRenderer.test.tsx
  - src/features/field-renderers/__tests__/CheckboxRenderer.test.tsx
  - src/features/field-renderers/__tests__/RadioRenderer.test.tsx
  - src/features/field-renderers/__tests__/UnsupportedTypeRenderer.test.tsx
autonomous: true
requirements: [CTRL-01, CTRL-05, CTRL-06, CTRL-07]
tags: [react-19, controlled-inputs, native-html, badge, i18n]

must_haves:
  truths:
    - "User sees an <input type=text> for plain string fields (CTRL-01)"
    - "User sees a <textarea> for description / multi-line string fields (CTRL-01)"
    - "User sees an <input type=url> for URL fields (CTRL-01)"
    - "User sees an <input type=date> for date fields (CTRL-05)"
    - "User sees an <input type=datetime-local> for datetime fields (CTRL-05)"
    - "User sees an <input type=number> for number fields (CTRL-05)"
    - "User sees a checkbox group for array-of-string fields when needed (CTRL-06)"
    - "User sees a radio group for radio-style fields (CTRL-06)"
    - "User sees a read-only Badge pill for unsupported field types — never an editable text input (CTRL-07)"
  artifacts:
    - path: "src/features/field-renderers/renderers/StringRenderer.tsx"
      provides: "StringRenderer — single-line text input"
      exports: ["StringRenderer"]
    - path: "src/features/field-renderers/renderers/TextAreaRenderer.tsx"
      provides: "TextAreaRenderer — multi-line textarea"
      exports: ["TextAreaRenderer"]
    - path: "src/features/field-renderers/renderers/UrlRenderer.tsx"
      provides: "UrlRenderer — URL input with HTML5 pattern hint"
      exports: ["UrlRenderer"]
    - path: "src/features/field-renderers/renderers/DateRenderer.tsx"
      provides: "DateRenderer — native date input"
      exports: ["DateRenderer"]
    - path: "src/features/field-renderers/renderers/DateTimeRenderer.tsx"
      provides: "DateTimeRenderer — native datetime-local input"
      exports: ["DateTimeRenderer"]
    - path: "src/features/field-renderers/renderers/NumberRenderer.tsx"
      provides: "NumberRenderer — native number input"
      exports: ["NumberRenderer"]
    - path: "src/features/field-renderers/renderers/CheckboxRenderer.tsx"
      provides: "CheckboxRenderer — checkbox group for array-of-string"
      exports: ["CheckboxRenderer"]
    - path: "src/features/field-renderers/renderers/RadioRenderer.tsx"
      provides: "RadioRenderer — radio group for single string selection"
      exports: ["RadioRenderer"]
    - path: "src/features/field-renderers/renderers/UnsupportedTypeRenderer.tsx"
      provides: "UnsupportedTypeRenderer — read-only Badge pill"
      exports: ["UnsupportedTypeRenderer"]
  key_links:
    - from: "src/features/field-renderers/renderers/UnsupportedTypeRenderer.tsx"
      to: "src/components/ui/badge"
      via: "import { Badge } from '@/components/ui/badge'"
      pattern: "from '@/components/ui/badge'"
    - from: "all renderers"
      to: "src/features/field-renderers/types.ts"
      via: "import type { RendererProps } from '../types'"
      pattern: "from '../types'"
---

<objective>
Build the 9 simple renderers that do NOT depend on VirtualizedCombobox: 6 native HTML inputs (String, TextArea, Url, Date, DateTime, Number), 2 multi-input controls (Checkbox, Radio), and the read-only UnsupportedTypeRenderer pill. Replace it.todo stubs with real assertions for each.

Purpose: Closes CTRL-01, CTRL-05, CTRL-06, CTRL-07 (the file-creation half of CTRL-07; registry routing closes the rest in Plan 05). All renderers are pure controlled inputs with no internal state, no Zustand, no Tauri.
Output: 9 renderer files + 6 updated test files.
</objective>

<execution_context>
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mimo/Documents/Projects/pmkar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-renderer-registry-field-type-aware-controls/20-CONTEXT.md
@.planning/phases/20-renderer-registry-field-type-aware-controls/20-PATTERNS.md
@.planning/phases/20-renderer-registry-field-type-aware-controls/20-UI-SPEC.md
@.planning/phases/20-renderer-registry-field-type-aware-controls/20-01-SUMMARY.md
@src/features/field-renderers/types.ts
@src/features/tickets/CopyPreviewModal.tsx
@src/components/ui/badge.tsx
@src/lib/utils.ts

<interfaces>
<!-- All 9 renderers consume RendererProps (Plan 01) -->

From src/features/field-renderers/types.ts:
```typescript
export interface RendererProps {
  field: FieldSchema;
  value: unknown;
  onChange: (v: unknown) => void;
  required?: boolean;
  disabled?: boolean;
  onSearch?: (q: string) => Promise<JiraUser[]>;  // unused in Plan 03
  initialQuery?: string;                          // unused in Plan 03
}
```

All renderers in this plan share the same export contract:
```typescript
export function {RendererName}(props: RendererProps): JSX.Element;
```

Shared input className (StringRenderer / UrlRenderer / DateRenderer / DateTimeRenderer / NumberRenderer):
```
"flex h-9 w-full rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring disabled:opacity-50 disabled:pointer-events-none"
```

Textarea className (TextAreaRenderer):
```
"flex min-h-[80px] w-full rounded-md border border-input bg-transparent px-3 py-2 text-sm shadow-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring disabled:opacity-50 disabled:pointer-events-none resize-y"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the 6 native-input renderers (String, TextArea, Url, Date, DateTime, Number) + replace 3 test stubs</name>
  <read_first>
    - src/features/field-renderers/types.ts (RendererProps contract)
    - src/features/tickets/CopyPreviewModal.tsx (lines 249-265: text input pattern; lines 331-345: textarea pattern)
    - .planning/phases/20-renderer-registry-field-type-aware-controls/20-PATTERNS.md (sections "StringRenderer", "TextAreaRenderer", "UrlRenderer", "DateRenderer", "DateTimeRenderer", "NumberRenderer", "Controlled-input value cast pattern")
    - .planning/phases/20-renderer-registry-field-type-aware-controls/20-UI-SPEC.md (Component Inventory: StringRenderer / TextAreaRenderer / UrlRenderer / DateRenderer / DateTimeRenderer / NumberRenderer — exact classes)
    - src/features/field-renderers/__tests__/StringRenderer.test.tsx (it.todo stub from Plan 01)
    - src/features/field-renderers/__tests__/TextAreaRenderer.test.tsx (it.todo stub)
    - src/features/field-renderers/__tests__/UrlRenderer.test.tsx (it.todo stub)
  </read_first>
  <behavior>
    - StringRenderer: input[type=text], controlled by `value` (cast string|''), onChange(string)
    - TextAreaRenderer: textarea, controlled, onChange(string), `min-h-[80px]` class, `resize-y`
    - UrlRenderer: input[type=url] with `pattern="https?://.*"` and `inputMode="url"`
    - DateRenderer: input[type=date], value cast to string '' if not string
    - DateTimeRenderer: input[type=datetime-local]
    - NumberRenderer: input[type=number], `step="any"` for decimals, onChange(Number(e.target.value)) when input non-empty, onChange(null) when empty
    - All renderers: aria-required={required}, disabled={disabled}, render `id={field.fieldId}` for label association by DynamicTargetForm
  </behavior>
  <files>
src/features/field-renderers/renderers/StringRenderer.tsx,
src/features/field-renderers/renderers/TextAreaRenderer.tsx,
src/features/field-renderers/renderers/UrlRenderer.tsx,
src/features/field-renderers/renderers/DateRenderer.tsx,
src/features/field-renderers/renderers/DateTimeRenderer.tsx,
src/features/field-renderers/renderers/NumberRenderer.tsx,
src/features/field-renderers/__tests__/StringRenderer.test.tsx,
src/features/field-renderers/__tests__/TextAreaRenderer.test.tsx,
src/features/field-renderers/__tests__/UrlRenderer.test.tsx
  </files>
  <action>
**File: `src/features/field-renderers/renderers/StringRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

export function StringRenderer({ field, value, onChange, required, disabled }: RendererProps) {
  const strValue = typeof value === 'string' ? value : '';
  return (
    <input
      id={field.fieldId}
      type="text"
      value={strValue}
      onChange={(e) => onChange(e.target.value)}
      disabled={disabled}
      aria-required={required}
      className={cn(
        'flex h-9 w-full rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm',
        'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring',
        'disabled:opacity-50 disabled:pointer-events-none',
      )}
    />
  );
}
```

**File: `src/features/field-renderers/renderers/TextAreaRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

export function TextAreaRenderer({ field, value, onChange, required, disabled }: RendererProps) {
  const strValue = typeof value === 'string' ? value : '';
  return (
    <textarea
      id={field.fieldId}
      value={strValue}
      onChange={(e) => onChange(e.target.value)}
      disabled={disabled}
      aria-required={required}
      rows={4}
      className={cn(
        'flex min-h-[80px] w-full rounded-md border border-input bg-transparent px-3 py-2 text-sm shadow-sm',
        'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring',
        'disabled:opacity-50 disabled:pointer-events-none resize-y',
      )}
    />
  );
}
```

**File: `src/features/field-renderers/renderers/UrlRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

export function UrlRenderer({ field, value, onChange, required, disabled }: RendererProps) {
  const strValue = typeof value === 'string' ? value : '';
  return (
    <input
      id={field.fieldId}
      type="url"
      inputMode="url"
      // HTML5 hint only — no blocking validation in Phase 20 (UI-SPEC: Component Inventory > UrlRenderer)
      pattern="https?://.*"
      value={strValue}
      onChange={(e) => onChange(e.target.value)}
      disabled={disabled}
      aria-required={required}
      className={cn(
        'flex h-9 w-full rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm',
        'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring',
        'disabled:opacity-50 disabled:pointer-events-none',
      )}
    />
  );
}
```

**File: `src/features/field-renderers/renderers/DateRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

export function DateRenderer({ field, value, onChange, required, disabled }: RendererProps) {
  const strValue = typeof value === 'string' ? value : '';
  return (
    <input
      id={field.fieldId}
      type="date"
      value={strValue}
      onChange={(e) => onChange(e.target.value)}
      disabled={disabled}
      aria-required={required}
      className={cn(
        'flex h-9 w-full rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm',
        'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring',
        'disabled:opacity-50 disabled:pointer-events-none',
      )}
    />
  );
}
```

**File: `src/features/field-renderers/renderers/DateTimeRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

export function DateTimeRenderer({ field, value, onChange, required, disabled }: RendererProps) {
  const strValue = typeof value === 'string' ? value : '';
  return (
    <input
      id={field.fieldId}
      type="datetime-local"
      value={strValue}
      onChange={(e) => onChange(e.target.value)}
      disabled={disabled}
      aria-required={required}
      className={cn(
        'flex h-9 w-full rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm',
        'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring',
        'disabled:opacity-50 disabled:pointer-events-none',
      )}
    />
  );
}
```

**File: `src/features/field-renderers/renderers/NumberRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

export function NumberRenderer({ field, value, onChange, required, disabled }: RendererProps) {
  const strValue =
    typeof value === 'number' && Number.isFinite(value)
      ? String(value)
      : typeof value === 'string'
        ? value
        : '';
  return (
    <input
      id={field.fieldId}
      type="number"
      step="any"
      value={strValue}
      onChange={(e) => {
        const raw = e.target.value;
        if (raw === '') {
          onChange(null);
          return;
        }
        const parsed = Number(raw);
        // If parse fails (NaN), pass the raw string upward — Phase 22 validates
        onChange(Number.isFinite(parsed) ? parsed : raw);
      }}
      disabled={disabled}
      aria-required={required}
      className={cn(
        'flex h-9 w-full rounded-md border border-input bg-transparent px-3 py-1 text-sm shadow-sm',
        'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring',
        'disabled:opacity-50 disabled:pointer-events-none',
      )}
    />
  );
}
```

**Test files** — replace it.todo stubs in StringRenderer.test.tsx, TextAreaRenderer.test.tsx, UrlRenderer.test.tsx with real assertions. Pattern (apply identically, varying renderer + type):

```typescript
import { fireEvent, render, screen } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';

import { StringRenderer } from '../renderers/StringRenderer';
import type { RendererProps } from '../types';

const baseField: RendererProps['field'] = {
  fieldId: 'summary',
  name: 'Summary',
  required: false,
  schema: { type: 'string' },
};

describe('StringRenderer', () => {
  it('CTRL-01 renders an input[type=text] with the current string value', () => {
    render(<StringRenderer field={baseField} value="Hello" onChange={vi.fn()} />);
    const input = screen.getByRole('textbox') as HTMLInputElement;
    expect(input.type).toBe('text');
    expect(input.value).toBe('Hello');
  });

  it('CTRL-01 calls onChange with new string when user types', () => {
    const onChange = vi.fn();
    render(<StringRenderer field={baseField} value="" onChange={onChange} />);
    fireEvent.change(screen.getByRole('textbox'), { target: { value: 'updated' } });
    expect(onChange).toHaveBeenCalledWith('updated');
  });

  it('CTRL-01 sets aria-required when required prop is true', () => {
    render(<StringRenderer field={baseField} value="" onChange={vi.fn()} required />);
    expect(screen.getByRole('textbox')).toHaveAttribute('aria-required', 'true');
  });

  it('CTRL-01 disables input when disabled prop is true', () => {
    render(<StringRenderer field={baseField} value="" onChange={vi.fn()} disabled />);
    expect(screen.getByRole('textbox')).toBeDisabled();
  });
});
```

Apply the same pattern to TextAreaRenderer (use `screen.getByRole('textbox')` since textarea has role textbox; assert min-h-[80px] in className via `expect(textarea.className).toContain('min-h-[80px]')`) and UrlRenderer (assert `input.type === 'url'` and `input.pattern === 'https?://.*'`).

Note: jsdom does NOT validate `pattern` attribute server-side; we only need to verify the attribute is present in the DOM, not that it blocks input.

After all 6 renderer files exist, run `npm test -- src/features/field-renderers` and confirm the StringRenderer / TextAreaRenderer / UrlRenderer tests pass while the rest stay in it.todo state.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck</automated>
    <automated>npm test -- src/features/field-renderers/__tests__/StringRenderer.test.tsx --reporter=verbose</automated>
    <automated>npm test -- src/features/field-renderers/__tests__/TextAreaRenderer.test.tsx --reporter=verbose</automated>
    <automated>npm test -- src/features/field-renderers/__tests__/UrlRenderer.test.tsx --reporter=verbose</automated>
    <automated>grep -c "export function StringRenderer" src/features/field-renderers/renderers/StringRenderer.tsx</automated>
    <automated>grep -c "export function TextAreaRenderer" src/features/field-renderers/renderers/TextAreaRenderer.tsx</automated>
    <automated>grep -c "type=\"url\"" src/features/field-renderers/renderers/UrlRenderer.tsx</automated>
    <automated>grep -c 'type="date"' src/features/field-renderers/renderers/DateRenderer.tsx</automated>
    <automated>grep -c 'type="datetime-local"' src/features/field-renderers/renderers/DateTimeRenderer.tsx</automated>
    <automated>grep -c 'type="number"' src/features/field-renderers/renderers/NumberRenderer.tsx</automated>
    <automated>grep -c "min-h-\[80px\]" src/features/field-renderers/renderers/TextAreaRenderer.tsx</automated>
    <automated>grep -c 'pattern="https?://\.\*"' src/features/field-renderers/renderers/UrlRenderer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 6 renderer files exist and compile (`npx tsc --noEmit` exits 0)
    - Each renderer has named export `export function {Name}`
    - StringRenderer renders `<input type="text">`; UrlRenderer renders `<input type="url">` with `pattern="https?://.*"`; TextAreaRenderer renders `<textarea>` with `min-h-[80px]`; DateRenderer `<input type="date">`; DateTimeRenderer `<input type="datetime-local">`; NumberRenderer `<input type="number" step="any">`
    - Each renderer is fully controlled (no useState; value flows in via props, onChange flows out)
    - Each renderer sets `id={field.fieldId}`, `aria-required={required}`, `disabled={disabled}`
    - StringRenderer.test.tsx, TextAreaRenderer.test.tsx, UrlRenderer.test.tsx have zero `it.todo` and all tests pass
    - NumberRenderer's onChange passes a `number` for valid numeric input, `null` for empty input
    - No file imports `@tauri-apps/api/core` (D-01)
    - No `dangerouslySetInnerHTML` anywhere (XSS mitigation)
  </acceptance_criteria>
  <done>
    Six native-input renderers + their three test files complete; CTRL-01 (text/multi-line/URL) and CTRL-05 (date/datetime/number) covered.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CheckboxRenderer + RadioRenderer + UnsupportedTypeRenderer + replace 3 test stubs</name>
  <read_first>
    - src/features/field-renderers/types.ts (RendererProps contract)
    - src/features/tickets/CopyPreviewModal.tsx (lines 311-328: checkbox group pattern; lines 270-286: select pattern that informs RadioRenderer)
    - .planning/phases/20-renderer-registry-field-type-aware-controls/20-PATTERNS.md (sections "CheckboxRenderer", "RadioRenderer", "UnsupportedTypeRenderer")
    - .planning/phases/20-renderer-registry-field-type-aware-controls/20-UI-SPEC.md (Component Inventory: CheckboxRenderer / RadioRenderer / UnsupportedTypeRenderer)
    - .planning/phases/20-renderer-registry-field-type-aware-controls/20-RESEARCH.md ("UnsupportedTypeRenderer Badge" code example)
    - src/components/ui/badge.tsx (full file — Badge variants, cn() pattern)
    - src/features/field-renderers/__tests__/CheckboxRenderer.test.tsx (it.todo stub)
    - src/features/field-renderers/__tests__/RadioRenderer.test.tsx (it.todo stub)
    - src/features/field-renderers/__tests__/UnsupportedTypeRenderer.test.tsx (it.todo stub)
  </read_first>
  <behavior>
    - CheckboxRenderer: renders one `<input type=checkbox>` per `field.allowedValues` entry; value is `string[]` of checked option values; onChange propagates updated array
    - RadioRenderer: renders `<div role="radiogroup">` containing one `<input type=radio>` per allowedValues; only one checked (matching `value as string`); onChange propagates the option string
    - UnsupportedTypeRenderer: renders `<Badge variant="outline" role="status">` with AlertCircle icon + label "Unsupported: {type}"; no onChange, never crashes for any FieldSchemaType
  </behavior>
  <files>
src/features/field-renderers/renderers/CheckboxRenderer.tsx,
src/features/field-renderers/renderers/RadioRenderer.tsx,
src/features/field-renderers/renderers/UnsupportedTypeRenderer.tsx,
src/features/field-renderers/__tests__/CheckboxRenderer.test.tsx,
src/features/field-renderers/__tests__/RadioRenderer.test.tsx,
src/features/field-renderers/__tests__/UnsupportedTypeRenderer.test.tsx
  </files>
  <action>
**File: `src/features/field-renderers/renderers/CheckboxRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

interface AllowedValue {
  id?: string;
  value?: string;
  name?: string;
}

function getOptionLabel(opt: unknown): string {
  if (typeof opt === 'string') return opt;
  if (opt && typeof opt === 'object') {
    const o = opt as AllowedValue;
    return o.value ?? o.name ?? o.id ?? '';
  }
  return '';
}

export function CheckboxRenderer({ field, value, onChange, disabled }: RendererProps) {
  const allowed = Array.isArray(field.allowedValues) ? field.allowedValues : [];
  const selected = Array.isArray(value) ? (value as string[]) : [];
  const labelId = `${field.fieldId}-label`;

  function toggle(optLabel: string, isOn: boolean) {
    if (isOn) {
      onChange([...selected, optLabel]);
    } else {
      onChange(selected.filter((v) => v !== optLabel));
    }
  }

  return (
    <div role="group" aria-labelledby={labelId} className={cn('flex flex-col gap-1.5')}>
      {/* labelId is referenced by the field label rendered by DynamicTargetForm via the same id */}
      {allowed.map((opt, idx) => {
        const optLabel = getOptionLabel(opt);
        const checkboxId = `${field.fieldId}-${idx}`;
        const checked = selected.includes(optLabel);
        return (
          <label key={`${optLabel}-${idx}`} htmlFor={checkboxId} className="flex items-center gap-2 text-sm">
            <input
              id={checkboxId}
              type="checkbox"
              checked={checked}
              disabled={disabled}
              onChange={(e) => toggle(optLabel, e.target.checked)}
              className="h-4 w-4 rounded border border-input"
            />
            <span>{optLabel}</span>
          </label>
        );
      })}
    </div>
  );
}
```

**File: `src/features/field-renderers/renderers/RadioRenderer.tsx`**

```typescript
import type { RendererProps } from '../types';
import { cn } from '@/lib/utils';

interface AllowedValue {
  id?: string;
  value?: string;
  name?: string;
}

function getOptionLabel(opt: unknown): string {
  if (typeof opt === 'string') return opt;
  if (opt && typeof opt === 'object') {
    const o = opt as AllowedValue;
    return o.value ?? o.name ?? o.id ?? '';
  }
  return '';
}

export function RadioRenderer({ field, value, onChange, required, disabled }: RendererProps) {
  const allowed = Array.isArray(field.allowedValues) ? field.allowedValues : [];
  const current = typeof value === 'string' ? value : '';
  const labelId = `${field.fieldId}-label`;

  return (
    <div
      role="radiogroup"
      aria-labelledby={labelId}
      aria-required={required}
      className={cn('flex flex-col gap-1.5')}
    >
      {allowed.map((opt, idx) => {
        const optLabel = getOptionLabel(opt);
        const radioId = `${field.fieldId}-${idx}`;
        return (
          <label key={`${optLabel}-${idx}`} htmlFor={radioId} className="flex items-center gap-2 text-sm">
            <input
              id={radioId}
              type="radio"
              name={field.fieldId}
              value={optLabel}
              checked={current === optLabel}
              disabled={disabled}
              onChange={() => onChange(optLabel)}
              className="h-4 w-4 border border-input"
            />
            <span>{optLabel}</span>
          </label>
        );
      })}
    </div>
  );
}
```

**File: `src/features/field-renderers/renderers/UnsupportedTypeRenderer.tsx`**

```typescript
import { AlertCircle } from 'lucide-react';
import { useTranslation } from 'react-i18next';
import { Badge } from '@/components/ui/badge';
import { cn } from '@/lib/utils';
import type { RendererProps } from '../types';

export function UnsupportedTypeRenderer({ field }: RendererProps) {
  const { t } = useTranslation();
  const typeStr = field.schema.type;
  return (
    <Badge
      variant="outline"
      role="status"
      aria-label={`Unsupported field type: ${typeStr}`}
      className={cn('text-xs font-medium text-muted-foreground gap-1 inline-flex items-center')}
    >
      <AlertCircle className="w-3 h-3" aria-hidden="true" />
      <span>{t('fieldRenderer.unsupportedType', { type: typeStr, defaultValue: `Unsupported: ${typeStr}` })}</span>
    </Badge>
  );
}
```

**Test: `src/features/field-renderers/__tests__/CheckboxRenderer.test.tsx`**

```typescript
import { fireEvent, render, screen } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';

import { CheckboxRenderer } from '../renderers/CheckboxRenderer';
import type { RendererProps } from '../types';

const field: RendererProps['field'] = {
  fieldId: 'flags',
  name: 'Flags',
  required: false,
  schema: { type: 'array', items: 'string' },
  allowedValues: ['alpha', 'beta', 'gamma'],
};

describe('CheckboxRenderer', () => {
  it('CTRL-06 renders one input[type=checkbox] per allowedValues entry', () => {
    render(<CheckboxRenderer field={field} value={[]} onChange={vi.fn()} />);
    expect(screen.getAllByRole('checkbox')).toHaveLength(3);
  });

  it('CTRL-06 each checkbox checked when value array includes its option', () => {
    render(<CheckboxRenderer field={field} value={['beta']} onChange={vi.fn()} />);
    const boxes = screen.getAllByRole('checkbox') as HTMLInputElement[];
    expect(boxes[0].checked).toBe(false);
    expect(boxes[1].checked).toBe(true);
    expect(boxes[2].checked).toBe(false);
  });

  it('CTRL-06 calls onChange with appended array when checkbox toggled on', () => {
    const onChange = vi.fn();
    render(<CheckboxRenderer field={field} value={['alpha']} onChange={onChange} />);
    fireEvent.click(screen.getAllByRole('checkbox')[1]);
    expect(onChange).toHaveBeenCalledWith(['alpha', 'beta']);
  });

  it('CTRL-06 calls onChange with filtered array when checkbox toggled off', () => {
    const onChange = vi.fn();
    render(<CheckboxRenderer field={field} value={['alpha', 'beta']} onChange={onChange} />);
    fireEvent.click(screen.getAllByRole('checkbox')[0]);
    expect(onChange).toHaveBeenCalledWith(['beta']);
  });

  it('CTRL-06 group has role="group" with aria-labelledby pointing to field name', () => {
    render(<CheckboxRenderer field={field} value={[]} onChange={vi.fn()} />);
    const group = screen.getByRole('group');
    expect(group).toHaveAttribute('aria-labelledby', 'flags-label');
  });
});
```

**Test: `src/features/field-renderers/__tests__/RadioRenderer.test.tsx`**

```typescript
import { fireEvent, render, screen } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';

import { RadioRenderer } from '../renderers/RadioRenderer';
import type { RendererProps } from '../types';

const field: RendererProps['field'] = {
  fieldId: 'choice',
  name: 'Choice',
  required: true,
  schema: { type: 'option' },
  allowedValues: [{ value: 'one' }, { value: 'two' }, { value: 'three' }],
};

describe('RadioRenderer', () => {
  it('CTRL-06 renders div with role="radiogroup" and aria-labelledby', () => {
    render(<RadioRenderer field={field} value="" onChange={vi.fn()} />);
    expect(screen.getByRole('radiogroup')).toHaveAttribute('aria-labelledby', 'choice-label');
  });

  it('CTRL-06 renders one input[type=radio] per allowedValues entry', () => {
    render(<RadioRenderer field={field} value="" onChange={vi.fn()} />);
    expect(screen.getAllByRole('radio')).toHaveLength(3);
  });

  it('CTRL-06 only one radio is checked at a time (matching value)', () => {
    render(<RadioRenderer field={field} value="two" onChange={vi.fn()} />);
    const radios = screen.getAllByRole('radio') as HTMLInputElement[];
    expect(radios.filter((r) => r.checked)).toHaveLength(1);
    expect(radios[1].checked).toBe(true);
  });

  it('CTRL-06 calls onChange with selected option string when radio clicked', () => {
    const onChange = vi.fn();
    render(<RadioRenderer field={field} value="" onChange={onChange} />);
    fireEvent.click(screen.getAllByRole('radio')[2]);
    expect(onChange).toHaveBeenCalledWith('three');
  });
});
```

**Test: `src/features/field-renderers/__tests__/UnsupportedTypeRenderer.test.tsx`**

```typescript
import { render, screen } from '@testing-library/react';
import { describe, expect, it, vi } from 'vitest';

import { UnsupportedTypeRenderer } from '../renderers/UnsupportedTypeRenderer';
import type { RendererProps } from '../types';

function makeField(type: string): RendererProps['field'] {
  return {
    fieldId: 'unknown',
    name: 'Unknown Field',
    required: false,
    // Cast to any to allow exotic types in tests; production uses FieldSchemaType union
    schema: { type } as RendererProps['field']['schema'],
  };
}

describe('UnsupportedTypeRenderer', () => {
  it('CTRL-07 renders read-only Badge with role="status"', () => {
    render(<UnsupportedTypeRenderer field={makeField('any')} value={null} onChange={vi.fn()} />);
    expect(screen.getByRole('status')).toBeInTheDocument();
  });

  it('CTRL-07 displays field.schema.type in label text for debugging', () => {
    render(<UnsupportedTypeRenderer field={makeField('issuetype')} value={null} onChange={vi.fn()} />);
    expect(screen.getByRole('status').textContent).toMatch(/issuetype/);
  });

  it('CTRL-07 has no interactive input element', () => {
    render(<UnsupportedTypeRenderer field={makeField('any')} value={null} onChange={vi.fn()} />);
    expect(screen.queryByRole('textbox')).not.toBeInTheDocument();
    expect(screen.queryByRole('combobox')).not.toBeInTheDocument();
  });

  it('CTRL-07 does not crash when given an unknown FieldSchemaType', () => {
    expect(() =>
      render(<UnsupportedTypeRenderer field={makeField('completely-unknown-type')} value={null} onChange={vi.fn()} />),
    ).not.toThrow();
  });

  it('CTRL-07 aria-label includes "Unsupported field type:" prefix', () => {
    render(<UnsupportedTypeRenderer field={makeField('priority')} value={null} onChange={vi.fn()} />);
    const status = screen.getByRole('status');
    expect(status).toHaveAttribute('aria-label', expect.stringMatching(/Unsupported field type:/));
  });
});
```

After all files exist, run the targeted suite:
```
npm test -- src/features/field-renderers/__tests__/CheckboxRenderer.test.tsx src/features/field-renderers/__tests__/RadioRenderer.test.tsx src/features/field-renderers/__tests__/UnsupportedTypeRenderer.test.tsx
```
All assertions must pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck</automated>
    <automated>npm test -- src/features/field-renderers/__tests__/CheckboxRenderer.test.tsx --reporter=verbose</automated>
    <automated>npm test -- src/features/field-renderers/__tests__/RadioRenderer.test.tsx --reporter=verbose</automated>
    <automated>npm test -- src/features/field-renderers/__tests__/UnsupportedTypeRenderer.test.tsx --reporter=verbose</automated>
    <automated>grep -c "export function CheckboxRenderer" src/features/field-renderers/renderers/CheckboxRenderer.tsx</automated>
    <automated>grep -c "export function RadioRenderer" src/features/field-renderers/renderers/RadioRenderer.tsx</automated>
    <automated>grep -c "export function UnsupportedTypeRenderer" src/features/field-renderers/renderers/UnsupportedTypeRenderer.tsx</automated>
    <automated>grep -c 'role="status"' src/features/field-renderers/renderers/UnsupportedTypeRenderer.tsx</automated>
    <automated>grep -c 'role="radiogroup"' src/features/field-renderers/renderers/RadioRenderer.tsx</automated>
    <automated>grep -c 'role="group"' src/features/field-renderers/renderers/CheckboxRenderer.tsx</automated>
    <automated>grep -c "dangerouslySetInnerHTML" src/features/field-renderers/renderers/</automated>
  </verify>
  <acceptance_criteria>
    - CheckboxRenderer.tsx, RadioRenderer.tsx, UnsupportedTypeRenderer.tsx exist and compile
    - CheckboxRenderer renders `<div role="group">` with `aria-labelledby` referencing `{fieldId}-label`
    - RadioRenderer renders `<div role="radiogroup">` with `aria-labelledby` and only one checked input matching `value`
    - UnsupportedTypeRenderer renders `<Badge variant="outline" role="status">` with `aria-label` starting with "Unsupported field type:"
    - UnsupportedTypeRenderer has NO `onChange` invocation (read-only — D-07 + CTRL-07)
    - UnsupportedTypeRenderer never throws — covered by "does not crash" test (CTRL-07 SC #2)
    - All three test files have zero `it.todo` and pass; total assertions across the three files >= 14 (5 + 4 + 5)
    - `grep -r "dangerouslySetInnerHTML" src/features/field-renderers/renderers/` returns 0 matches (XSS mitigation T-20-07)
    - No file imports `@tauri-apps/api/core` (D-01)
  </acceptance_criteria>
  <done>
    Checkbox / Radio / Unsupported renderers + tests complete. CTRL-06 closed at the file level; CTRL-07 file-level closed (registry routing in Plan 05 will close the discrimination half).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → controlled inputs | All inputs are controlled; values flow up via onChange. No internal state, no persistence inside Phase 20. |
| field.name / field.allowedValues → DOM text | Field schemas come from Phase 17 discovery (trusted but originally from Jira API responses) and are rendered as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Tampering (XSS) | Renderer label/option text rendering | mitigate | All renderers render strings as React text nodes (`{label}`, `{optLabel}`); no `dangerouslySetInnerHTML` anywhere — verified by grep gate in Task 2 |
| T-20-08 | Tampering | UrlRenderer storing URLs | mitigate | UrlRenderer is `<input type="url">` only — stores values, does NOT navigate. No `<a href>` from Phase 20. Phase 22 may render preview links elsewhere with their own sanitization. |
| T-20-09 | Tampering (XSS) | UnsupportedTypeRenderer rendering field.schema.type | mitigate | `field.schema.type` is a discriminant string from FieldSchemaType union — values are typed and React-escaped on render |
| T-20-10 | Information Disclosure | NumberRenderer for sensitive numeric fields | accept | No PII handling in Phase 20 renderers — Phase 22/23 owns the audit log redaction |
</threat_model>

<verification>
- `npm test -- src/features/field-renderers` exits 0 — six test files now passing (StringRenderer, TextAreaRenderer, UrlRenderer, CheckboxRenderer, RadioRenderer, UnsupportedTypeRenderer); other stubs remain pending.
- `npx tsc --noEmit` exits 0.
- `npm test` (full suite) exits 0 — no regression in existing tests.
- `grep -r "dangerouslySetInnerHTML" src/features/field-renderers/` returns 0.
- `grep -r "@tauri-apps/api/core" src/features/field-renderers/` returns 0 (D-01).
</verification>

<success_criteria>
1. Nine renderer files exist under `src/features/field-renderers/renderers/` and compile.
2. Six test files (StringRenderer, TextAreaRenderer, UrlRenderer, CheckboxRenderer, RadioRenderer, UnsupportedTypeRenderer) have zero it.todo and all assertions pass.
3. CTRL-01 (text/multi-line/URL), CTRL-05 (date/datetime/number), CTRL-06 (checkbox/radio), and the file-creation half of CTRL-07 (UnsupportedTypeRenderer) are covered.
4. UnsupportedTypeRenderer never throws for ANY type value (proven by "does not crash" test).
5. No renderer imports invoke (D-01); no dangerouslySetInnerHTML anywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/20-renderer-registry-field-type-aware-controls/20-03-SUMMARY.md` documenting:
- Final filenames + line counts for the 9 renderers
- Test counts (passing assertions per file)
- ARIA roles used (group, radiogroup, status)
- Confirmation: zero invoke imports, zero dangerouslySetInnerHTML, zero default exports
</output>
