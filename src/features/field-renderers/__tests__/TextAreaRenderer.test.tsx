import { describe, it } from 'vitest';

describe('TextAreaRenderer', () => {
  it.todo('CTRL-01 renders a textarea element with the current value');
  it.todo('CTRL-01 calls onChange with new string when user types');
  it.todo('CTRL-01 textarea has min-h-[80px] class');
});
