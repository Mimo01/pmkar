import { describe, it } from 'vitest';

describe('MultiUserPickerRenderer', () => {
  it.todo('CTRL-02 renders one chip per JiraUser in value array');
  it.todo('CTRL-02 calls onChange with new array when chip remove button clicked');
  it.todo('CTRL-02 calls onChange appending JiraUser when option selected');
  it.todo('CTRL-02 ignores UnresolvedPerson values — renders only JiraUser shapes (D-02)');
});
