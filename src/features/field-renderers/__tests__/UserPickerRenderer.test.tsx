import { describe, it } from 'vitest';

describe('UserPickerRenderer', () => {
  it.todo('CTRL-02 renders the selected user displayName when value is set');
  it.todo('CTRL-02 calls onSearch(initialQuery) on mount when initialQuery is provided (D-03)');
  it.todo('CTRL-02 calls onSearch with debounced query when user types');
  it.todo('CTRL-02 calls onChange with selected JiraUser when option clicked');
  it.todo('CTRL-02 does not log onSearch query params (security: PII initialQuery)');
});
